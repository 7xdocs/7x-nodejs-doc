{
  "type": "module",
  "source": "doc/api/stream.md",
  "modules": [
    {
      "textRaw": "Stream",
      "name": "stream",
      "introduced_in": "v0.10.0",
      "stability": 2,
      "stabilityText": "Stable",
      "desc": "<p><strong>Source Code:</strong> <a href=\"https://github.com/nodejs/node/blob/v24.10.1/lib/stream.js\">lib/stream.js</a></p>\n<p>流是 Node.js 中处理流数据的抽象接口。<code>node:stream</code> 模块提供了实现流接口的 API。</p>\n<p>Node.js 提供了许多流对象。例如，<a href=\"http.html#class-httpincomingmessage\">发往 HTTP 服务器的请求</a> 和 <a href=\"process.html#processstdout\"><code>process.stdout</code></a> 都是流的实例。</p>\n<p>流可以是可读的、可写的，或者两者兼有。所有流都是 <a href=\"events.html#class-eventemitter\"><code>EventEmitter</code></a> 的实例。</p>\n<p>要访问 <code>node:stream</code> 模块：</p>\n<pre><code class=\"language-js\">const stream = require('node:stream');\n</code></pre>\n<p><code>node:stream</code> 模块对于创建新类型的流实例非常有用。通常不需要使用 <code>node:stream</code> 模块来消费流。</p>",
      "modules": [
        {
          "textRaw": "本文档的组织结构",
          "name": "本文档的组织结构",
          "desc": "<p>本文档包含两个主要部分和一个第三部分用于说明。第一部分解释了如何在应用程序中使用现有的流。第二部分解释了如何创建新类型的流。</p>",
          "type": "module",
          "displayName": "本文档的组织结构"
        },
        {
          "textRaw": "流的类型",
          "name": "流的类型",
          "desc": "<p>Node.js 中有四种基本的流类型：</p>\n<ul>\n<li><a href=\"#class-streamwritable\"><code>Writable</code></a>：可以向其写入数据的流（例如 <a href=\"fs.html#fscreatewritestreampath-options\"><code>fs.createWriteStream()</code></a>）。</li>\n<li><a href=\"#class-streamreadable\"><code>Readable</code></a>：可以从中读取数据的流（例如 <a href=\"fs.html#fscreatereadstreampath-options\"><code>fs.createReadStream()</code></a>）。</li>\n<li><a href=\"#class-streamduplex\"><code>Duplex</code></a>：既是 <code>Readable</code> 又是 <code>Writable</code> 的流（例如 <a href=\"net.html#class-netsocket\"><code>net.Socket</code></a>）。</li>\n<li><a href=\"#class-streamtransform\"><code>Transform</code></a>：<code>Duplex</code> 流，可以在写入和读取时修改或转换数据（例如 <a href=\"zlib.html#zlibcreatedeflateoptions\"><code>zlib.createDeflate()</code></a>）。</li>\n</ul>\n<p>此外，该模块还包括实用函数 <a href=\"#streamduplexpairoptions\"><code>stream.duplexPair()</code></a>、<a href=\"#streampipelinesource-transforms-destination-callback\"><code>stream.pipeline()</code></a>、<a href=\"#streamfinishedstream-options-callback\"><code>stream.finished()</code></a>、<a href=\"#streamreadablefromiterable-options\"><code>stream.Readable.from()</code></a> 和 <a href=\"#streamaddabortsignalsignal-stream\"><code>stream.addAbortSignal()</code></a>。</p>",
          "modules": [
            {
              "textRaw": "Streams Promises API",
              "name": "streams_promises_api",
              "meta": {
                "added": [
                  "v15.0.0"
                ],
                "changes": []
              },
              "desc": "<p><code>stream/promises</code> API 提供了一组返回 <code>Promise</code> 对象的异步实用函数，而不是使用回调。可以通过 <code>require('node:stream/promises')</code> 或 <code>require('node:stream').promises</code> 访问该 API。</p>",
              "type": "module",
              "displayName": "Streams Promises API"
            },
            {
              "textRaw": "对象模式",
              "name": "对象模式",
              "desc": "<p>所有由 Node.js API 创建的流都专门操作字符串、<a href=\"buffer.html#class-buffer\" class=\"type\">&lt;Buffer&gt;</a>、<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypedArray\" class=\"type\">&lt;TypedArray&gt;</a> 和 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/DataView\" class=\"type\">&lt;DataView&gt;</a> 对象：</p>\n<ul>\n<li><code>字符串</code> 和 <code>Buffers</code> 是与流一起使用的最常见类型。</li>\n<li><code>TypedArray</code> 和 <code>DataView</code> 让你可以处理像 <code>Int32Array</code> 或 <code>Uint8Array</code> 这样的二进制数据。当你将 TypedArray 或 DataView 写入流时，Node.js 处理原始字节。</li>\n</ul>\n<p>然而，流实现也可以与其他类型的 JavaScript 值一起工作（除了 <code>null</code>，它在流中有特殊用途）。这样的流被认为是工作在 \"对象模式\"。</p>\n<p>流实例在创建流时使用 <code>objectMode</code> 选项切换到对象模式。尝试将现有流切换到对象模式是不安全的。</p>",
              "type": "module",
              "displayName": "对象模式"
            }
          ],
          "methods": [
            {
              "textRaw": "`stream.pipeline(streams[, options])`",
              "type": "method",
              "name": "pipeline",
              "meta": {
                "added": [
                  "v15.0.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v18.0.0",
                      "v17.2.0",
                      "v16.14.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/40886",
                    "description": "Add the `end` option, which can be set to `false` to prevent automatically closing the destination stream when the source ends."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]}",
                      "name": "streams",
                      "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]"
                    },
                    {
                      "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function}",
                      "name": "source",
                      "type": "Stream|Iterable|AsyncIterable|Function",
                      "options": [
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`...transforms` {Stream|Function}",
                      "name": "...transforms",
                      "type": "Stream|Function",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`destination` {Stream|Function}",
                      "name": "destination",
                      "type": "Stream|Function",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`options` {Object} 管道选项",
                      "name": "options",
                      "type": "Object",
                      "desc": "管道选项",
                      "options": [
                        {
                          "textRaw": "`signal` {AbortSignal}",
                          "name": "signal",
                          "type": "AbortSignal"
                        },
                        {
                          "textRaw": "`end` {boolean} 当源流结束时是否结束目标流。转换流总是会结束，即使该值为 `false`。**默认值：** `true`。",
                          "name": "end",
                          "type": "boolean",
                          "desc": "当源流结束时是否结束目标流。转换流总是会结束，即使该值为 `false`。**默认值：** `true`。"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{Promise} 当管道完成时兑现。",
                      "name": "返回：",
                      "type": "Promise",
                      "desc": "当管道完成时兑现。"
                    }
                  ]
                }
              ],
              "desc": "<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\nasync function run() {\n  await pipeline(\n    fs.createReadStream('archive.tar'),\n    zlib.createGzip(),\n    fs.createWriteStream('archive.tar.gz'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\nimport { createGzip } from 'node:zlib';\n\nawait pipeline(\n  createReadStream('archive.tar'),\n  createGzip(),\n  createWriteStream('archive.tar.gz'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p>要使用 <code>AbortSignal</code>，将其作为最后一个参数传入选项对象中。当信号被中止时，将在底层管道上调用 <code>destroy</code>，并附带一个 <code>AbortError</code>。</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\nasync function run() {\n  const ac = new AbortController();\n  const signal = ac.signal;\n\n  setImmediate(() => ac.abort());\n  await pipeline(\n    fs.createReadStream('archive.tar'),\n    zlib.createGzip(),\n    fs.createWriteStream('archive.tar.gz'),\n    { signal },\n  );\n}\n\nrun().catch(console.error); // AbortError\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\nimport { createGzip } from 'node:zlib';\n\nconst ac = new AbortController();\nconst { signal } = ac;\nsetImmediate(() => ac.abort());\ntry {\n  await pipeline(\n    createReadStream('archive.tar'),\n    createGzip(),\n    createWriteStream('archive.tar.gz'),\n    { signal },\n  );\n} catch (err) {\n  console.error(err); // AbortError\n}\n</code></pre>\n<p><code>pipeline</code> API 还支持异步生成器：</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\n\nasync function run() {\n  await pipeline(\n    fs.createReadStream('lowercase.txt'),\n    async function* (source, { signal }) {\n      source.setEncoding('utf8');  // 处理字符串而不是 `Buffer`。\n      for await (const chunk of source) {\n        yield await processChunk(chunk, { signal });\n      }\n    },\n    fs.createWriteStream('uppercase.txt'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\n\nawait pipeline(\n  createReadStream('lowercase.txt'),\n  async function* (source, { signal }) {\n    source.setEncoding('utf8');  // 处理字符串而不是 `Buffer`。\n    for await (const chunk of source) {\n      yield await processChunk(chunk, { signal });\n    }\n  },\n  createWriteStream('uppercase.txt'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p>请记住处理传递给异步生成器的 <code>signal</code> 参数。特别是在异步生成器是管道的源（即第一个参数）的情况下，否则管道将永远不会完成。</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\n\nasync function run() {\n  await pipeline(\n    async function* ({ signal }) {\n      await someLongRunningfn({ signal });\n      yield 'asd';\n    },\n    fs.createWriteStream('uppercase.txt'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport fs from 'node:fs';\nawait pipeline(\n  async function* ({ signal }) {\n    await someLongRunningfn({ signal });\n    yield 'asd';\n  },\n  fs.createWriteStream('uppercase.txt'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p><code>pipeline</code> API 提供了 <a href=\"#streampipelinesource-transforms-destination-callback\">回调版本</a>。</p>"
            },
            {
              "textRaw": "`stream.pipeline(source[, ...transforms], destination[, options])`",
              "type": "method",
              "name": "pipeline",
              "meta": {
                "added": [
                  "v15.0.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v18.0.0",
                      "v17.2.0",
                      "v16.14.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/40886",
                    "description": "Add the `end` option, which can be set to `false` to prevent automatically closing the destination stream when the source ends."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]}",
                      "name": "streams",
                      "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]"
                    },
                    {
                      "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function}",
                      "name": "source",
                      "type": "Stream|Iterable|AsyncIterable|Function",
                      "options": [
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`...transforms` {Stream|Function}",
                      "name": "...transforms",
                      "type": "Stream|Function",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`destination` {Stream|Function}",
                      "name": "destination",
                      "type": "Stream|Function",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{Promise|AsyncIterable}",
                          "name": "返回：",
                          "type": "Promise|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`options` {Object} 管道选项",
                      "name": "options",
                      "type": "Object",
                      "desc": "管道选项",
                      "options": [
                        {
                          "textRaw": "`signal` {AbortSignal}",
                          "name": "signal",
                          "type": "AbortSignal"
                        },
                        {
                          "textRaw": "`end` {boolean} 当源流结束时是否结束目标流。转换流总是会结束，即使该值为 `false`。**默认值：** `true`。",
                          "name": "end",
                          "type": "boolean",
                          "desc": "当源流结束时是否结束目标流。转换流总是会结束，即使该值为 `false`。**默认值：** `true`。"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{Promise} 当管道完成时兑现。",
                      "name": "返回：",
                      "type": "Promise",
                      "desc": "当管道完成时兑现。"
                    }
                  ]
                }
              ],
              "desc": "<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\nasync function run() {\n  await pipeline(\n    fs.createReadStream('archive.tar'),\n    zlib.createGzip(),\n    fs.createWriteStream('archive.tar.gz'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\nimport { createGzip } from 'node:zlib';\n\nawait pipeline(\n  createReadStream('archive.tar'),\n  createGzip(),\n  createWriteStream('archive.tar.gz'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p>要使用 <code>AbortSignal</code>，将其作为最后一个参数传入选项对象中。当信号被中止时，将在底层管道上调用 <code>destroy</code>，并附带一个 <code>AbortError</code>。</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\nasync function run() {\n  const ac = new AbortController();\n  const signal = ac.signal;\n\n  setImmediate(() => ac.abort());\n  await pipeline(\n    fs.createReadStream('archive.tar'),\n    zlib.createGzip(),\n    fs.createWriteStream('archive.tar.gz'),\n    { signal },\n  );\n}\n\nrun().catch(console.error); // AbortError\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\nimport { createGzip } from 'node:zlib';\n\nconst ac = new AbortController();\nconst { signal } = ac;\nsetImmediate(() => ac.abort());\ntry {\n  await pipeline(\n    createReadStream('archive.tar'),\n    createGzip(),\n    createWriteStream('archive.tar.gz'),\n    { signal },\n  );\n} catch (err) {\n  console.error(err); // AbortError\n}\n</code></pre>\n<p><code>pipeline</code> API 还支持异步生成器：</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\n\nasync function run() {\n  await pipeline(\n    fs.createReadStream('lowercase.txt'),\n    async function* (source, { signal }) {\n      source.setEncoding('utf8');  // 处理字符串而不是 `Buffer`。\n      for await (const chunk of source) {\n        yield await processChunk(chunk, { signal });\n      }\n    },\n    fs.createWriteStream('uppercase.txt'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport { createReadStream, createWriteStream } from 'node:fs';\n\nawait pipeline(\n  createReadStream('lowercase.txt'),\n  async function* (source, { signal }) {\n    source.setEncoding('utf8');  // 处理字符串而不是 `Buffer`。\n    for await (const chunk of source) {\n      yield await processChunk(chunk, { signal });\n    }\n  },\n  createWriteStream('uppercase.txt'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p>请记住处理传递给异步生成器的 <code>signal</code> 参数。特别是在异步生成器是管道的源（即第一个参数）的情况下，否则管道将永远不会完成。</p>\n<pre><code class=\"language-cjs\">const { pipeline } = require('node:stream/promises');\nconst fs = require('node:fs');\n\nasync function run() {\n  await pipeline(\n    async function* ({ signal }) {\n      await someLongRunningfn({ signal });\n      yield 'asd';\n    },\n    fs.createWriteStream('uppercase.txt'),\n  );\n  console.log('Pipeline succeeded.');\n}\n\nrun().catch(console.error);\n</code></pre>\n<pre><code class=\"language-mjs\">import { pipeline } from 'node:stream/promises';\nimport fs from 'node:fs';\nawait pipeline(\n  async function* ({ signal }) {\n    await someLongRunningfn({ signal });\n    yield 'asd';\n  },\n  fs.createWriteStream('uppercase.txt'),\n);\nconsole.log('Pipeline succeeded.');\n</code></pre>\n<p><code>pipeline</code> API 提供了 <a href=\"#streampipelinesource-transforms-destination-callback\">回调版本</a>。</p>"
            },
            {
              "textRaw": "`stream.finished(stream[, options])`",
              "type": "method",
              "name": "finished",
              "meta": {
                "added": [
                  "v15.0.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v19.5.0",
                      "v18.14.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46205",
                    "description": "Added support for `ReadableStream` and `WritableStream`."
                  },
                  {
                    "version": [
                      "v19.1.0",
                      "v18.13.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/44862",
                    "description": "The `cleanup` option was added."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {Stream|ReadableStream|WritableStream} 一个可读和/或可写的流/网络流。",
                      "name": "stream",
                      "type": "Stream|ReadableStream|WritableStream",
                      "desc": "一个可读和/或可写的流/网络流。"
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`error` {boolean|undefined}",
                          "name": "error",
                          "type": "boolean|undefined"
                        },
                        {
                          "textRaw": "`readable` {boolean|undefined}",
                          "name": "readable",
                          "type": "boolean|undefined"
                        },
                        {
                          "textRaw": "`writable` {boolean|undefined}",
                          "name": "writable",
                          "type": "boolean|undefined"
                        },
                        {
                          "textRaw": "`signal` {AbortSignal|undefined}",
                          "name": "signal",
                          "type": "AbortSignal|undefined"
                        },
                        {
                          "textRaw": "`cleanup` {boolean|undefined} 如果为 `true`，则在 promise 兑现前移除由此函数注册的监听器。**默认值：** `false`。",
                          "name": "cleanup",
                          "type": "boolean|undefined",
                          "desc": "如果为 `true`，则在 promise 兑现前移除由此函数注册的监听器。**默认值：** `false`。"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{Promise} 当流不再可读或可写时兑现。",
                      "name": "返回：",
                      "type": "Promise",
                      "desc": "当流不再可读或可写时兑现。"
                    }
                  ]
                }
              ],
              "desc": "<pre><code class=\"language-cjs\">const { finished } = require('node:stream/promises');\nconst fs = require('node:fs');\n\nconst rs = fs.createReadStream('archive.tar');\n\nasync function run() {\n  await finished(rs);\n  console.log('Stream is done reading.');\n}\n\nrun().catch(console.error);\nrs.resume(); // 排空流。\n</code></pre>\n<pre><code class=\"language-mjs\">import { finished } from 'node:stream/promises';\nimport { createReadStream } from 'node:fs';\n\nconst rs = createReadStream('archive.tar');\n\nasync function run() {\n  await finished(rs);\n  console.log('Stream is done reading.');\n}\n\nrun().catch(console.error);\nrs.resume(); // 排空流。\n</code></pre>\n<p><code>finished</code> API 也提供了 <a href=\"#streamfinishedstream-options-callback\">回调版本</a>。</p>\n<p><code>stream.finished()</code> 在返回的 promise 被解决或拒绝后，会留下悬空的事件监听器（特别是 <code>'error'</code>、<code>'end'</code>、<code>'finish'</code> 和 <code>'close'</code>）。这样做的原因是，以免（由于不正确的流实现导致的）意外的 <code>'error'</code> 事件引起意外的崩溃。如果不需要此行为，则应将 <code>options.cleanup</code> 设置为 <code>true</code>：</p>\n<pre><code class=\"language-mjs\">await finished(rs, { cleanup: true });\n</code></pre>"
            }
          ],
          "miscs": [
            {
              "textRaw": "缓冲",
              "name": "缓冲",
              "type": "misc",
              "desc": "<p><a href=\"#class-streamwritable\"><code>Writable</code></a> 和 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流都会将数据存储在内部缓冲区中。</p>\n<p>可能缓冲的数据量取决于传递给流构造函数的 <code>highWaterMark</code> 选项。对于普通流，<code>highWaterMark</code> 选项指定了 <a href=\"#highwatermark-discrepancy-after-calling-readablesetencoding\">总字节数</a>。对于工作在对象模式的流，<code>highWaterMark</code> 指定了总对象数。对于操作（但不解码）字符串的流，<code>highWaterMark</code> 指定了 UTF-16 代码单元的总数。</p>\n<p>当实现调用 <a href=\"#readablepushchunk-encoding\"><code>stream.push(chunk)</code></a> 时，数据会在 <code>Readable</code> 流中缓冲。如果流的消费者没有调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a>，数据将停留在内部队列中直到被消费。</p>\n<p>一旦内部读取缓冲区的总大小达到 <code>highWaterMark</code> 指定的阈值，流将暂时停止从底层资源读取数据，直到当前缓冲的数据被消费（也就是说，流将停止调用内部的 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法，该方法用于填充读取缓冲区）。</p>\n<p>当重复调用 <a href=\"#writablewritechunk-encoding-callback\"><code>writable.write(chunk)</code></a> 方法时，数据会在 <code>Writable</code> 流中缓冲。当内部写入缓冲区的总大小低于 <code>highWaterMark</code> 设置的阈值时，调用 <code>writable.write()</code> 将返回 <code>true</code>。一旦内部缓冲区的大小达到或超过 <code>highWaterMark</code>，将返回 <code>false</code>。</p>\n<p><code>stream</code> API 的一个关键目标，特别是 <a href=\"#readablepipedestination-options\"><code>stream.pipe()</code></a> 方法，是将数据缓冲限制在可接受的水平，使得不同速度的源和目标不会压垮可用内存。</p>\n<p><code>highWaterMark</code> 选项是一个阈值，而不是限制：它规定了流在停止请求更多数据之前缓冲的数据量。通常它不强制执行严格的内存限制。特定的流实现可能选择强制执行更严格的限制，但这样做是可选的。</p>\n<p>因为 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 和 <a href=\"#class-streamtransform\"><code>Transform</code></a> 流都是 <code>Readable</code> 和 <code>Writable</code>，所以每个都维护 <em>两个</em> 独立的内部缓冲区，用于读取和写入，允许每一方在维持适当且高效的数据流的同时独立于另一方操作。例如，<a href=\"net.html#class-netsocket\"><code>net.Socket</code></a> 实例是 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流，其 <code>Readable</code> 侧允许消费 <em>从</em> 套接字接收的数据，而其 <code>Writable</code> 侧允许写入数据 <em>到</em> 套接字。因为数据写入套接字的速度可能比接收数据的速度快或慢，每一方都应该独立于另一方操作（和缓冲）。</p>\n<p>内部缓冲的机制是内部实现细节，可能随时更改。然而，对于某些高级实现，可以使用 <code>writable.writableBuffer</code> 或 <code>readable.readableBuffer</code> 检索内部缓冲区。不鼓励使用这些未文档化的属性。</p>"
            }
          ],
          "type": "module",
          "displayName": "流的类型"
        }
      ],
      "methods": [
        {
          "textRaw": "`stream.finished(stream[, options], callback)`",
          "type": "method",
          "name": "finished",
          "meta": {
            "added": [
              "v10.0.0"
            ],
            "changes": [
              {
                "version": "v19.5.0",
                "pr-url": "https://github.com/nodejs/node/pull/46205",
                "description": "Added support for `ReadableStream` and `WritableStream`."
              },
              {
                "version": "v15.11.0",
                "pr-url": "https://github.com/nodejs/node/pull/37354",
                "description": "The `signal` option was added."
              },
              {
                "version": "v14.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/32158",
                "description": "The `finished(stream, cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
              },
              {
                "version": "v14.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/31545",
                "description": "Emitting `'close'` before `'end'` on a `Readable` stream will cause an `ERR_STREAM_PREMATURE_CLOSE` error."
              },
              {
                "version": "v14.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/31509",
                "description": "Callback will be invoked on streams which have already finished before the call to `finished(stream, cb)`."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`stream` {Stream|ReadableStream|WritableStream} 一个可读和/或可写的流/网络流。",
                  "name": "stream",
                  "type": "Stream|ReadableStream|WritableStream",
                  "desc": "一个可读和/或可写的流/网络流。"
                },
                {
                  "textRaw": "`options` {Object}",
                  "name": "options",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`error` {boolean} 如果设置为 `false`，则调用 `emit('error', err)` 不被视为已完成。**默认值：** `true`。",
                      "name": "error",
                      "type": "boolean",
                      "desc": "如果设置为 `false`，则调用 `emit('error', err)` 不被视为已完成。**默认值：** `true`。"
                    },
                    {
                      "textRaw": "`readable` {boolean} 当设置为 `false` 时，即使流可能仍然可读，也会在流结束时调用回调。**默认值：** `true`。",
                      "name": "readable",
                      "type": "boolean",
                      "desc": "当设置为 `false` 时，即使流可能仍然可读，也会在流结束时调用回调。**默认值：** `true`。"
                    },
                    {
                      "textRaw": "`writable` {boolean} 当设置为 `false` 时，即使流可能仍然可写，也会在流结束时调用回调。**默认值：** `true`。",
                      "name": "writable",
                      "type": "boolean",
                      "desc": "当设置为 `false` 时，即使流可能仍然可写，也会在流结束时调用回调。**默认值：** `true`。"
                    },
                    {
                      "textRaw": "`signal` {AbortSignal} 允许中止等待流完成。如果信号被中止，底层流将 _不会_ 被中止。回调将以 `AbortError` 调用。由此函数添加的所有已注册监听器也将被移除。",
                      "name": "signal",
                      "type": "AbortSignal",
                      "desc": "允许中止等待流完成。如果信号被中止，底层流将 _不会_ 被中止。回调将以 `AbortError` 调用。由此函数添加的所有已注册监听器也将被移除。"
                    }
                  ]
                },
                {
                  "textRaw": "`callback` {Function} 一个回调函数，接受一个可选的错误参数。",
                  "name": "callback",
                  "type": "Function",
                  "desc": "一个回调函数，接受一个可选的错误参数。"
                },
                {
                  "textRaw": "返回：{Function} 一个清理函数，移除所有已注册的监听器。",
                  "name": "返回：",
                  "type": "Function",
                  "desc": "一个清理函数，移除所有已注册的监听器。"
                }
              ]
            }
          ],
          "desc": "<p>一个函数，用于在流不再可读、可写或遇到错误或过早关闭事件时得到通知。</p>\n<pre><code class=\"language-js\">const { finished } = require('node:stream');\nconst fs = require('node:fs');\n\nconst rs = fs.createReadStream('archive.tar');\n\nfinished(rs, (err) => {\n  if (err) {\n    console.error('Stream failed.', err);\n  } else {\n    console.log('Stream is done reading.');\n  }\n});\n\nrs.resume(); // 排空流。\n</code></pre>\n<p>在错误处理场景中特别有用，其中流被过早销毁（如中止的 HTTP 请求），并且不会发出 <code>'end'</code> 或 <code>'finish'</code>。</p>\n<p><code>finished</code> API 提供了 <a href=\"#streamfinishedstream-options\">promise 版本</a>。</p>\n<p><code>stream.finished()</code> 在调用 <code>callback</code> 后留下悬空的事件监听器（特别是 <code>'error'</code>、<code>'end'</code>、<code>'finish'</code> 和 <code>'close'</code>）。这样做的原因是，以免（由于不正确的流实现导致的）意外的 <code>'error'</code> 事件引起意外的崩溃。\n如果不需要此行为，则需要在回调中调用返回的清理函数：</p>\n<pre><code class=\"language-js\">const cleanup = finished(rs, (err) => {\n  cleanup();\n  // ...\n});\n</code></pre>"
        },
        {
          "textRaw": "`stream.pipeline(source[, ...transforms], destination, callback)`",
          "type": "method",
          "name": "pipeline",
          "meta": {
            "added": [
              "v10.0.0"
            ],
            "changes": [
              {
                "version": [
                  "v19.7.0",
                  "v18.16.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/46307",
                "description": "Added support for webstreams."
              },
              {
                "version": "v18.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/41678",
                "description": "Passing an invalid callback to the `callback` argument now throws `ERR_INVALID_ARG_TYPE` instead of `ERR_INVALID_CALLBACK`."
              },
              {
                "version": "v14.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/32158",
                "description": "The `pipeline(..., cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
              },
              {
                "version": "v13.10.0",
                "pr-url": "https://github.com/nodejs/node/pull/31223",
                "description": "Add support for async generators."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]}",
                  "name": "streams",
                  "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]"
                },
                {
                  "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function|ReadableStream}",
                  "name": "source",
                  "type": "Stream|Iterable|AsyncIterable|Function|ReadableStream",
                  "options": [
                    {
                      "textRaw": "返回：{Iterable|AsyncIterable}",
                      "name": "返回：",
                      "type": "Iterable|AsyncIterable"
                    }
                  ]
                },
                {
                  "textRaw": "`...transforms` {Stream|Function|TransformStream}",
                  "name": "...transforms",
                  "type": "Stream|Function|TransformStream",
                  "options": [
                    {
                      "textRaw": "`source` {AsyncIterable}",
                      "name": "source",
                      "type": "AsyncIterable"
                    },
                    {
                      "textRaw": "返回：{AsyncIterable}",
                      "name": "返回：",
                      "type": "AsyncIterable"
                    }
                  ]
                },
                {
                  "textRaw": "`destination` {Stream|Function|WritableStream}",
                  "name": "destination",
                  "type": "Stream|Function|WritableStream",
                  "options": [
                    {
                      "textRaw": "`source` {AsyncIterable}",
                      "name": "source",
                      "type": "AsyncIterable"
                    },
                    {
                      "textRaw": "返回：{AsyncIterable|Promise}",
                      "name": "返回：",
                      "type": "AsyncIterable|Promise"
                    }
                  ]
                },
                {
                  "textRaw": "`callback` {Function} 当管道完全完成时调用。",
                  "name": "callback",
                  "type": "Function",
                  "desc": "当管道完全完成时调用。",
                  "options": [
                    {
                      "textRaw": "`err` {Error}",
                      "name": "err",
                      "type": "Error"
                    },
                    {
                      "textRaw": "`val` 由 `destination` 返回的 `Promise` 的解决值。",
                      "name": "val",
                      "desc": "由 `destination` 返回的 `Promise` 的解决值。"
                    }
                  ]
                },
                {
                  "textRaw": "返回：{Stream}",
                  "name": "返回：",
                  "type": "Stream"
                }
              ]
            }
          ],
          "desc": "<p>一个模块方法，用于在流和生成器之间管道传输，转发错误并正确清理，并在管道完成时提供回调。</p>\n<pre><code class=\"language-js\">const { pipeline } = require('node:stream');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\n// 使用 pipeline API 轻松将一系列流管道传输在一起，并在管道完全完成时得到通知。\n\n// 一个高效地 gzip 可能巨大的 tar 文件的管道：\n\npipeline(\n  fs.createReadStream('archive.tar'),\n  zlib.createGzip(),\n  fs.createWriteStream('archive.tar.gz'),\n  (err) => {\n    if (err) {\n      console.error('Pipeline failed.', err);\n    } else {\n      console.log('Pipeline succeeded.');\n    }\n  },\n);\n</code></pre>\n<p><code>pipeline</code> API 提供了一个 <a href=\"#streampipelinesource-transforms-destination-options\">promise 版本</a>。</p>\n<p><code>stream.pipeline()</code> 将在所有流上调用 <code>stream.destroy(err)</code>，除了：</p>\n<ul>\n<li>已发出 <code>'end'</code> 或 <code>'close'</code> 的 <code>Readable</code> 流。</li>\n<li>已发出 <code>'finish'</code> 或 <code>'close'</code> 的 <code>Writable</code> 流。</li>\n</ul>\n<p><code>stream.pipeline()</code> 在调用 <code>callback</code> 后在流上留下悬空的事件监听器。在故障后重用流的情况下，这可能导致事件监听器泄漏和错误被吞掉。如果最后一个流是可读的，则将移除悬空的事件监听器，以便以后可以消费最后一个流。</p>\n<p><code>stream.pipeline()</code> 在引发错误时关闭所有流。\n将 <code>IncomingRequest</code> 与 <code>pipeline</code> 一起使用可能导致意外行为，因为它会在不发送预期响应的情况下销毁套接字。\n请参见下面的示例：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst http = require('node:http');\nconst { pipeline } = require('node:stream');\n\nconst server = http.createServer((req, res) => {\n  const fileStream = fs.createReadStream('./fileNotExist.txt');\n  pipeline(fileStream, res, (err) => {\n    if (err) {\n      console.log(err); // 没有这样的文件\n      // 一旦 `pipeline` 已经销毁了套接字，此消息将无法发送\n      return res.end('error!!!');\n    }\n  });\n});\n</code></pre>"
        },
        {
          "textRaw": "`stream.pipeline(streams, callback)`",
          "type": "method",
          "name": "pipeline",
          "meta": {
            "added": [
              "v10.0.0"
            ],
            "changes": [
              {
                "version": [
                  "v19.7.0",
                  "v18.16.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/46307",
                "description": "Added support for webstreams."
              },
              {
                "version": "v18.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/41678",
                "description": "Passing an invalid callback to the `callback` argument now throws `ERR_INVALID_ARG_TYPE` instead of `ERR_INVALID_CALLBACK`."
              },
              {
                "version": "v14.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/32158",
                "description": "The `pipeline(..., cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
              },
              {
                "version": "v13.10.0",
                "pr-url": "https://github.com/nodejs/node/pull/31223",
                "description": "Add support for async generators."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]}",
                  "name": "streams",
                  "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]"
                },
                {
                  "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function|ReadableStream}",
                  "name": "source",
                  "type": "Stream|Iterable|AsyncIterable|Function|ReadableStream",
                  "options": [
                    {
                      "textRaw": "返回：{Iterable|AsyncIterable}",
                      "name": "返回：",
                      "type": "Iterable|AsyncIterable"
                    }
                  ]
                },
                {
                  "textRaw": "`...transforms` {Stream|Function|TransformStream}",
                  "name": "...transforms",
                  "type": "Stream|Function|TransformStream",
                  "options": [
                    {
                      "textRaw": "`source` {AsyncIterable}",
                      "name": "source",
                      "type": "AsyncIterable"
                    },
                    {
                      "textRaw": "返回：{AsyncIterable}",
                      "name": "返回：",
                      "type": "AsyncIterable"
                    }
                  ]
                },
                {
                  "textRaw": "`destination` {Stream|Function|WritableStream}",
                  "name": "destination",
                  "type": "Stream|Function|WritableStream",
                  "options": [
                    {
                      "textRaw": "`source` {AsyncIterable}",
                      "name": "source",
                      "type": "AsyncIterable"
                    },
                    {
                      "textRaw": "返回：{AsyncIterable|Promise}",
                      "name": "返回：",
                      "type": "AsyncIterable|Promise"
                    }
                  ]
                },
                {
                  "textRaw": "`callback` {Function} 当管道完全完成时调用。",
                  "name": "callback",
                  "type": "Function",
                  "desc": "当管道完全完成时调用。",
                  "options": [
                    {
                      "textRaw": "`err` {Error}",
                      "name": "err",
                      "type": "Error"
                    },
                    {
                      "textRaw": "`val` 由 `destination` 返回的 `Promise` 的解决值。",
                      "name": "val",
                      "desc": "由 `destination` 返回的 `Promise` 的解决值。"
                    }
                  ]
                },
                {
                  "textRaw": "返回：{Stream}",
                  "name": "返回：",
                  "type": "Stream"
                }
              ]
            }
          ],
          "desc": "<p>一个模块方法，用于在流和生成器之间管道传输，转发错误并正确清理，并在管道完成时提供回调。</p>\n<pre><code class=\"language-js\">const { pipeline } = require('node:stream');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\n// 使用 pipeline API 轻松将一系列流管道传输在一起，并在管道完全完成时得到通知。\n\n// 一个高效地 gzip 可能巨大的 tar 文件的管道：\n\npipeline(\n  fs.createReadStream('archive.tar'),\n  zlib.createGzip(),\n  fs.createWriteStream('archive.tar.gz'),\n  (err) => {\n    if (err) {\n      console.error('Pipeline failed.', err);\n    } else {\n      console.log('Pipeline succeeded.');\n    }\n  },\n);\n</code></pre>\n<p><code>pipeline</code> API 提供了一个 <a href=\"#streampipelinesource-transforms-destination-options\">promise 版本</a>。</p>\n<p><code>stream.pipeline()</code> 将在所有流上调用 <code>stream.destroy(err)</code>，除了：</p>\n<ul>\n<li>已发出 <code>'end'</code> 或 <code>'close'</code> 的 <code>Readable</code> 流。</li>\n<li>已发出 <code>'finish'</code> 或 <code>'close'</code> 的 <code>Writable</code> 流。</li>\n</ul>\n<p><code>stream.pipeline()</code> 在调用 <code>callback</code> 后在流上留下悬空的事件监听器。在故障后重用流的情况下，这可能导致事件监听器泄漏和错误被吞掉。如果最后一个流是可读的，则将移除悬空的事件监听器，以便以后可以消费最后一个流。</p>\n<p><code>stream.pipeline()</code> 在引发错误时关闭所有流。\n将 <code>IncomingRequest</code> 与 <code>pipeline</code> 一起使用可能导致意外行为，因为它会在不发送预期响应的情况下销毁套接字。\n请参见下面的示例：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst http = require('node:http');\nconst { pipeline } = require('node:stream');\n\nconst server = http.createServer((req, res) => {\n  const fileStream = fs.createReadStream('./fileNotExist.txt');\n  pipeline(fileStream, res, (err) => {\n    if (err) {\n      console.log(err); // 没有这样的文件\n      // 一旦 `pipeline` 已经销毁了套接字，此消息将无法发送\n      return res.end('error!!!');\n    }\n  });\n});\n</code></pre>"
        },
        {
          "textRaw": "`stream.compose(...streams)`",
          "type": "method",
          "name": "compose",
          "meta": {
            "added": [
              "v16.9.0"
            ],
            "changes": [
              {
                "version": [
                  "v21.1.0",
                  "v20.10.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/50187",
                "description": "Added support for stream class."
              },
              {
                "version": [
                  "v19.8.0",
                  "v18.16.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/46675",
                "description": "Added support for webstreams."
              }
            ]
          },
          "stability": 1,
          "stabilityText": "`stream.compose` 是实验性的。",
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]|Duplex\\[]|Function}",
                  "name": "streams",
                  "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]|Duplex\\[]|Function"
                },
                {
                  "textRaw": "返回：{stream.Duplex}",
                  "name": "返回：",
                  "type": "stream.Duplex"
                }
              ]
            }
          ],
          "desc": "<p>将两个或多个流组合成一个 <code>Duplex</code> 流，该流写入第一个流并从最后一个流读取。每个提供的流都通过 <code>stream.pipeline</code> 管道传输到下一个流。如果任何流出错，则所有流都将被销毁，包括外部的 <code>Duplex</code> 流。</p>\n<p>因为 <code>stream.compose</code> 返回一个新的流，该流又可以（并且应该）被管道传输到其他流，它支持组合。相反，当将流传递给 <code>stream.pipeline</code> 时，通常第一个流是可读流，最后一个流是可写流，形成一个闭合电路。</p>\n<p>如果传递一个 <code>Function</code>，它必须是一个工厂方法，接受一个 <code>source</code> <code>Iterable</code>。</p>\n<pre><code class=\"language-mjs\">import { compose, Transform } from 'node:stream';\n\nconst removeSpaces = new Transform({\n  transform(chunk, encoding, callback) {\n    callback(null, String(chunk).replace(' ', ''));\n  },\n});\n\nasync function* toUpper(source) {\n  for await (const chunk of source) {\n    yield String(chunk).toUpperCase();\n  }\n}\n\nlet res = '';\nfor await (const buf of compose(removeSpaces, toUpper).end('hello world')) {\n  res += buf;\n}\n\nconsole.log(res); // 打印 'HELLOWORLD'\n</code></pre>\n<p><code>stream.compose</code> 可用于将异步可迭代对象、生成器和函数转换为流。</p>\n<ul>\n<li><code>AsyncIterable</code> 转换为可读的 <code>Duplex</code>。不能产生 <code>null</code>。</li>\n<li><code>AsyncGeneratorFunction</code> 转换为可读/可写的转换 <code>Duplex</code>。必须将源 <code>AsyncIterable</code> 作为第一个参数。不能产生 <code>null</code>。</li>\n<li><code>AsyncFunction</code> 转换为可写的 <code>Duplex</code>。必须返回 <code>null</code> 或 <code>undefined</code>。</li>\n</ul>\n<pre><code class=\"language-mjs\">import { compose } from 'node:stream';\nimport { finished } from 'node:stream/promises';\n\n// 将 AsyncIterable 转换为可读的 Duplex。\nconst s1 = compose(async function*() {\n  yield 'Hello';\n  yield 'World';\n}());\n\n// 将 AsyncGenerator 转换为转换 Duplex。\nconst s2 = compose(async function*(source) {\n  for await (const chunk of source) {\n    yield String(chunk).toUpperCase();\n  }\n});\n\nlet res = '';\n\n// 将 AsyncFunction 转换为可写的 Duplex。\nconst s3 = compose(async function(source) {\n  for await (const chunk of source) {\n    res += chunk;\n  }\n});\n\nawait finished(compose(s1, s2, s3));\n\nconsole.log(res); // 打印 'HELLOWORLD'\n</code></pre>\n<p>有关 <code>stream.compose</code> 作为操作符的信息，请参阅 <a href=\"#readablecomposestream-options\"><code>readable.compose(stream)</code></a>。</p>"
        },
        {
          "textRaw": "`stream.isErrored(stream)`",
          "type": "method",
          "name": "isErrored",
          "meta": {
            "added": [
              "v17.3.0",
              "v16.14.0"
            ],
            "changes": [
              {
                "version": [
                  "v24.0.0",
                  "v22.17.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`stream` {Readable|Writable|Duplex|WritableStream|ReadableStream}",
                  "name": "stream",
                  "type": "Readable|Writable|Duplex|WritableStream|ReadableStream"
                },
                {
                  "textRaw": "返回：{boolean}",
                  "name": "返回：",
                  "type": "boolean"
                }
              ]
            }
          ],
          "desc": "<p>返回流是否遇到错误。</p>"
        },
        {
          "textRaw": "`stream.isReadable(stream)`",
          "type": "method",
          "name": "isReadable",
          "meta": {
            "added": [
              "v17.4.0",
              "v16.14.0"
            ],
            "changes": [
              {
                "version": [
                  "v24.0.0",
                  "v22.17.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`stream` {Readable|Duplex|ReadableStream}",
                  "name": "stream",
                  "type": "Readable|Duplex|ReadableStream"
                },
                {
                  "textRaw": "返回：{boolean|null} - 仅当 `stream` 不是有效的 `Readable`、`Duplex` 或 `ReadableStream` 时返回 `null`。",
                  "name": "返回：",
                  "type": "boolean|null",
                  "desc": "仅当 `stream` 不是有效的 `Readable`、`Duplex` 或 `ReadableStream` 时返回 `null`。"
                }
              ]
            }
          ],
          "desc": "<p>返回流是否可读。</p>"
        },
        {
          "textRaw": "`stream.isWritable(stream)`",
          "type": "method",
          "name": "isWritable",
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`stream` {Writable|Duplex|WritableStream}",
                  "name": "stream",
                  "type": "Writable|Duplex|WritableStream"
                },
                {
                  "textRaw": "返回：{boolean|null} - 仅当 `stream` 不是有效的 `Writable`、`Duplex` 或 `WritableStream` 时返回 `null`。",
                  "name": "返回：",
                  "type": "boolean|null",
                  "desc": "仅当 `stream` 不是有效的 `Writable`、`Duplex` 或 `WritableStream` 时返回 `null`。"
                }
              ]
            }
          ],
          "desc": "<p>返回流是否可写。</p>"
        },
        {
          "textRaw": "`stream.Readable.from(iterable[, options])`",
          "type": "method",
          "name": "from",
          "meta": {
            "added": [
              "v12.3.0",
              "v10.17.0"
            ],
            "changes": []
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`iterable` {Iterable} 实现 `Symbol.asyncIterator` 或 `Symbol.iterator` 可迭代协议的对象。如果传递了空值，则发出 'error' 事件。",
                  "name": "iterable",
                  "type": "Iterable",
                  "desc": "实现 `Symbol.asyncIterator` 或 `Symbol.iterator` 可迭代协议的对象。如果传递了空值，则发出 'error' 事件。"
                },
                {
                  "textRaw": "`options` {Object} 提供给 `new stream.Readable([options])` 的选项。默认情况下，`Readable.from()` 会将 `options.objectMode` 设置为 `true`，除非通过将 `options.objectMode` 设置为 `false` 明确选择退出。",
                  "name": "options",
                  "type": "Object",
                  "desc": "提供给 `new stream.Readable([options])` 的选项。默认情况下，`Readable.from()` 会将 `options.objectMode` 设置为 `true`，除非通过将 `options.objectMode` 设置为 `false` 明确选择退出。"
                },
                {
                  "textRaw": "返回：{stream.Readable}",
                  "name": "返回：",
                  "type": "stream.Readable"
                }
              ]
            }
          ],
          "desc": "<p>一个实用方法，用于从迭代器创建可读流。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nasync function * generate() {\n  yield 'hello';\n  yield 'streams';\n}\n\nconst readable = Readable.from(generate());\n\nreadable.on('data', (chunk) => {\n  console.log(chunk);\n});\n</code></pre>\n<p>调用 <code>Readable.from(string)</code> 或 <code>Readable.from(buffer)</code> 不会迭代字符串或缓冲区以匹配其他流的语义，这是出于性能原因。</p>\n<p>如果传递包含 promise 的 <code>Iterable</code> 对象作为参数，可能会导致未处理的拒绝。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nReadable.from([\n  new Promise((resolve) => setTimeout(resolve('1'), 1500)),\n  new Promise((_, reject) => setTimeout(reject(new Error('2')), 1000)), // 未处理的拒绝\n]);\n</code></pre>"
        },
        {
          "textRaw": "`stream.Readable.fromWeb(readableStream[, options])`",
          "type": "method",
          "name": "fromWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`readableStream` {ReadableStream}",
                  "name": "readableStream",
                  "type": "ReadableStream"
                },
                {
                  "textRaw": "`options` {Object}",
                  "name": "options",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`encoding` {string}",
                      "name": "encoding",
                      "type": "string"
                    },
                    {
                      "textRaw": "`highWaterMark` {number}",
                      "name": "highWaterMark",
                      "type": "number"
                    },
                    {
                      "textRaw": "`objectMode` {boolean}",
                      "name": "objectMode",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`signal` {AbortSignal}",
                      "name": "signal",
                      "type": "AbortSignal"
                    }
                  ]
                },
                {
                  "textRaw": "返回：{stream.Readable}",
                  "name": "返回：",
                  "type": "stream.Readable"
                }
              ]
            }
          ]
        },
        {
          "textRaw": "`stream.Readable.isDisturbed(stream)`",
          "type": "method",
          "name": "isDisturbed",
          "meta": {
            "added": [
              "v16.8.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`stream` {stream.Readable|ReadableStream}",
                  "name": "stream",
                  "type": "stream.Readable|ReadableStream"
                },
                {
                  "textRaw": "返回：`boolean`",
                  "name": "返回：",
                  "desc": "boolean`"
                }
              ]
            }
          ],
          "desc": "<p>返回流是否已被读取或取消。</p>"
        },
        {
          "textRaw": "`stream.Readable.toWeb(streamReadable[, options])`",
          "type": "method",
          "name": "toWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              },
              {
                "version": [
                  "v18.7.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/43515",
                "description": "include strategy options on Readable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streamReadable` {stream.Readable}",
                  "name": "streamReadable",
                  "type": "stream.Readable"
                },
                {
                  "textRaw": "`options` {Object}",
                  "name": "options",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`strategy` {Object}",
                      "name": "strategy",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`highWaterMark` {number} 在从给定的 `stream.Readable` 读取时应用背压之前，创建的 `ReadableStream` 的最大内部队列大小。如果未提供值，将从给定的 `stream.Readable` 中获取。",
                          "name": "highWaterMark",
                          "type": "number",
                          "desc": "在从给定的 `stream.Readable` 读取时应用背压之前，创建的 `ReadableStream` 的最大内部队列大小。如果未提供值，将从给定的 `stream.Readable` 中获取。"
                        },
                        {
                          "textRaw": "`size` {Function} 一个函数，用于计算给定数据块的大小。如果未提供值，所有数据块的大小将为 `1`。",
                          "name": "size",
                          "type": "Function",
                          "desc": "一个函数，用于计算给定数据块的大小。如果未提供值，所有数据块的大小将为 `1`。",
                          "options": [
                            {
                              "textRaw": "`chunk` {any}",
                              "name": "chunk",
                              "type": "any"
                            },
                            {
                              "textRaw": "返回：{number}",
                              "name": "返回：",
                              "type": "number"
                            }
                          ]
                        }
                      ]
                    }
                  ]
                },
                {
                  "textRaw": "返回：{ReadableStream}",
                  "name": "返回：",
                  "type": "ReadableStream"
                }
              ]
            }
          ]
        },
        {
          "textRaw": "`stream.Writable.fromWeb(writableStream[, options])`",
          "type": "method",
          "name": "fromWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`writableStream` {WritableStream}",
                  "name": "writableStream",
                  "type": "WritableStream"
                },
                {
                  "textRaw": "`options` {Object}",
                  "name": "options",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`decodeStrings` {boolean}",
                      "name": "decodeStrings",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`highWaterMark` {number}",
                      "name": "highWaterMark",
                      "type": "number"
                    },
                    {
                      "textRaw": "`objectMode` {boolean}",
                      "name": "objectMode",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`signal` {AbortSignal}",
                      "name": "signal",
                      "type": "AbortSignal"
                    }
                  ]
                },
                {
                  "textRaw": "返回：{stream.Writable}",
                  "name": "返回：",
                  "type": "stream.Writable"
                }
              ]
            }
          ]
        },
        {
          "textRaw": "`stream.Writable.toWeb(streamWritable)`",
          "type": "method",
          "name": "toWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streamWritable` {stream.Writable}",
                  "name": "streamWritable",
                  "type": "stream.Writable"
                },
                {
                  "textRaw": "返回：{WritableStream}",
                  "name": "返回：",
                  "type": "WritableStream"
                }
              ]
            }
          ]
        },
        {
          "textRaw": "`stream.Duplex.from(src)`",
          "type": "method",
          "name": "from",
          "meta": {
            "added": [
              "v16.8.0"
            ],
            "changes": [
              {
                "version": [
                  "v19.5.0",
                  "v18.17.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/46190",
                "description": "The `src` argument can now be a `ReadableStream` or `WritableStream`."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`src` {Stream|Blob|ArrayBuffer|string|Iterable|AsyncIterable| AsyncGeneratorFunction|AsyncFunction|Promise|Object| ReadableStream|WritableStream}",
                  "name": "src",
                  "type": "Stream|Blob|ArrayBuffer|string|Iterable|AsyncIterable| AsyncGeneratorFunction|AsyncFunction|Promise|Object| ReadableStream|WritableStream"
                }
              ]
            }
          ],
          "desc": "<p>一个用于创建双工流的实用方法。</p>\n<ul>\n<li><code>Stream</code> 将可写流转换为可写 <code>Duplex</code>，将可读流转换为 <code>Duplex</code>。</li>\n<li><code>Blob</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>string</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>ArrayBuffer</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>AsyncIterable</code> 转换为可读 <code>Duplex</code>。不能产生 <code>null</code>。</li>\n<li><code>AsyncGeneratorFunction</code> 转换为可读/可写的转换 <code>Duplex</code>。必须将源 <code>AsyncIterable</code> 作为第一个参数。不能产生 <code>null</code>。</li>\n<li><code>AsyncFunction</code> 转换为可写 <code>Duplex</code>。必须返回 <code>null</code> 或 <code>undefined</code>。</li>\n<li><code>Object ({ writable, readable })</code> 将 <code>readable</code> 和 <code>writable</code> 转换为 <code>Stream</code>，然后将它们组合成 <code>Duplex</code>，其中 <code>Duplex</code> 将写入 <code>writable</code> 并从 <code>readable</code> 读取。</li>\n<li><code>Promise</code> 转换为可读 <code>Duplex</code>。值 <code>null</code> 被忽略。</li>\n<li><code>ReadableStream</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>WritableStream</code> 转换为可写 <code>Duplex</code>。</li>\n<li>返回：<a href=\"stream.html#class-streamduplex\" class=\"type\">&lt;stream.Duplex&gt;</a></li>\n</ul>\n<p>如果传递包含 promise 的 <code>Iterable</code> 对象作为参数，可能会导致未处理的拒绝。</p>\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\n\nDuplex.from([\n  new Promise((resolve) => setTimeout(resolve('1'), 1500)),\n  new Promise((_, reject) => setTimeout(reject(new Error('2')), 1000)), // 未处理的拒绝\n]);\n</code></pre>"
        },
        {
          "textRaw": "`stream.Duplex.fromWeb(pair[, options])`",
          "type": "method",
          "name": "fromWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`pair` {Object}",
                  "name": "pair",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`readable` {ReadableStream}",
                      "name": "readable",
                      "type": "ReadableStream"
                    },
                    {
                      "textRaw": "`writable` {WritableStream}",
                      "name": "writable",
                      "type": "WritableStream"
                    }
                  ]
                },
                {
                  "textRaw": "`options` {Object}",
                  "name": "options",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`allowHalfOpen` {boolean}",
                      "name": "allowHalfOpen",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`decodeStrings` {boolean}",
                      "name": "decodeStrings",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`encoding` {string}",
                      "name": "encoding",
                      "type": "string"
                    },
                    {
                      "textRaw": "`highWaterMark` {number}",
                      "name": "highWaterMark",
                      "type": "number"
                    },
                    {
                      "textRaw": "`objectMode` {boolean}",
                      "name": "objectMode",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`signal` {AbortSignal}",
                      "name": "signal",
                      "type": "AbortSignal"
                    }
                  ]
                },
                {
                  "textRaw": "返回：{stream.Duplex}",
                  "name": "返回：",
                  "type": "stream.Duplex"
                }
              ]
            }
          ],
          "desc": "<pre><code class=\"language-mjs\">import { Duplex } from 'node:stream';\nimport {\n  ReadableStream,\n  WritableStream,\n} from 'node:stream/web';\n\nconst readable = new ReadableStream({\n  start(controller) {\n    controller.enqueue('world');\n  },\n});\n\nconst writable = new WritableStream({\n  write(chunk) {\n    console.log('writable', chunk);\n  },\n});\n\nconst pair = {\n  readable,\n  writable,\n};\nconst duplex = Duplex.fromWeb(pair, { encoding: 'utf8', objectMode: true });\n\nduplex.write('hello');\n\nfor await (const chunk of duplex) {\n  console.log('readable', chunk);\n}\n</code></pre>\n<pre><code class=\"language-cjs\">const { Duplex } = require('node:stream');\nconst {\n  ReadableStream,\n  WritableStream,\n} = require('node:stream/web');\n\nconst readable = new ReadableStream({\n  start(controller) {\n    controller.enqueue('world');\n  },\n});\n\nconst writable = new WritableStream({\n  write(chunk) {\n    console.log('writable', chunk);\n  },\n});\n\nconst pair = {\n  readable,\n  writable,\n};\nconst duplex = Duplex.fromWeb(pair, { encoding: 'utf8', objectMode: true });\n\nduplex.write('hello');\nduplex.once('readable', () => console.log('readable', duplex.read()));\n</code></pre>"
        },
        {
          "textRaw": "`stream.Duplex.toWeb(streamDuplex)`",
          "type": "method",
          "name": "toWeb",
          "meta": {
            "added": [
              "v17.0.0"
            ],
            "changes": [
              {
                "version": "v24.0.0",
                "pr-url": "https://github.com/nodejs/node/pull/57513",
                "description": "Marking the API stable."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`streamDuplex` {stream.Duplex}",
                  "name": "streamDuplex",
                  "type": "stream.Duplex"
                },
                {
                  "textRaw": "返回：{Object}",
                  "name": "返回：",
                  "type": "Object",
                  "options": [
                    {
                      "textRaw": "`readable` {ReadableStream}",
                      "name": "readable",
                      "type": "ReadableStream"
                    },
                    {
                      "textRaw": "`writable` {WritableStream}",
                      "name": "writable",
                      "type": "WritableStream"
                    }
                  ]
                }
              ]
            }
          ],
          "desc": "<pre><code class=\"language-mjs\">import { Duplex } from 'node:stream';\n\nconst duplex = Duplex({\n  objectMode: true,\n  read() {\n    this.push('world');\n    this.push(null);\n  },\n  write(chunk, encoding, callback) {\n    console.log('writable', chunk);\n    callback();\n  },\n});\n\nconst { readable, writable } = Duplex.toWeb(duplex);\nwritable.getWriter().write('hello');\n\nconst { value } = await readable.getReader().read();\nconsole.log('readable', value);\n</code></pre>\n<pre><code class=\"language-cjs\">const { Duplex } = require('node:stream');\n\nconst duplex = Duplex({\n  objectMode: true,\n  read() {\n    this.push('world');\n    this.push(null);\n  },\n  write(chunk, encoding, callback) {\n    console.log('writable', chunk);\n    callback();\n  },\n});\n\nconst { readable, writable } = Duplex.toWeb(duplex);\nwritable.getWriter().write('hello');\n\nreadable.getReader().read().then((result) => {\n  console.log('readable', result.value);\n});\n</code></pre>"
        },
        {
          "textRaw": "`stream.addAbortSignal(signal, stream)`",
          "type": "method",
          "name": "addAbortSignal",
          "meta": {
            "added": [
              "v15.4.0"
            ],
            "changes": [
              {
                "version": [
                  "v19.7.0",
                  "v18.16.0"
                ],
                "pr-url": "https://github.com/nodejs/node/pull/46273",
                "description": "Added support for `ReadableStream` and `WritableStream`."
              }
            ]
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`signal` {AbortSignal} 表示可能取消的信号。",
                  "name": "signal",
                  "type": "AbortSignal",
                  "desc": "表示可能取消的信号。"
                },
                {
                  "textRaw": "`stream` {Stream|ReadableStream|WritableStream} 要附加信号的流。",
                  "name": "stream",
                  "type": "Stream|ReadableStream|WritableStream",
                  "desc": "要附加信号的流。"
                }
              ]
            }
          ],
          "desc": "<p>将 AbortSignal 附加到可读或可写流。这允许代码使用 <code>AbortController</code> 控制流销毁。</p>\n<p>在传递的 <code>AbortSignal</code> 对应的 <code>AbortController</code> 上调用 <code>abort</code> 的行为方式与在流上调用 <code>.destroy(new AbortError())</code> 相同，对于网络流，则与 <code>controller.error(new AbortError())</code> 相同。</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\n\nconst controller = new AbortController();\nconst read = addAbortSignal(\n  controller.signal,\n  fs.createReadStream(('object.json')),\n);\n// 稍后，中止操作，关闭流。\ncontroller.abort();\n</code></pre>\n<p>或者将 <code>AbortSignal</code> 与作为异步可迭代对象的可读流一起使用：</p>\n<pre><code class=\"language-js\">const controller = new AbortController();\nsetTimeout(() => controller.abort(), 10_000); // 设置超时。\nconst stream = addAbortSignal(\n  controller.signal,\n  fs.createReadStream(('object.json')),\n);\n(async () => {\n  try {\n    for await (const chunk of stream) {\n      await process(chunk);\n    }\n  } catch (e) {\n    if (e.name === 'AbortError') {\n      // 操作被取消。\n    } else {\n      throw e;\n    }\n  }\n})();\n</code></pre>\n<p>或者将 <code>AbortSignal</code> 与 ReadableStream 一起使用：</p>\n<pre><code class=\"language-js\">const controller = new AbortController();\nconst rs = new ReadableStream({\n  start(controller) {\n    controller.enqueue('hello');\n    controller.enqueue('world');\n    controller.close();\n  },\n});\n\naddAbortSignal(controller.signal, rs);\n\nfinished(rs, (err) => {\n  if (err) {\n    if (err.name === 'AbortError') {\n      // 操作被取消。\n    }\n  }\n});\n\nconst reader = rs.getReader();\n\nreader.read().then(({ value, done }) => {\n  console.log(value); // hello\n  console.log(done); // false\n  controller.abort();\n});\n</code></pre>"
        },
        {
          "textRaw": "`stream.getDefaultHighWaterMark(objectMode)`",
          "type": "method",
          "name": "getDefaultHighWaterMark",
          "meta": {
            "added": [
              "v19.9.0",
              "v18.17.0"
            ],
            "changes": []
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`objectMode` {boolean}",
                  "name": "objectMode",
                  "type": "boolean"
                },
                {
                  "textRaw": "返回：{integer}",
                  "name": "返回：",
                  "type": "integer"
                }
              ]
            }
          ],
          "desc": "<p>返回流使用的默认 highWaterMark。默认为 <code>65536</code> (64 KiB)，对于 <code>objectMode</code> 为 <code>16</code>。</p>"
        },
        {
          "textRaw": "`stream.setDefaultHighWaterMark(objectMode, value)`",
          "type": "method",
          "name": "setDefaultHighWaterMark",
          "meta": {
            "added": [
              "v19.9.0",
              "v18.17.0"
            ],
            "changes": []
          },
          "signatures": [
            {
              "params": [
                {
                  "textRaw": "`objectMode` {boolean}",
                  "name": "objectMode",
                  "type": "boolean"
                },
                {
                  "textRaw": "`value` {integer} highWaterMark 值。",
                  "name": "value",
                  "type": "integer",
                  "desc": "highWaterMark 值。"
                }
              ]
            }
          ],
          "desc": "<p>设置流使用的默认 highWaterMark。</p>"
        },
        {
          "textRaw": "`readable.read(0)`",
          "type": "method",
          "name": "read",
          "signatures": [
            {
              "params": []
            }
          ],
          "desc": "<p>在某些情况下，有必要触发底层可读流机制的刷新，而不实际消耗任何数据。在这种情况下，可以调用 <code>readable.read(0)</code>，它总是返回 <code>null</code>。</p>\n<p>如果内部读取缓冲区低于 <code>highWaterMark</code>，并且流当前没有在读取，那么调用 <code>stream.read(0)</code> 将触发低级的 <a href=\"#readable_readsize\"><code>stream._read()</code></a> 调用。</p>\n<p>虽然大多数应用程序几乎不需要这样做，但在 Node.js 内部有一些情况会这样做，特别是在 <code>Readable</code> 流类内部。</p>"
        },
        {
          "textRaw": "`readable.push('')`",
          "type": "method",
          "name": "push",
          "signatures": [
            {
              "params": []
            }
          ],
          "desc": "<p>不建议使用 <code>readable.push('')</code>。</p>\n<p>将零字节的 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#String_type\" class=\"type\">&lt;string&gt;</a>、<a href=\"buffer.html#class-buffer\" class=\"type\">&lt;Buffer&gt;</a>、<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypedArray\" class=\"type\">&lt;TypedArray&gt;</a> 或 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/DataView\" class=\"type\">&lt;DataView&gt;</a> 推送到不在对象模式下的流有一个有趣的副作用。因为它是调用 <a href=\"#readablepushchunk-encoding\"><code>readable.push()</code></a>，该调用将结束读取过程。但是，因为参数是空字符串，没有数据被添加到可读缓冲区，因此用户没有数据可消耗。</p>"
        }
      ],
      "miscs": [
        {
          "textRaw": "流消费者的 API",
          "name": "流消费者的 API",
          "type": "misc",
          "desc": "<p>几乎所有的 Node.js 应用程序，无论多么简单，都在某种程度上使用流。以下是在实现 HTTP 服务器的 Node.js 应用程序中使用流的示例：</p>\n<pre><code class=\"language-js\">const http = require('node:http');\n\nconst server = http.createServer((req, res) => {\n  // `req` 是一个 http.IncomingMessage，它是一个可读流。\n  // `res` 是一个 http.ServerResponse，它是一个可写流。\n\n  let body = '';\n  // 以 utf8 字符串形式获取数据。\n  // 如果未设置编码，将接收 Buffer 对象。\n  req.setEncoding('utf8');\n\n  // 一旦添加了监听器，可读流就会触发 'data' 事件。\n  req.on('data', (chunk) => {\n    body += chunk;\n  });\n\n  // 'end' 事件表示整个正文已被接收。\n  req.on('end', () => {\n    try {\n      const data = JSON.parse(body);\n      // 向用户写回一些有趣的内容：\n      res.write(typeof data);\n      res.end();\n    } catch (er) {\n      // 糟糕！坏的 json！\n      res.statusCode = 400;\n      return res.end(`error: ${er.message}`);\n    }\n  });\n});\n\nserver.listen(1337);\n\n// $ curl localhost:1337 -d \"{}\"\n// object\n// $ curl localhost:1337 -d \"\\\"foo\\\"\"\n// string\n// $ curl localhost:1337 -d \"not json\"\n// error: Unexpected token 'o', \"not json\" is not valid JSON\n</code></pre>\n<p><a href=\"#class-streamwritable\"><code>Writable</code></a> 流（例如示例中的 <code>res</code>）暴露了诸如 <code>write()</code> 和 <code>end()</code> 之类的方法，用于将数据写入流。</p>\n<p><a href=\"#class-streamreadable\"><code>Readable</code></a> 流使用 <a href=\"events.html#class-eventemitter\"><code>EventEmitter</code></a> API 在数据可供从流中读取时通知应用程序代码。可以通过多种方式从流中读取可用数据。</p>\n<p><a href=\"#class-streamwritable\"><code>Writable</code></a> 和 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流都以各种方式使用 <a href=\"events.html#class-eventemitter\"><code>EventEmitter</code></a> API 来传达流的当前状态。</p>\n<p><a href=\"#class-streamduplex\"><code>Duplex</code></a> 和 <a href=\"#class-streamtransform\"><code>Transform</code></a> 流都是 <a href=\"#class-streamwritable\"><code>Writable</code></a> 和 <a href=\"#class-streamreadable\"><code>Readable</code></a>。</p>\n<p>向流写入数据或从流消费数据的应用程序不需要直接实现流接口，通常也没有理由调用 <code>require('node:stream')</code>。</p>\n<p>希望实现新类型流的开发人员应参考 [流实现者的 API][] 部分。</p>",
          "miscs": [
            {
              "textRaw": "可写流",
              "name": "可写流",
              "desc": "<p>可写流是对数据被写入的 <em>目标</em> 的抽象。</p>\n<p><a href=\"#class-streamwritable\"><code>Writable</code></a> 流的示例包括：</p>\n<ul>\n<li>[HTTP 请求，在客户端][]</li>\n<li>[HTTP 响应，在服务器端][]</li>\n<li>[fs 写入流][]</li>\n<li><a href=\"zlib.html\">zlib 流</a></li>\n<li><a href=\"crypto.html\">crypto 流</a></li>\n<li>[TCP 套接字][]</li>\n<li>[子进程 stdin][]</li>\n<li><a href=\"process.html#processstdout\"><code>process.stdout</code></a>、<a href=\"process.html#processstderr\"><code>process.stderr</code></a></li>\n</ul>\n<p>其中一些示例实际上是实现了 <a href=\"#class-streamwritable\"><code>Writable</code></a> 接口的 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流。</p>\n<p>所有 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流都实现了 <code>stream.Writable</code> 类定义的接口。</p>\n<p>虽然 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流的特定实例可能以各种方式不同，但所有 <code>Writable</code> 流都遵循相同的基本使用模式，如下例所示：</p>\n<pre><code class=\"language-js\">const myStream = getWritableStreamSomehow();\nmyStream.write('some data');\nmyStream.write('some more data');\nmyStream.end('done writing data');\n</code></pre>",
              "classes": [
                {
                  "textRaw": "类：`stream.Writable`",
                  "name": "类：`stream.Writable`",
                  "meta": {
                    "added": [
                      "v0.9.4"
                    ],
                    "changes": []
                  },
                  "type": "class",
                  "modules": [
                    {
                      "textRaw": "事件：`'close'`",
                      "name": "事件：`'close'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/18438",
                            "description": "Add `emitClose` option to specify if `'close'` is emitted on destroy."
                          }
                        ]
                      },
                      "desc": "<p>当流及其任何底层资源（例如文件描述符）已关闭时，会触发 <code>'close'</code> 事件。该事件表明将不再发出更多事件，也不会发生进一步的计算。</p>\n<p>如果使用 <code>emitClose</code> 选项创建 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流，则它将始终发出 <code>'close'</code> 事件。</p>",
                      "type": "module",
                      "displayName": "事件：`'close'`"
                    },
                    {
                      "textRaw": "事件：`'drain'`",
                      "name": "事件：`'drain'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果调用 <a href=\"#writablewritechunk-encoding-callback\"><code>stream.write(chunk)</code></a> 返回 <code>false</code>，则当可以继续将数据写入流时，将发出 <code>'drain'</code> 事件。</p>\n<pre><code class=\"language-js\">// 将数据写入提供的可写流一百万次。\n// 注意背压。\nfunction writeOneMillionTimes(writer, data, encoding, callback) {\n  let i = 1000000;\n  write();\n  function write() {\n    let ok = true;\n    do {\n      i--;\n      if (i === 0) {\n        // 最后一次！\n        writer.write(data, encoding, callback);\n      } else {\n        // 查看是应该继续还是等待。\n        // 不要传递回调，因为我们还没有完成。\n        ok = writer.write(data, encoding);\n      }\n    } while (i > 0 &#x26;&#x26; ok);\n    if (i > 0) {\n      // 必须提前停止！\n      // 一旦它排空，再写一些。\n      writer.once('drain', write);\n    }\n  }\n}\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'drain'`"
                    },
                    {
                      "textRaw": "事件：`'error'`",
                      "name": "事件：`'error'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<ul>\n<li>类型：<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error\" class=\"type\">&lt;Error&gt;</a></li>\n</ul>\n<p>在写入或管道传输数据时发生错误，则会触发 <code>'error'</code> 事件。调用监听器回调时会传递一个 <code>Error</code> 参数。</p>\n<p>除非在创建流时将 <a href=\"#new-streamwritableoptions\"><code>autoDestroy</code></a> 选项设置为 <code>false</code>，否则在发出 <code>'error'</code> 事件时流将被关闭。</p>\n<p>在 <code>'error'</code> 之后，除了 <code>'close'</code> 之外，不应发出其他事件（包括 <code>'error'</code> 事件）。</p>",
                      "type": "module",
                      "displayName": "事件：`'error'`"
                    },
                    {
                      "textRaw": "事件：`'finish'`",
                      "name": "事件：`'finish'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在调用 <a href=\"#writableendchunk-encoding-callback\"><code>stream.end()</code></a> 方法后，并且所有数据都已刷新到底层系统时，会触发 <code>'finish'</code> 事件。</p>\n<pre><code class=\"language-js\">const writer = getWritableStreamSomehow();\nfor (let i = 0; i &#x3C; 100; i++) {\n  writer.write(`hello, #${i}!\\n`);\n}\nwriter.on('finish', () => {\n  console.log('All writes are now complete.');\n});\nwriter.end('This is the end\\n');\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'finish'`"
                    },
                    {
                      "textRaw": "事件：`'pipe'`",
                      "name": "事件：`'pipe'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<ul>\n<li><code>src</code> <a href=\"stream.html#class-streamreadable\" class=\"type\">&lt;stream.Readable&gt;</a> 正在管道传输到此可写流的源流</li>\n</ul>\n<p>当在可读流上调用 <a href=\"#readablepipedestination-options\"><code>stream.pipe()</code></a> 方法，将此可写流添加到其目标集时，会触发 <code>'pipe'</code> 事件。</p>\n<pre><code class=\"language-js\">const writer = getWritableStreamSomehow();\nconst reader = getReadableStreamSomehow();\nwriter.on('pipe', (src) => {\n  console.log('Something is piping into the writer.');\n  assert.equal(src, reader);\n});\nreader.pipe(writer);\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'pipe'`"
                    },
                    {
                      "textRaw": "事件：`'unpipe'`",
                      "name": "事件：`'unpipe'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<ul>\n<li><code>src</code> <a href=\"stream.html#class-streamreadable\" class=\"type\">&lt;stream.Readable&gt;</a> 取消管道传输此可写流的源流</li>\n</ul>\n<p>当在 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流上调用 <a href=\"#readableunpipedestination\"><code>stream.unpipe()</code></a> 方法，从此 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流的目标集中移除时，会触发 <code>'unpipe'</code> 事件。</p>\n<p>当 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流管道传输到此 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流时，如果此 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流发出错误，也会发出此事件。</p>\n<pre><code class=\"language-js\">const writer = getWritableStreamSomehow();\nconst reader = getReadableStreamSomehow();\nwriter.on('unpipe', (src) => {\n  console.log('Something has stopped piping into the writer.');\n  assert.equal(src, reader);\n});\nreader.pipe(writer);\nreader.unpipe(writer);\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'unpipe'`"
                    }
                  ],
                  "methods": [
                    {
                      "textRaw": "`writable.cork()`",
                      "type": "method",
                      "name": "cork",
                      "meta": {
                        "added": [
                          "v0.11.2"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": []
                        }
                      ],
                      "desc": "<p><code>writable.cork()</code> 方法强制所有写入的数据在内存中缓冲。当调用 <a href=\"#writableuncork\"><code>stream.uncork()</code></a> 或 <a href=\"#writableendchunk-encoding-callback\"><code>stream.end()</code></a> 方法时，缓冲的数据将被刷新。</p>\n<p><code>writable.cork()</code> 的主要目的是适应一种情况，即几个小数据块快速连续地写入流。<code>writable.cork()</code> 缓冲所有数据块，直到调用 <code>writable.uncork()</code>，这将把它们全部传递给 <code>writable._writev()</code>（如果存在）。这可以防止在等待第一个小数据块被处理时缓冲数据的行头阻塞情况。但是，在没有实现 <code>writable._writev()</code> 的情况下使用 <code>writable.cork()</code> 可能会对吞吐量产生不利影响。</p>\n<p>另请参阅：<a href=\"#writableuncork\"><code>writable.uncork()</code></a>、<a href=\"#writable_writevchunks-callback\"><code>writable._writev()</code></a>。</p>"
                    },
                    {
                      "textRaw": "`writable.destroy([error])`",
                      "type": "method",
                      "name": "destroy",
                      "meta": {
                        "added": [
                          "v8.0.0"
                        ],
                        "changes": [
                          {
                            "version": "v14.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/29197",
                            "description": "Work as a no-op on a stream that has already been destroyed."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`error` {Error} 可选，使用 `'error'` 事件发出错误。",
                              "name": "error",
                              "type": "Error",
                              "desc": "可选，使用 `'error'` 事件发出错误。"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>销毁流。可选择发出 <code>'error'</code> 事件，并发出 <code>'close'</code> 事件（除非 <code>emitClose</code> 设置为 <code>false</code>）。在此调用之后，可写流已结束，后续对 <code>write()</code> 或 <code>end()</code> 的调用将导致 <code>ERR_STREAM_DESTROYED</code> 错误。\n这是一种破坏性的、立即销毁流的方式。先前的 <code>write()</code> 调用可能尚未耗尽，并且可能触发 <code>ERR_STREAM_DESTROYED</code> 错误。如果数据应在关闭前刷新，请使用 <code>end()</code> 而不是 destroy，或者在销毁流之前等待 <code>'drain'</code> 事件。</p>\n<pre><code class=\"language-cjs\">const { Writable } = require('node:stream');\n\nconst myStream = new Writable();\n\nconst fooErr = new Error('foo error');\nmyStream.destroy(fooErr);\nmyStream.on('error', (fooErr) => console.error(fooErr.message)); // foo error\n</code></pre>\n<pre><code class=\"language-cjs\">const { Writable } = require('node:stream');\n\nconst myStream = new Writable();\n\nmyStream.destroy();\nmyStream.on('error', function wontHappen() {});\n</code></pre>\n<pre><code class=\"language-cjs\">const { Writable } = require('node:stream');\n\nconst myStream = new Writable();\nmyStream.destroy();\n\nmyStream.write('foo', (error) => console.error(error.code));\n// ERR_STREAM_DESTROYED\n</code></pre>\n<p>一旦调用 <code>destroy()</code>，任何进一步的调用都将是无操作的，并且除了来自 <code>_destroy()</code> 的错误之外，不会发出其他错误作为 <code>'error'</code>。</p>\n<p>实现者不应重写此方法，而应实现 <a href=\"#writable_destroyerr-callback\"><code>writable._destroy()</code></a>。</p>"
                    },
                    {
                      "textRaw": "`writable.end([chunk[, encoding]][, callback])`",
                      "type": "method",
                      "name": "end",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": [
                              "v22.0.0",
                              "v20.13.0"
                            ],
                            "pr-url": "https://github.com/nodejs/node/pull/51866",
                            "description": "The `chunk` argument can now be a `TypedArray` or `DataView` instance."
                          },
                          {
                            "version": "v15.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/34101",
                            "description": "The `callback` is invoked before 'finish' or on error."
                          },
                          {
                            "version": "v14.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/29747",
                            "description": "The `callback` is invoked if 'finish' or 'error' is emitted."
                          },
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/18780",
                            "description": "This method now returns a reference to `writable`."
                          },
                          {
                            "version": "v8.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/11608",
                            "description": "The `chunk` argument can now be a `Uint8Array` instance."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`chunk` {string|Buffer|TypedArray|DataView|any} 要写入的可选数据。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是除 `null` 之外的任何 JavaScript 值。",
                              "name": "chunk",
                              "type": "string|Buffer|TypedArray|DataView|any",
                              "desc": "要写入的可选数据。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是除 `null` 之外的任何 JavaScript 值。"
                            },
                            {
                              "textRaw": "`encoding` {string} 如果 `chunk` 是字符串，则为编码",
                              "name": "encoding",
                              "type": "string",
                              "desc": "如果 `chunk` 是字符串，则为编码"
                            },
                            {
                              "textRaw": "`callback` {Function} 流完成时的回调。",
                              "name": "callback",
                              "type": "Function",
                              "desc": "流完成时的回调。"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>调用 <code>writable.end()</code> 方法表示不再有数据写入 <a href=\"#class-streamwritable\"><code>Writable</code></a>。可选的 <code>chunk</code> 和 <code>encoding</code> 参数允许在关闭流之前立即再写入一个最终数据块。</p>\n<p>在调用 <a href=\"#writableendchunk-encoding-callback\"><code>stream.end()</code></a> 之后调用 <a href=\"#writablewritechunk-encoding-callback\"><code>stream.write()</code></a> 方法将引发错误。</p>\n<pre><code class=\"language-js\">// 写入 'hello, '，然后以 'world!' 结束。\nconst fs = require('node:fs');\nconst file = fs.createWriteStream('example.txt');\nfile.write('hello, ');\nfile.end('world!');\n// 现在不允许再写入更多数据！\n</code></pre>"
                    },
                    {
                      "textRaw": "`writable.setDefaultEncoding(encoding)`",
                      "type": "method",
                      "name": "setDefaultEncoding",
                      "meta": {
                        "added": [
                          "v0.11.15"
                        ],
                        "changes": [
                          {
                            "version": "v6.1.0",
                            "pr-url": "https://github.com/nodejs/node/pull/5040",
                            "description": "This method now returns a reference to `writable`."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`encoding` {string} 新的默认编码",
                              "name": "encoding",
                              "type": "string",
                              "desc": "新的默认编码"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>writable.setDefaultEncoding()</code> 方法设置 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流的默认 <code>encoding</code>。</p>"
                    },
                    {
                      "textRaw": "`writable.uncork()`",
                      "type": "method",
                      "name": "uncork",
                      "meta": {
                        "added": [
                          "v0.11.2"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": []
                        }
                      ],
                      "desc": "<p><code>writable.uncork()</code> 方法刷新自调用 <a href=\"#writablecork\"><code>stream.cork()</code></a> 以来所有缓冲的数据。</p>\n<p>当使用 <a href=\"#writablecork\"><code>writable.cork()</code></a> 和 <code>writable.uncork()</code> 来管理对流的写入缓冲时，使用 <code>process.nextTick()</code> 推迟调用 <code>writable.uncork()</code>。这样做允许对在给定 Node.js 事件循环阶段内发生的所有 <code>writable.write()</code> 调用进行批处理。</p>\n<pre><code class=\"language-js\">stream.cork();\nstream.write('some ');\nstream.write('data ');\nprocess.nextTick(() => stream.uncork());\n</code></pre>\n<p>如果在流上多次调用 <a href=\"#writablecork\"><code>writable.cork()</code></a> 方法，则必须调用相同次数的 <code>writable.uncork()</code> 调用来刷新缓冲的数据。</p>\n<pre><code class=\"language-js\">stream.cork();\nstream.write('some ');\nstream.cork();\nstream.write('data ');\nprocess.nextTick(() => {\n  stream.uncork();\n  // 数据直到第二次调用 uncork() 时才会被刷新。\n  stream.uncork();\n});\n</code></pre>\n<p>另请参阅：<a href=\"#writablecork\"><code>writable.cork()</code></a>。</p>"
                    },
                    {
                      "textRaw": "`writable[Symbol.asyncDispose]()`",
                      "type": "method",
                      "name": "[Symbol.asyncDispose]",
                      "meta": {
                        "added": [
                          "v22.4.0",
                          "v20.16.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.2.0",
                            "pr-url": "https://github.com/nodejs/node/pull/58467",
                            "description": "No longer experimental."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": []
                        }
                      ],
                      "desc": "<p>使用 <code>AbortError</code> 调用 <a href=\"#writabledestroyerror\"><code>writable.destroy()</code></a>，并返回一个在流完成时兑现的 promise。</p>"
                    },
                    {
                      "textRaw": "`writable.write(chunk[, encoding][, callback])`",
                      "type": "method",
                      "name": "write",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": [
                              "v22.0.0",
                              "v20.13.0"
                            ],
                            "pr-url": "https://github.com/nodejs/node/pull/51866",
                            "description": "The `chunk` argument can now be a `TypedArray` or `DataView` instance."
                          },
                          {
                            "version": "v8.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/11608",
                            "description": "The `chunk` argument can now be a `Uint8Array` instance."
                          },
                          {
                            "version": "v6.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/6170",
                            "description": "Passing `null` as the `chunk` parameter will always be considered invalid now, even in object mode."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`chunk` {string|Buffer|TypedArray|DataView|any} 要写入的可选数据。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是除 `null` 之外的任何 JavaScript 值。",
                              "name": "chunk",
                              "type": "string|Buffer|TypedArray|DataView|any",
                              "desc": "要写入的可选数据。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是除 `null` 之外的任何 JavaScript 值。"
                            },
                            {
                              "textRaw": "`encoding` {string|null} 如果 `chunk` 是字符串，则为编码。**默认值：** `'utf8'`",
                              "name": "encoding",
                              "type": "string|null",
                              "desc": "如果 `chunk` 是字符串，则为编码。**默认值：** `'utf8'`"
                            },
                            {
                              "textRaw": "`callback` {Function} 此数据块刷新时的回调。",
                              "name": "callback",
                              "type": "Function",
                              "desc": "此数据块刷新时的回调。"
                            },
                            {
                              "textRaw": "返回：{boolean} 如果流希望调用代码在继续写入更多数据之前等待发出 `'drain'` 事件，则为 `false`；否则为 `true`。",
                              "name": "返回：",
                              "type": "boolean",
                              "desc": "如果流希望调用代码在继续写入更多数据之前等待发出 `'drain'` 事件，则为 `false`；否则为 `true`。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>writable.write()</code> 方法将一些数据写入流，并在数据被完全处理后调用提供的 <code>callback</code>。如果发生错误，<code>callback</code> 将以错误作为其第一个参数被调用。<code>callback</code> 在 <code>'error'</code> 发出之前被异步调用。</p>\n<p>返回值是 <code>true</code> 如果在内部缓冲区小于创建流时配置的 <code>highWaterMark</code> 的情况下接受了 <code>chunk</code>。如果返回 <code>false</code>，则应停止进一步尝试将数据写入流，直到发出 <a href=\"#event-drain\"><code>'drain'</code></a> 事件。</p>\n<p>当流未排空时，对 <code>write()</code> 的调用将缓冲 <code>chunk</code>，并返回 false。一旦所有当前缓冲的数据块被排空（被操作系统接受交付），将发出 <code>'drain'</code> 事件。\n一旦 <code>write()</code> 返回 false，在发出 <code>'drain'</code> 事件之前不要写入更多数据块。虽然在未排空的流上调用 <code>write()</code> 是允许的，但 Node.js 将缓冲所有写入的数据块，直到达到最大内存使用量，此时它将无条件中止。\n即使在中止之前，高内存使用量也会导致垃圾收集器性能差和高 RSS（通常不会释放回系统，即使内存不再需要）。由于如果远程对等方不读取数据，TCP 套接字可能永远不会排空，写入未排空的套接字可能导致可远程利用的漏洞。</p>\n<p>在流未排空时写入数据对于 <a href=\"#class-streamtransform\"><code>Transform</code></a> 尤其有问题，因为 <code>Transform</code> 流默认是暂停的，直到它们被管道传输或添加了 <code>'data'</code> 或 <code>'readable'</code> 事件处理程序。</p>\n<p>如果要写入的数据可以按需生成或获取，建议将逻辑封装到 <a href=\"#class-streamreadable\"><code>Readable</code></a> 中并使用 <a href=\"#readablepipedestination-options\"><code>stream.pipe()</code></a>。但是，如果首选调用 <code>write()</code>，则可以使用 <a href=\"#event-drain\"><code>'drain'</code></a> 事件来尊重背压并避免内存问题：</p>\n<pre><code class=\"language-js\">function write(data, cb) {\n  if (!stream.write(data)) {\n    stream.once('drain', cb);\n  } else {\n    process.nextTick(cb);\n  }\n}\n\n// 在调用 cb 之前等待执行任何其他写入。\nwrite('hello', () => {\n  console.log('Write completed, do more writes now.');\n});\n</code></pre>\n<p>对象模式下的 <code>Writable</code> 流将始终忽略 <code>encoding</code> 参数。</p>"
                    }
                  ],
                  "properties": [
                    {
                      "textRaw": "`closed` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v18.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在发出 <code>'close'</code> 后为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`destroyed` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v8.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在调用 <a href=\"#writabledestroyerror\"><code>writable.destroy()</code></a> 后为 <code>true</code>。</p>\n<pre><code class=\"language-cjs\">const { Writable } = require('node:stream');\n\nconst myStream = new Writable();\n\nconsole.log(myStream.destroyed); // false\nmyStream.destroy();\nconsole.log(myStream.destroyed); // true\n</code></pre>"
                    },
                    {
                      "textRaw": "`writable` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v11.4.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果调用 <a href=\"#writablewritechunk-encoding-callback\"><code>writable.write()</code></a> 是安全的，则为 <code>true</code>，这意味着流尚未被销毁、出错或结束。</p>"
                    },
                    {
                      "textRaw": "`writableAborted` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v18.0.0",
                          "v16.17.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/57513",
                            "description": "Marking the API stable."
                          }
                        ]
                      },
                      "desc": "<p>返回流是否在发出 <code>'finish'</code> 之前被销毁或出错。</p>"
                    },
                    {
                      "textRaw": "`writableEnded` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.9.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在调用 <a href=\"#writableendchunk-encoding-callback\"><code>writable.end()</code></a> 后为 <code>true</code>。此属性不指示数据是否已刷新，为此请使用 <a href=\"#writablewritablefinished\"><code>writable.writableFinished</code></a>。</p>"
                    },
                    {
                      "textRaw": "`writableCorked` 类型：{integer}",
                      "type": "integer",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v13.2.0",
                          "v12.16.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>需要调用 <a href=\"#writableuncork\"><code>writable.uncork()</code></a> 的次数才能完全取消流的 cork。</p>"
                    },
                    {
                      "textRaw": "`errored` 类型：{Error}",
                      "type": "Error",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v18.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果流因错误而被销毁，则返回错误。</p>"
                    },
                    {
                      "textRaw": "`writableFinished` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.6.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在发出 <a href=\"#event-finish\"><code>'finish'</code></a> 事件之前立即设置为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`writableHighWaterMark` 类型：{number}",
                      "type": "number",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v9.3.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>返回创建此 <code>Writable</code> 时传递的 <code>highWaterMark</code> 值。</p>"
                    },
                    {
                      "textRaw": "`writableLength` 类型：{number}",
                      "type": "number",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v9.4.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>此属性包含队列中准备写入的字节（或对象）数。该值提供了有关 <code>highWaterMark</code> 状态的内省数据。</p>"
                    },
                    {
                      "textRaw": "`writableNeedDrain` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v15.2.0",
                          "v14.17.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果流的缓冲区已满且流将发出 <code>'drain'</code>，则为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`writableObjectMode` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.3.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>获取给定 <code>Writable</code> 流的 <code>objectMode</code> 属性。</p>"
                    }
                  ]
                }
              ],
              "type": "misc",
              "displayName": "可写流"
            },
            {
              "textRaw": "可读流",
              "name": "可读流",
              "desc": "<p>可读流是消费数据的 <em>源</em> 的抽象。</p>\n<p><code>Readable</code> 流的示例包括：</p>\n<ul>\n<li><a href=\"http.html#class-httpincomingmessage\">HTTP 响应，在客户端</a></li>\n<li><a href=\"http.html#class-httpincomingmessage\">HTTP 请求，在服务器端</a></li>\n<li>[fs 读取流][]</li>\n<li><a href=\"zlib.html\">zlib 流</a></li>\n<li><a href=\"crypto.html\">crypto 流</a></li>\n<li>[TCP 套接字][]</li>\n<li>[子进程 stdout 和 stderr][]</li>\n<li><a href=\"process.html#processstdin\"><code>process.stdin</code></a></li>\n</ul>\n<p>所有 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流都实现了 <code>stream.Readable</code> 类定义的接口。</p>",
              "modules": [
                {
                  "textRaw": "两种读取模式",
                  "name": "两种读取模式",
                  "desc": "<p><code>Readable</code> 流有效地以两种模式之一操作：流动模式和暂停模式。这些模式与 <a href=\"#object-mode\">对象模式</a> 是分开的。<a href=\"#class-streamreadable\"><code>Readable</code></a> 流可以处于对象模式或非对象模式，无论它处于流动模式还是暂停模式。</p>\n<ul>\n<li>\n<p>在流动模式下，数据从底层系统自动读取，并通过 <a href=\"events.html#class-eventemitter\"><code>EventEmitter</code></a> 接口使用事件尽快提供给应用程序。</p>\n</li>\n<li>\n<p>在暂停模式下，必须显式调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 方法从流中读取数据块。</p>\n</li>\n</ul>\n<p>所有 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流都以暂停模式开始，但可以通过以下方式之一切换到流动模式：</p>\n<ul>\n<li>添加 <a href=\"#event-data\"><code>'data'</code></a> 事件处理程序。</li>\n<li>调用 <a href=\"#readableresume\"><code>stream.resume()</code></a> 方法。</li>\n<li>调用 <a href=\"#readablepipedestination-options\"><code>stream.pipe()</code></a> 方法将数据发送到 <a href=\"#class-streamwritable\"><code>Writable</code></a>。</li>\n</ul>\n<p><code>Readable</code> 可以使用以下方式之一切换回暂停模式：</p>\n<ul>\n<li>如果没有管道目标，通过调用 <a href=\"#readablepause\"><code>stream.pause()</code></a> 方法。</li>\n<li>如果有管道目标，通过移除所有管道目标。可以通过调用 <a href=\"#readableunpipedestination\"><code>stream.unpipe()</code></a> 方法移除多个管道目标。</li>\n</ul>\n<p>要记住的重要概念是，在提供消费或忽略数据的机制之前，<code>Readable</code> 不会生成数据。如果消费机制被禁用或移除，<code>Readable</code> 将 <em>尝试</em> 停止生成数据。</p>\n<p>出于向后兼容性的原因，移除 <a href=\"#event-data\"><code>'data'</code></a> 事件处理程序不会自动暂停流。此外，如果有管道目标，调用 <a href=\"#readablepause\"><code>stream.pause()</code></a> 不能保证一旦这些目标排空并请求更多数据时流将 <em>保持</em> 暂停。</p>\n<p>如果 <a href=\"#class-streamreadable\"><code>Readable</code></a> 切换到流动模式并且没有可用的消费者来处理数据，则数据将丢失。例如，当调用 <code>readable.resume()</code> 方法而没有附加监听器到 <code>'data'</code> 事件时，或者当从流中移除 <code>'data'</code> 事件处理程序时，可能会发生这种情况。</p>\n<p>添加 <a href=\"#event-readable\"><code>'readable'</code></a> 事件处理程序会自动使流停止流动，并且必须通过 <a href=\"#readablereadsize\"><code>readable.read()</code></a> 消费数据。如果移除 <a href=\"#event-readable\"><code>'readable'</code></a> 事件处理程序，那么如果存在 <a href=\"#event-data\"><code>'data'</code></a> 事件处理程序，流将再次开始流动。</p>",
                  "type": "module",
                  "displayName": "两种读取模式"
                },
                {
                  "textRaw": "三种状态",
                  "name": "三种状态",
                  "desc": "<p><code>Readable</code> 流的 \"两种模式\" 操作是对 <code>Readable</code> 流实现内部发生的更复杂内部状态管理的简化抽象。</p>\n<p>具体来说，在任何给定时间点，每个 <code>Readable</code> 都处于以下三种可能状态之一：</p>\n<ul>\n<li><code>readable.readableFlowing === null</code></li>\n<li><code>readable.readableFlowing === false</code></li>\n<li><code>readable.readableFlowing === true</code></li>\n</ul>\n<p>当 <code>readable.readableFlowing</code> 为 <code>null</code> 时，没有提供消费流数据的机制。因此，流不会生成数据。在此状态下，附加 <code>'data'</code> 事件监听器、调用 <code>readable.pipe()</code> 方法或调用 <code>readable.resume()</code> 方法会将 <code>readable.readableFlowing</code> 切换到 <code>true</code>，导致 <code>Readable</code> 在数据生成时开始主动发出事件。</p>\n<p>调用 <code>readable.pause()</code>、<code>readable.unpipe()</code> 或接收背压将导致 <code>readable.readableFlowing</code> 设置为 <code>false</code>，暂时停止事件流动，但 <em>不</em> 停止数据生成。在此状态下，附加 <code>'data'</code> 事件监听器不会将 <code>readable.readableFlowing</code> 切换到 <code>true</code>。</p>\n<pre><code class=\"language-js\">const { PassThrough, Writable } = require('node:stream');\nconst pass = new PassThrough();\nconst writable = new Writable();\n\npass.pipe(writable);\npass.unpipe(writable);\n// readableFlowing 现在为 false。\n\npass.on('data', (chunk) => { console.log(chunk.toString()); });\n// readableFlowing 仍然为 false。\npass.write('ok');  // 不会发出 'data'。\npass.resume();     // 必须调用以使流发出 'data'。\n// readableFlowing 现在为 true。\n</code></pre>\n<p>当 <code>readable.readableFlowing</code> 为 <code>false</code> 时，数据可能在流的内部缓冲区中累积。</p>",
                  "type": "module",
                  "displayName": "三种状态"
                },
                {
                  "textRaw": "选择一种 API 风格",
                  "name": "选择一种_api_风格",
                  "desc": "<p><code>Readable</code> 流 API 在多个 Node.js 版本中演变，并提供了多种消费流数据的方法。通常，开发人员应选择 <em>一种</em> 消费数据的方法，并且 <em>绝不</em> 使用多种方法从单个流消费数据。具体来说，混合使用 <code>on('data')</code>、<code>on('readable')</code>、<code>pipe()</code> 或异步迭代器可能导致不直观的行为。</p>",
                  "type": "module",
                  "displayName": "选择一种 API 风格"
                }
              ],
              "classes": [
                {
                  "textRaw": "类：`stream.Readable`",
                  "name": "类：`stream.Readable`",
                  "meta": {
                    "added": [
                      "v0.9.4"
                    ],
                    "changes": []
                  },
                  "type": "class",
                  "modules": [
                    {
                      "textRaw": "事件：`'close'`",
                      "name": "事件：`'close'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/18438",
                            "description": "Add `emitClose` option to specify if `'close'` is emitted on destroy."
                          }
                        ]
                      },
                      "desc": "<p>当流及其任何底层资源（例如文件描述符）已关闭时，会触发 <code>'close'</code> 事件。该事件表明将不再发出更多事件，也不会发生进一步的计算。</p>\n<p>如果使用 <code>emitClose</code> 选项创建 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流，则它将始终发出 <code>'close'</code> 事件。</p>",
                      "type": "module",
                      "displayName": "事件：`'close'`"
                    },
                    {
                      "textRaw": "事件：`'data'`",
                      "name": "事件：`'data'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<ul>\n<li><code>chunk</code> <a href=\"buffer.html#class-buffer\" class=\"type\">&lt;Buffer&gt;</a> | <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#String_type\" class=\"type\">&lt;string&gt;</a> | <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#Data_types\" class=\"type\">&lt;any&gt;</a> 数据块。对于不在对象模式下操作的流，数据块将是字符串或 <code>Buffer</code>。对于对象模式下的流，数据块可以是除 <code>null</code> 之外的任何 JavaScript 值。</li>\n</ul>\n<p>每当流将数据块的所有权放弃给消费者时，就会触发 <code>'data'</code> 事件。这可能在通过调用 <code>readable.pipe()</code>、<code>readable.resume()</code> 或将监听器回调附加到 <code>'data'</code> 事件将流切换到流动模式时发生。每当调用 <code>readable.read()</code> 方法并且有数据块可供返回时，也会触发 <code>'data'</code> 事件。</p>\n<p>将 <code>'data'</code> 事件监听器附加到尚未显式暂停的流将使流切换到流动模式。数据将在可用时立即传递。</p>\n<p>如果已使用 <code>readable.setEncoding()</code> 方法为流指定了默认编码，则监听器回调将传递字符串形式的数据块；否则数据将作为 <code>Buffer</code> 传递。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\nreadable.on('data', (chunk) => {\n  console.log(`Received ${chunk.length} bytes of data.`);\n});\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'data'`"
                    },
                    {
                      "textRaw": "事件：`'end'`",
                      "name": "事件：`'end'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>当没有更多数据可供从流中消费时，会触发 <code>'end'</code> 事件。</p>\n<p>除非数据被完全消费，否则 <strong>不会发出</strong> <code>'end'</code> 事件。这可以通过将流切换到流动模式，或重复调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 直到所有数据被消费来完成。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\nreadable.on('data', (chunk) => {\n  console.log(`Received ${chunk.length} bytes of data.`);\n});\nreadable.on('end', () => {\n  console.log('There will be no more data.');\n});\n</code></pre>",
                      "type": "module",
                      "displayName": "事件：`'end'`"
                    },
                    {
                      "textRaw": "事件：`'error'`",
                      "name": "事件：`'error'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<ul>\n<li>类型：<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error\" class=\"type\">&lt;Error&gt;</a></li>\n</ul>\n<p><code>Readable</code> 实现可能在任何时候发出 <code>'error'</code> 事件。通常，这可能发生在底层流由于底层内部故障而无法生成数据时，或者当流实现尝试推送无效数据块时。</p>\n<p>监听器回调将传递一个 <code>Error</code> 对象。</p>",
                      "type": "module",
                      "displayName": "事件：`'error'`"
                    },
                    {
                      "textRaw": "事件：`'pause'`",
                      "name": "事件：`'pause'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>当调用 <a href=\"#readablepause\"><code>stream.pause()</code></a> 并且 <code>readableFlowing</code> 不为 <code>false</code> 时，会触发 <code>'pause'</code> 事件。</p>",
                      "type": "module",
                      "displayName": "事件：`'pause'`"
                    },
                    {
                      "textRaw": "事件：`'readable'`",
                      "name": "事件：`'readable'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/17979",
                            "description": "The `'readable'` is always emitted in the next tick after `.push()` is called."
                          },
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/18994",
                            "description": "Using `'readable'` requires calling `.read()`."
                          }
                        ]
                      },
                      "desc": "<p>当有数据可从流中读取，最多达到配置的高水位标记（<code>state.highWaterMark</code>）时，会触发 <code>'readable'</code> 事件。实际上，它表示流缓冲区中有新信息。如果此缓冲区中有可用数据，可以调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 来检索该数据。此外，当流结束时，也可能发出 <code>'readable'</code> 事件。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\nreadable.on('readable', function() {\n  // 现在有一些数据要读取。\n  let data;\n\n  while ((data = this.read()) !== null) {\n    console.log(data);\n  }\n});\n</code></pre>\n<p>如果流已结束，调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 将返回 <code>null</code> 并触发 <code>'end'</code> 事件。如果从未有任何数据可读，这也是正确的。例如，在以下示例中，<code>foo.txt</code> 是一个空文件：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst rr = fs.createReadStream('foo.txt');\nrr.on('readable', () => {\n  console.log(`readable: ${rr.read()}`);\n});\nrr.on('end', () => {\n  console.log('end');\n});\n</code></pre>\n<p>运行此脚本的输出是：</p>\n<pre><code class=\"language-console\">$ node test.js\nreadable: null\nend\n</code></pre>\n<p>在某些情况下，附加 <code>'readable'</code> 事件监听器会导致一定量的数据被读入内部缓冲区。</p>\n<p>通常，<code>readable.pipe()</code> 和 <code>'data'</code> 事件机制比 <code>'readable'</code> 事件更容易理解。然而，处理 <code>'readable'</code> 可能会带来更高的吞吐量。</p>\n<p>如果同时使用 <code>'readable'</code> 和 <a href=\"#event-data\"><code>'data'</code></a>，则 <code>'readable'</code> 在控制流方面具有优先权，即只有在调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 时才会发出 <code>'data'</code>。<code>readableFlowing</code> 属性将变为 <code>false</code>。\n如果在移除 <code>'readable'</code> 时有 <code>'data'</code> 监听器，则流将开始流动，即无需调用 <code>.resume()</code> 就会发出 <code>'data'</code> 事件。</p>",
                      "type": "module",
                      "displayName": "事件：`'readable'`"
                    },
                    {
                      "textRaw": "事件：`'resume'`",
                      "name": "事件：`'resume'`",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>当调用 <a href=\"#readableresume\"><code>stream.resume()</code></a> 并且 <code>readableFlowing</code> 不为 <code>true</code> 时，会触发 <code>'resume'</code> 事件。</p>",
                      "type": "module",
                      "displayName": "事件：`'resume'`"
                    }
                  ],
                  "methods": [
                    {
                      "textRaw": "`readable.destroy([error])`",
                      "type": "method",
                      "name": "destroy",
                      "meta": {
                        "added": [
                          "v8.0.0"
                        ],
                        "changes": [
                          {
                            "version": "v14.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/29197",
                            "description": "Work as a no-op on a stream that has already been destroyed."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`error` {Error} 将作为有效负载在 `'error'` 事件中传递的错误。",
                              "name": "error",
                              "type": "Error",
                              "desc": "将作为有效负载在 `'error'` 事件中传递的错误。"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>销毁流。可选择发出 <code>'error'</code> 事件，并发出 <code>'close'</code> 事件（除非 <code>emitClose</code> 设置为 <code>false</code>）。在此调用之后，可读流将释放任何内部资源，后续对 <code>push()</code> 的调用将被忽略。</p>\n<p>一旦调用 <code>destroy()</code>，任何进一步的调用都将是无操作的，并且除了来自 <code>_destroy()</code> 的错误之外，不会发出其他错误作为 <code>'error'</code>。</p>\n<p>实现者不应重写此方法，而应实现 <a href=\"#readable_destroyerr-callback\"><code>readable._destroy()</code></a>。</p>"
                    },
                    {
                      "textRaw": "`readable.isPaused()`",
                      "type": "method",
                      "name": "isPaused",
                      "meta": {
                        "added": [
                          "v0.11.14"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "返回：{boolean}",
                              "name": "返回：",
                              "type": "boolean"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.isPaused()</code> 方法返回 <code>Readable</code> 的当前操作状态。这主要由 <code>readable.pipe()</code> 方法背后的机制使用。在大多数典型情况下，没有理由直接使用此方法。</p>\n<pre><code class=\"language-js\">const readable = new stream.Readable();\n\nreadable.isPaused(); // === false\nreadable.pause();\nreadable.isPaused(); // === true\nreadable.resume();\nreadable.isPaused(); // === false\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.pause()`",
                      "type": "method",
                      "name": "pause",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.pause()</code> 方法将使处于流动模式的流停止发出 <a href=\"#event-data\"><code>'data'</code></a> 事件，从而切换出流动模式。任何可用的数据将保留在内部缓冲区中。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\nreadable.on('data', (chunk) => {\n  console.log(`Received ${chunk.length} bytes of data.`);\n  readable.pause();\n  console.log('There will be no additional data for 1 second.');\n  setTimeout(() => {\n    console.log('Now data will start flowing again.');\n    readable.resume();\n  }, 1000);\n});\n</code></pre>\n<p>如果有 <code>'readable'</code> 事件监听器，则 <code>readable.pause()</code> 方法无效。</p>"
                    },
                    {
                      "textRaw": "`readable.pipe(destination[, options])`",
                      "type": "method",
                      "name": "pipe",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`destination` {stream.Writable} 写入数据的目标。",
                              "name": "destination",
                              "type": "stream.Writable",
                              "desc": "写入数据的目标。"
                            },
                            {
                              "textRaw": "`options` {Object} 管道选项。",
                              "name": "options",
                              "type": "Object",
                              "desc": "管道选项。",
                              "options": [
                                {
                                  "textRaw": "`end` {boolean} 当读取器结束时结束写入器。**默认值：** `true`。",
                                  "name": "end",
                                  "type": "boolean",
                                  "desc": "当读取器结束时结束写入器。**默认值：** `true`。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{stream.Writable} _目标_，如果它是 [`Duplex`][] 或 [`Transform`][] 流，则允许链式管道。",
                              "name": "返回：",
                              "type": "stream.Writable",
                              "desc": "_目标_，如果它是 [`Duplex`][] 或 [`Transform`][] 流，则允许链式管道。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.pipe()</code> 方法将 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流附加到 <code>readable</code>，使其自动切换到流动模式并将其所有数据推送到附加的 <a href=\"#class-streamwritable\"><code>Writable</code></a>。数据流将自动管理，以便目标 <code>Writable</code> 流不会被更快的 <code>Readable</code> 流压垮。</p>\n<p>以下示例将 <code>readable</code> 中的所有数据管道传输到名为 <code>file.txt</code> 的文件：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst readable = getReadableStreamSomehow();\nconst writable = fs.createWriteStream('file.txt');\n// readable 中的所有数据都进入 'file.txt'。\nreadable.pipe(writable);\n</code></pre>\n<p>可以将多个 <code>Writable</code> 流附加到单个 <code>Readable</code> 流。</p>\n<p><code>readable.pipe()</code> 方法返回对 <em>目标</em> 流的引用，从而可以设置链式管道流：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst zlib = require('node:zlib');\nconst r = fs.createReadStream('file.txt');\nconst z = zlib.createGzip();\nconst w = fs.createWriteStream('file.txt.gz');\nr.pipe(z).pipe(w);\n</code></pre>\n<p>默认情况下，当源 <code>Readable</code> 流发出 <a href=\"#event-end\"><code>'end'</code></a> 时，会在目标 <code>Writable</code> 流上调用 <a href=\"#writableendchunk-encoding-callback\"><code>stream.end()</code></a>，因此目标不再可写。要禁用此默认行为，可以将 <code>end</code> 选项传递为 <code>false</code>，这将导致目标流保持打开状态：</p>\n<pre><code class=\"language-js\">reader.pipe(writer, { end: false });\nreader.on('end', () => {\n  writer.end('Goodbye\\n');\n});\n</code></pre>\n<p>一个重要注意事项是，如果 <code>Readable</code> 流在处理过程中发出错误，则 <code>Writable</code> 目标 <em>不会自动关闭</em>。如果发生错误，将需要 <em>手动</em> 关闭每个流以防止内存泄漏。</p>\n<p><a href=\"process.html#processstderr\"><code>process.stderr</code></a> 和 <a href=\"process.html#processstdout\"><code>process.stdout</code></a> <code>Writable</code> 流在 Node.js 进程退出之前永远不会关闭，无论指定的选项如何。</p>"
                    },
                    {
                      "textRaw": "`readable.read([size])`",
                      "type": "method",
                      "name": "read",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`size` {number} 指定要读取多少数据的可选参数。",
                              "name": "size",
                              "type": "number",
                              "desc": "指定要读取多少数据的可选参数。"
                            },
                            {
                              "textRaw": "返回：{string|Buffer|null|any}",
                              "name": "返回：",
                              "type": "string|Buffer|null|any"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.read()</code> 方法从内部缓冲区读取数据并返回。如果没有数据可读，则返回 <code>null</code>。默认情况下，数据作为 <code>Buffer</code> 对象返回，除非已使用 <code>readable.setEncoding()</code> 方法指定了编码，或者流处于对象模式。</p>\n<p>可选的 <code>size</code> 参数指定要读取的特定字节数。如果 <code>size</code> 字节不可读，则返回 <code>null</code>，<em>除非</em> 流已结束，在这种情况下将返回内部缓冲区中剩余的所有数据。</p>\n<p>如果未指定 <code>size</code> 参数，则返回内部缓冲区中包含的所有数据。</p>\n<p><code>size</code> 参数必须小于或等于 1 GiB。</p>\n<p><code>readable.read()</code> 方法只应在暂停模式下操作的 <code>Readable</code> 流上调用。在流动模式下，<code>readable.read()</code> 会自动调用，直到内部缓冲区完全排空。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\n\n// 'readable' 可能在数据缓冲时多次触发\nreadable.on('readable', () => {\n  let chunk;\n  console.log('Stream is readable (new data received in buffer)');\n  // 使用循环确保我们读取所有当前可用的数据\n  while (null !== (chunk = readable.read())) {\n    console.log(`Read ${chunk.length} bytes of data...`);\n  }\n});\n\n// 'end' 将在没有更多数据可用时触发一次\nreadable.on('end', () => {\n  console.log('Reached end of stream.');\n});\n</code></pre>\n<p>每次调用 <code>readable.read()</code> 返回一个数据块或 <code>null</code>，表示此时没有更多数据可读。这些数据块不会自动连接。因为单个 <code>read()</code> 调用不会返回所有数据，所以可能需要使用 while 循环来持续读取数据块，直到检索到所有数据。当读取大文件时，<code>.read()</code> 可能暂时返回 <code>null</code>，表示它已消耗所有缓冲内容，但可能还有更多数据尚未缓冲。在这种情况下，一旦缓冲区中有更多数据，就会发出新的 <code>'readable'</code> 事件，而 <code>'end'</code> 事件表示数据传输结束。</p>\n<p>因此，要从 <code>readable</code> 读取文件的全部内容，有必要跨多个 <code>'readable'</code> 事件收集数据块：</p>\n<pre><code class=\"language-js\">const chunks = [];\n\nreadable.on('readable', () => {\n  let chunk;\n  while (null !== (chunk = readable.read())) {\n    chunks.push(chunk);\n  }\n});\n\nreadable.on('end', () => {\n  const content = chunks.join('');\n});\n</code></pre>\n<p>对象模式下的 <code>Readable</code> 流将从调用 <a href=\"#readablereadsize\"><code>readable.read(size)</code></a> 返回单个项，无论 <code>size</code> 参数的值如何。</p>\n<p>如果 <code>readable.read()</code> 方法返回一个数据块，也会发出 <code>'data'</code> 事件。</p>\n<p>在发出 <a href=\"#event-end\"><code>'end'</code></a> 事件后调用 <a href=\"#readablereadsize\"><code>stream.read([size])</code></a> 将返回 <code>null</code>。不会引发运行时错误。</p>"
                    },
                    {
                      "textRaw": "`readable.resume()`",
                      "type": "method",
                      "name": "resume",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": [
                          {
                            "version": "v10.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/18994",
                            "description": "The `resume()` has no effect if there is a `'readable'` event listening."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.resume()</code> 方法使显式暂停的 <code>Readable</code> 流恢复发出 <a href=\"#event-data\"><code>'data'</code></a> 事件，将流切换到流动模式。</p>\n<p><code>readable.resume()</code> 方法可用于完全消费流中的数据而无需实际处理任何数据：</p>\n<pre><code class=\"language-js\">getReadableStreamSomehow()\n  .resume()\n  .on('end', () => {\n    console.log('Reached the end, but did not read anything.');\n  });\n</code></pre>\n<p>如果有 <code>'readable'</code> 事件监听器，则 <code>readable.resume()</code> 方法无效。</p>"
                    },
                    {
                      "textRaw": "`readable.setEncoding(encoding)`",
                      "type": "method",
                      "name": "setEncoding",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`encoding` {string} 要使用的编码。",
                              "name": "encoding",
                              "type": "string",
                              "desc": "要使用的编码。"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.setEncoding()</code> 方法设置从 <code>Readable</code> 流读取的数据的字符编码。</p>\n<p>默认情况下，不分配编码，流数据将作为 <code>Buffer</code> 对象返回。设置编码会导致流数据作为指定编码的字符串返回，而不是作为 <code>Buffer</code> 对象。例如，调用 <code>readable.setEncoding('utf8')</code> 将导致输出数据被解释为 UTF-8 数据，并作为字符串传递。调用 <code>readable.setEncoding('hex')</code> 将导致数据以十六进制字符串格式编码。</p>\n<p><code>Readable</code> 流将正确处理通过流传递的多字节字符，否则如果简单地从流中作为 <code>Buffer</code> 对象拉取，这些字符可能会被错误解码。</p>\n<pre><code class=\"language-js\">const readable = getReadableStreamSomehow();\nreadable.setEncoding('utf8');\nreadable.on('data', (chunk) => {\n  assert.equal(typeof chunk, 'string');\n  console.log('Got %d characters of string data:', chunk.length);\n});\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.unpipe([destination])`",
                      "type": "method",
                      "name": "unpipe",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`destination` {stream.Writable} 可选的特定要取消管道传输的流。",
                              "name": "destination",
                              "type": "stream.Writable",
                              "desc": "可选的特定要取消管道传输的流。"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p><code>readable.unpipe()</code> 方法分离先前使用 <a href=\"#readablepipedestination-options\"><code>stream.pipe()</code></a> 方法附加的 <code>Writable</code> 流。</p>\n<p>如果未指定 <code>destination</code>，则分离 <em>所有</em> 管道。</p>\n<p>如果指定了 <code>destination</code>，但未为其设置管道，则该方法不执行任何操作。</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst readable = getReadableStreamSomehow();\nconst writable = fs.createWriteStream('file.txt');\n// readable 中的所有数据都进入 'file.txt'，但仅在第一秒内。\nreadable.pipe(writable);\nsetTimeout(() => {\n  console.log('Stop writing to file.txt.');\n  readable.unpipe(writable);\n  console.log('Manually close the file stream.');\n  writable.end();\n}, 1000);\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.unshift(chunk[, encoding])`",
                      "type": "method",
                      "name": "unshift",
                      "meta": {
                        "added": [
                          "v0.9.11"
                        ],
                        "changes": [
                          {
                            "version": [
                              "v22.0.0",
                              "v20.13.0"
                            ],
                            "pr-url": "https://github.com/nodejs/node/pull/51866",
                            "description": "The `chunk` argument can now be a `TypedArray` or `DataView` instance."
                          },
                          {
                            "version": "v8.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/11608",
                            "description": "The `chunk` argument can now be a `Uint8Array` instance."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`chunk` {Buffer|TypedArray|DataView|string|null|any} 要推回读取队列的数据块。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray}、{DataView} 或 `null`。对于对象模式流，`chunk` 可以是任何 JavaScript 值。",
                              "name": "chunk",
                              "type": "Buffer|TypedArray|DataView|string|null|any",
                              "desc": "要推回读取队列的数据块。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray}、{DataView} 或 `null`。对于对象模式流，`chunk` 可以是任何 JavaScript 值。"
                            },
                            {
                              "textRaw": "`encoding` {string} 字符串块的编码。必须是有效的 `Buffer` 编码，例如 `'utf8'` 或 `'ascii'`。",
                              "name": "encoding",
                              "type": "string",
                              "desc": "字符串块的编码。必须是有效的 `Buffer` 编码，例如 `'utf8'` 或 `'ascii'`。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>将 <code>chunk</code> 作为 <code>null</code> 传递表示流结束（EOF），其行为与 <code>readable.push(null)</code> 相同，之后不能再写入数据。EOF 信号放在缓冲区的末尾，任何缓冲的数据仍将被刷新。</p>\n<p><code>readable.unshift()</code> 方法将一块数据推回内部缓冲区。这在某些情况下很有用，其中流被代码消费，这些代码需要 \"取消消费\" 从源中乐观拉出的某些数据量，以便数据可以传递给其他方。</p>\n<p>在发出 <a href=\"#event-end\"><code>'end'</code></a> 事件后调用 <code>stream.unshift(chunk)</code> 方法将引发运行时错误。</p>\n<p>使用 <code>stream.unshift()</code> 的开发人员通常应考虑切换到使用 <a href=\"#class-streamtransform\"><code>Transform</code></a> 流。有关更多信息，请参阅 [流实现者的 API][] 部分。</p>\n<pre><code class=\"language-js\">// 拉取由 \\n\\n 分隔的标头。\n// 如果获取太多，使用 unshift()。\n// 使用 (error, header, stream) 调用回调。\nconst { StringDecoder } = require('node:string_decoder');\nfunction parseHeader(stream, callback) {\n  stream.on('error', callback);\n  stream.on('readable', onReadable);\n  const decoder = new StringDecoder('utf8');\n  let header = '';\n  function onReadable() {\n    let chunk;\n    while (null !== (chunk = stream.read())) {\n      const str = decoder.write(chunk);\n      if (str.includes('\\n\\n')) {\n        // 找到标头边界。\n        const split = str.split(/\\n\\n/);\n        header += split.shift();\n        const remaining = split.join('\\n\\n');\n        const buf = Buffer.from(remaining, 'utf8');\n        stream.removeListener('error', callback);\n        // 在 unshift 之前移除 'readable' 监听器。\n        stream.removeListener('readable', onReadable);\n        if (buf.length)\n          stream.unshift(buf);\n        // 现在可以从流中读取消息正文。\n        callback(null, header, stream);\n        return;\n      }\n      // 仍在读取标头。\n      header += str;\n    }\n  }\n}\n</code></pre>\n<p>与 <a href=\"#readablepushchunk-encoding\"><code>stream.push(chunk)</code></a> 不同，<code>stream.unshift(chunk)</code> 不会通过重置流的内部读取状态来结束读取过程。如果在读取期间（即在自定义流上的 <a href=\"#readable_readsize\"><code>stream._read()</code></a> 实现内部）调用 <code>readable.unshift()</code>，这可能导致意外结果。在调用 <code>readable.unshift()</code> 后立即调用 <a href=\"#readablepushchunk-encoding\"><code>stream.push('')</code></a> 将适当地重置读取状态，但最好避免在执行读取过程中调用 <code>readable.unshift()</code>。</p>"
                    },
                    {
                      "textRaw": "`readable.wrap(stream)`",
                      "type": "method",
                      "name": "wrap",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`stream` {Stream} \"旧式\" 可读流",
                              "name": "stream",
                              "type": "Stream",
                              "desc": "\"旧式\" 可读流"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>在 Node.js 0.10 之前，流没有实现整个 <code>node:stream</code> 模块 API，就像当前定义的那样。（有关更多信息，请参阅 [兼容性][]。）</p>\n<p>当使用发出 <a href=\"#event-data\"><code>'data'</code></a> 事件并且具有仅建议性的 <a href=\"#readablepause\"><code>stream.pause()</code></a> 方法的旧 Node.js 库时，<code>readable.wrap()</code> 方法可用于创建 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流，该流使用旧流作为其数据源。</p>\n<p>很少需要使用 <code>readable.wrap()</code>，但该方法已提供以便与旧的 Node.js 应用程序和库进行交互。</p>\n<pre><code class=\"language-js\">const { OldReader } = require('./old-api-module.js');\nconst { Readable } = require('node:stream');\nconst oreader = new OldReader();\nconst myReader = new Readable().wrap(oreader);\n\nmyReader.on('readable', () => {\n  myReader.read(); // 等等。\n});\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable[Symbol.asyncIterator]()`",
                      "type": "method",
                      "name": "[Symbol.asyncIterator]",
                      "meta": {
                        "added": [
                          "v10.0.0"
                        ],
                        "changes": [
                          {
                            "version": "v11.14.0",
                            "pr-url": "https://github.com/nodejs/node/pull/26989",
                            "description": "Symbol.asyncIterator support is no longer experimental."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "返回：{AsyncIterator} 完全消费流。",
                              "name": "返回：",
                              "type": "AsyncIterator",
                              "desc": "完全消费流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<pre><code class=\"language-js\">const fs = require('node:fs');\n\nasync function print(readable) {\n  readable.setEncoding('utf8');\n  let data = '';\n  for await (const chunk of readable) {\n    data += chunk;\n  }\n  console.log(data);\n}\n\nprint(fs.createReadStream('file')).catch(console.error);\n</code></pre>\n<p>如果循环以 <code>break</code>、<code>return</code> 或 <code>throw</code> 终止，则流将被销毁。换句话说，迭代流将完全消费流。流将以等于 <code>highWaterMark</code> 选项的大小读取数据块。在上面的代码示例中，如果文件的数据少于 64 KiB，则数据将位于单个数据块中，因为没有向 <a href=\"fs.html#fscreatereadstreampath-options\"><code>fs.createReadStream()</code></a> 提供 <code>highWaterMark</code> 选项。</p>"
                    },
                    {
                      "textRaw": "`readable[Symbol.asyncDispose]()`",
                      "type": "method",
                      "name": "[Symbol.asyncDispose]",
                      "meta": {
                        "added": [
                          "v20.4.0",
                          "v18.18.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.2.0",
                            "pr-url": "https://github.com/nodejs/node/pull/58467",
                            "description": "No longer experimental."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": []
                        }
                      ],
                      "desc": "<p>使用 <code>AbortError</code> 调用 <a href=\"#readabledestroyerror\"><code>readable.destroy()</code></a>，并返回一个在流完成时兑现的 promise。</p>"
                    },
                    {
                      "textRaw": "`readable.compose(stream[, options])`",
                      "type": "method",
                      "name": "compose",
                      "meta": {
                        "added": [
                          "v19.1.0",
                          "v18.13.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/57513",
                            "description": "Marking the API stable."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`stream` {Stream|Iterable|AsyncIterable|Function}",
                              "name": "stream",
                              "type": "Stream|Iterable|AsyncIterable|Function"
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Duplex} 与流 `stream` 组合的流。",
                              "name": "返回：",
                              "type": "Duplex",
                              "desc": "与流 `stream` 组合的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\n\nasync function* splitToWords(source) {\n  for await (const chunk of source) {\n    const words = String(chunk).split(' ');\n\n    for (const word of words) {\n      yield word;\n    }\n  }\n}\n\nconst wordsStream = Readable.from(['this is', 'compose as operator']).compose(splitToWords);\nconst words = await wordsStream.toArray();\n\nconsole.log(words); // 打印 ['this', 'is', 'compose', 'as', 'operator']\n</code></pre>\n<p>有关更多信息，请参阅 <a href=\"#streamcomposestreams\"><code>stream.compose</code></a>。</p>"
                    },
                    {
                      "textRaw": "`readable.iterator([options])`",
                      "type": "method",
                      "name": "iterator",
                      "meta": {
                        "added": [
                          "v16.3.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/57513",
                            "description": "Marking the API stable."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`destroyOnReturn` {boolean} 当设置为 `false` 时，在异步迭代器上调用 `return`，或使用 `break`、`return` 或 `throw` 退出 `for await...of` 迭代将不会销毁流。**默认值：** `true`。",
                                  "name": "destroyOnReturn",
                                  "type": "boolean",
                                  "desc": "当设置为 `false` 时，在异步迭代器上调用 `return`，或使用 `break`、`return` 或 `throw` 退出 `for await...of` 迭代将不会销毁流。**默认值：** `true`。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{AsyncIterator} 用于消费流。",
                              "name": "返回：",
                              "type": "AsyncIterator",
                              "desc": "用于消费流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法创建的迭代器使用户可以选择取消销毁流，如果 <code>for await...of</code> 循环通过 <code>return</code>、<code>break</code> 或 <code>throw</code> 退出，或者如果迭代器应在迭代期间流发出错误时销毁流。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nasync function printIterator(readable) {\n  for await (const chunk of readable.iterator({ destroyOnReturn: false })) {\n    console.log(chunk); // 1\n    break;\n  }\n\n  console.log(readable.destroyed); // false\n\n  for await (const chunk of readable.iterator({ destroyOnReturn: false })) {\n    console.log(chunk); // 将打印 2 然后 3\n  }\n\n  console.log(readable.destroyed); // true，流被完全消费\n}\n\nasync function printSymbolAsyncIterator(readable) {\n  for await (const chunk of readable) {\n    console.log(chunk); // 1\n    break;\n  }\n\n  console.log(readable.destroyed); // true\n}\n\nasync function showBoth() {\n  await printIterator(Readable.from([1, 2, 3]));\n  await printSymbolAsyncIterator(Readable.from([1, 2, 3]));\n}\n\nshowBoth();\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.map(fn[, options])`",
                      "type": "method",
                      "name": "map",
                      "meta": {
                        "added": [
                          "v17.4.0",
                          "v16.14.0"
                        ],
                        "changes": [
                          {
                            "version": [
                              "v20.7.0",
                              "v18.19.0"
                            ],
                            "pr-url": "https://github.com/nodejs/node/pull/49249",
                            "description": "added `highWaterMark` in options."
                          }
                        ]
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于映射流中的每个数据块。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于映射流中的每个数据块。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`highWaterMark` {number} 在等待用户消费映射项时要缓冲的项数。**默认值：** `concurrency * 2 - 1`。",
                                  "name": "highWaterMark",
                                  "type": "number",
                                  "desc": "在等待用户消费映射项时要缓冲的项数。**默认值：** `concurrency * 2 - 1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Readable} 使用函数 `fn` 映射的流。",
                              "name": "返回：",
                              "type": "Readable",
                              "desc": "使用函数 `fn` 映射的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法允许映射流。<code>fn</code> 函数将为流中的每个数据块调用。如果 <code>fn</code> 函数返回一个 promise，该 promise 将在传递给结果流之前被 <code>await</code>。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { Resolver } from 'node:dns/promises';\n\n// 使用同步映射器。\nfor await (const chunk of Readable.from([1, 2, 3, 4]).map((x) => x * 2)) {\n  console.log(chunk); // 2, 4, 6, 8\n}\n// 使用异步映射器，最多同时进行 2 个查询。\nconst resolver = new Resolver();\nconst dnsResults = Readable.from([\n  'nodejs.org',\n  'openjsf.org',\n  'www.linuxfoundation.org',\n]).map((domain) => resolver.resolve4(domain), { concurrency: 2 });\nfor await (const result of dnsResults) {\n  console.log(result); // 记录 resolver.resolve4 的 DNS 结果。\n}\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.filter(fn[, options])`",
                      "type": "method",
                      "name": "filter",
                      "meta": {
                        "added": [
                          "v17.4.0",
                          "v16.14.0"
                        ],
                        "changes": [
                          {
                            "version": [
                              "v20.7.0",
                              "v18.19.0"
                            ],
                            "pr-url": "https://github.com/nodejs/node/pull/49249",
                            "description": "added `highWaterMark` in options."
                          }
                        ]
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于过滤流中的数据块。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于过滤流中的数据块。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`highWaterMark` {number} 在等待用户消费过滤项时要缓冲的项数。**默认值：** `concurrency * 2 - 1`。",
                                  "name": "highWaterMark",
                                  "type": "number",
                                  "desc": "在等待用户消费过滤项时要缓冲的项数。**默认值：** `concurrency * 2 - 1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Readable} 使用谓词 `fn` 过滤的流。",
                              "name": "返回：",
                              "type": "Readable",
                              "desc": "使用谓词 `fn` 过滤的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法允许过滤流。对于流中的每个数据块，将调用 <code>fn</code> 函数，如果它返回真值，则数据块将传递给结果流。如果 <code>fn</code> 函数返回一个 promise，该 promise 将被 <code>await</code>。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { Resolver } from 'node:dns/promises';\n\n// 使用同步谓词。\nfor await (const chunk of Readable.from([1, 2, 3, 4]).filter((x) => x > 2)) {\n  console.log(chunk); // 3, 4\n}\n// 使用异步谓词，最多同时进行 2 个查询。\nconst resolver = new Resolver();\nconst dnsResults = Readable.from([\n  'nodejs.org',\n  'openjsf.org',\n  'www.linuxfoundation.org',\n]).filter(async (domain) => {\n  const { address } = await resolver.resolve4(domain, { ttl: true });\n  return address.ttl > 60;\n}, { concurrency: 2 });\nfor await (const result of dnsResults) {\n  // 记录解析的 dns 记录存活时间超过 60 秒的域名。\n  console.log(result);\n}\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.forEach(fn[, options])`",
                      "type": "method",
                      "name": "forEach",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于对流的每个数据块调用。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于对流的每个数据块调用。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 当流完成时兑现的 promise。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "当流完成时兑现的 promise。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法允许迭代流。对于流中的每个数据块，将调用 <code>fn</code> 函数。如果 <code>fn</code> 函数返回一个 promise，该 promise 将被 <code>await</code>。</p>\n<p>此方法与 <code>for await...of</code> 循环的不同之处在于它可以并发处理数据块。此外，<code>forEach</code> 迭代只能通过传递 <code>signal</code> 选项并中止相关的 <code>AbortController</code> 来停止，而 <code>for await...of</code> 可以使用 <code>break</code> 或 <code>return</code> 停止。无论哪种情况，流都将被销毁。</p>\n<p>此方法与监听 <a href=\"#event-data\"><code>'data'</code></a> 事件的不同之处在于它使用底层机制中的 <a href=\"#class-streamreadable\"><code>readable</code></a> 事件，并且可以限制并发 <code>fn</code> 调用的数量。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { Resolver } from 'node:dns/promises';\n\n// 使用同步谓词。\nfor await (const chunk of Readable.from([1, 2, 3, 4]).filter((x) => x > 2)) {\n  console.log(chunk); // 3, 4\n}\n// 使用异步谓词，最多同时进行 2 个查询。\nconst resolver = new Resolver();\nconst dnsResults = Readable.from([\n  'nodejs.org',\n  'openjsf.org',\n  'www.linuxfoundation.org',\n]).map(async (domain) => {\n  const { address } = await resolver.resolve4(domain, { ttl: true });\n  return address;\n}, { concurrency: 2 });\nawait dnsResults.forEach((result) => {\n  // 记录结果，类似于 `for await (const result of dnsResults)`\n  console.log(result);\n});\nconsole.log('done'); // 流已完成\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.toArray([options])`",
                      "type": "method",
                      "name": "toArray",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许取消 toArray 操作。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许取消 toArray 操作。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 一个包含流内容的数组的 promise。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "一个包含流内容的数组的 promise。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法允许轻松获取流的内容。</p>\n<p>由于此方法将整个流读入内存，它抵消了流的好处。它旨在用于互操作性和便利性，而不是作为消费流的主要方式。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { Resolver } from 'node:dns/promises';\n\nawait Readable.from([1, 2, 3, 4]).toArray(); // [1, 2, 3, 4]\n\n// 使用 .map 并发进行 dns 查询，并使用 toArray 将结果收集到数组中\nconst dnsResults = await Readable.from([\n  'nodejs.org',\n  'openjsf.org',\n  'www.linuxfoundation.org',\n]).map(async (domain) => {\n  const { address } = await resolver.resolve4(domain, { ttl: true });\n  return address;\n}, { concurrency: 2 }).toArray();\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.some(fn[, options])`",
                      "type": "method",
                      "name": "some",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于对流的每个数据块调用。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于对流的每个数据块调用。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 一个 promise，如果 `fn` 对至少一个数据块返回了真值，则评估为 `true`。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "一个 promise，如果 `fn` 对至少一个数据块返回了真值，则评估为 `true`。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法类似于 <code>Array.prototype.some</code>，并在流中的每个数据块上调用 <code>fn</code>，直到等待的返回值为 <code>true</code>（或任何真值）。一旦在数据块上调用 <code>fn</code> 的等待返回值为真值，流将被销毁，并且 promise 以 <code>true</code> 兑现。如果没有任何 <code>fn</code> 调用在数据块上返回真值，则 promise 以 <code>false</code> 兑现。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { stat } from 'node:fs/promises';\n\n// 使用同步谓词。\nawait Readable.from([1, 2, 3, 4]).some((x) => x > 2); // true\nawait Readable.from([1, 2, 3, 4]).some((x) => x &#x3C; 0); // false\n\n// 使用异步谓词，最多同时进行 2 个文件检查。\nconst anyBigFile = await Readable.from([\n  'file1',\n  'file2',\n  'file3',\n]).some(async (fileName) => {\n  const stats = await stat(fileName);\n  return stats.size > 1024 * 1024;\n}, { concurrency: 2 });\nconsole.log(anyBigFile); // 如果列表中有任何文件大于 1MB，则为 `true`\nconsole.log('done'); // 流已完成\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.find(fn[, options])`",
                      "type": "method",
                      "name": "find",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.17.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于对流的每个数据块调用。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于对流的每个数据块调用。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 一个 promise，评估为 `fn` 评估为真值的第一个数据块，如果未找到元素，则为 `undefined`。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "一个 promise，评估为 `fn` 评估为真值的第一个数据块，如果未找到元素，则为 `undefined`。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法类似于 <code>Array.prototype.find</code>，并在流中的每个数据块上调用 <code>fn</code> 以查找具有 <code>fn</code> 真值的数据块。一旦调用 <code>fn</code> 的等待返回值为真值，流将被销毁，并且 promise 以 <code>fn</code> 返回真值的值兑现。如果所有 <code>fn</code> 调用在数据块上都返回假值，则 promise 以 <code>undefined</code> 兑现。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { stat } from 'node:fs/promises';\n\n// 使用同步谓词。\nawait Readable.from([1, 2, 3, 4]).find((x) => x > 2); // 3\nawait Readable.from([1, 2, 3, 4]).find((x) => x > 0); // 1\nawait Readable.from([1, 2, 3, 4]).find((x) => x > 10); // undefined\n\n// 使用异步谓词，最多同时进行 2 个文件检查。\nconst foundBigFile = await Readable.from([\n  'file1',\n  'file2',\n  'file3',\n]).find(async (fileName) => {\n  const stats = await stat(fileName);\n  return stats.size > 1024 * 1024;\n}, { concurrency: 2 });\nconsole.log(foundBigFile); // 大型文件的文件名，如果列表中有任何文件大于 1MB\nconsole.log('done'); // 流已完成\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.every(fn[, options])`",
                      "type": "method",
                      "name": "every",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个函数，用于对流的每个数据块调用。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个函数，用于对流的每个数据块调用。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 一个 promise，如果 `fn` 对所有数据块都返回了真值，则评估为 `true`。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "一个 promise，如果 `fn` 对所有数据块都返回了真值，则评估为 `true`。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法类似于 <code>Array.prototype.every</code>，并在流中的每个数据块上调用 <code>fn</code> 以检查所有等待的返回值是否都是 <code>fn</code> 的真值。一旦在数据块上调用 <code>fn</code> 的等待返回值为假值，流将被销毁，并且 promise 以 <code>false</code> 兑现。如果所有 <code>fn</code> 调用在数据块上都返回真值，则 promise 以 <code>true</code> 兑现。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { stat } from 'node:fs/promises';\n\n// 使用同步谓词。\nawait Readable.from([1, 2, 3, 4]).every((x) => x > 2); // false\nawait Readable.from([1, 2, 3, 4]).every((x) => x > 0); // true\n\n// 使用异步谓词，最多同时进行 2 个文件检查。\nconst allBigFiles = await Readable.from([\n  'file1',\n  'file2',\n  'file3',\n]).every(async (fileName) => {\n  const stats = await stat(fileName);\n  return stats.size > 1024 * 1024;\n}, { concurrency: 2 });\n// 如果列表中的所有文件都大于 1MiB，则为 `true`\nconsole.log(allBigFiles);\nconsole.log('done'); // 流已完成\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.flatMap(fn[, options])`",
                      "type": "method",
                      "name": "flatMap",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncGeneratorFunction|AsyncFunction} 一个函数，用于映射流中的每个数据块。",
                              "name": "fn",
                              "type": "Function|AsyncGeneratorFunction|AsyncFunction",
                              "desc": "一个函数，用于映射流中的每个数据块。",
                              "options": [
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`concurrency` {number} 一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。",
                                  "name": "concurrency",
                                  "type": "number",
                                  "desc": "一次在流上调用 `fn` 的最大并发调用数。**默认值：** `1`。"
                                },
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Readable} 使用函数 `fn` 平面映射的流。",
                              "name": "返回：",
                              "type": "Readable",
                              "desc": "使用函数 `fn` 平面映射的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法通过将给定的回调应用于流的每个数据块，然后展平结果来返回一个新流。</p>\n<p>可以从 <code>fn</code> 返回一个流或另一个可迭代或异步可迭代对象，结果流将被合并（展平）到返回的流中。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { createReadStream } from 'node:fs';\n\n// 使用同步映射器。\nfor await (const chunk of Readable.from([1, 2, 3, 4]).flatMap((x) => [x, x])) {\n  console.log(chunk); // 1, 1, 2, 2, 3, 3, 4, 4\n}\n// 使用异步映射器，合并 4 个文件的内容\nconst concatResult = Readable.from([\n  './1.mjs',\n  './2.mjs',\n  './3.mjs',\n  './4.mjs',\n]).flatMap((fileName) => createReadStream(fileName));\nfor await (const result of concatResult) {\n  // 这将包含所有 4 个文件的内容（所有数据块）\n  console.log(result);\n}\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.drop(limit[, options])`",
                      "type": "method",
                      "name": "drop",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`limit` {number} 要从可读流中丢弃的数据块数量。",
                              "name": "limit",
                              "type": "number",
                              "desc": "要从可读流中丢弃的数据块数量。"
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Readable} 丢弃了 `limit` 个数据块的流。",
                              "name": "返回：",
                              "type": "Readable",
                              "desc": "丢弃了 `limit` 个数据块的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法返回一个新流，其中前 <code>limit</code> 个数据块被丢弃。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\n\nawait Readable.from([1, 2, 3, 4]).drop(2).toArray(); // [3, 4]\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.take(limit[, options])`",
                      "type": "method",
                      "name": "take",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`limit` {number} 要从可读流中获取的数据块数量。",
                              "name": "limit",
                              "type": "number",
                              "desc": "要从可读流中获取的数据块数量。"
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Readable} 获取了 `limit` 个数据块的流。",
                              "name": "返回：",
                              "type": "Readable",
                              "desc": "获取了 `limit` 个数据块的流。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法返回一个新流，其中包含前 <code>limit</code> 个数据块。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\n\nawait Readable.from([1, 2, 3, 4]).take(2).toArray(); // [1, 2]\n</code></pre>"
                    },
                    {
                      "textRaw": "`readable.reduce(fn[, initial[, options]])`",
                      "type": "method",
                      "name": "reduce",
                      "meta": {
                        "added": [
                          "v17.5.0",
                          "v16.15.0"
                        ],
                        "changes": []
                      },
                      "stability": 1,
                      "stabilityText": "Experimental",
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`fn` {Function|AsyncFunction} 一个缩减器函数，用于调用流中的每个数据块。",
                              "name": "fn",
                              "type": "Function|AsyncFunction",
                              "desc": "一个缩减器函数，用于调用流中的每个数据块。",
                              "options": [
                                {
                                  "textRaw": "`previous` {any} 从上次调用 `fn` 获得的值，或者如果指定了 `initial` 值，则为 `initial`，否则为流的第一个数据块。",
                                  "name": "previous",
                                  "type": "any",
                                  "desc": "从上次调用 `fn` 获得的值，或者如果指定了 `initial` 值，则为 `initial`，否则为流的第一个数据块。"
                                },
                                {
                                  "textRaw": "`data` {any} 来自流的数据块。",
                                  "name": "data",
                                  "type": "any",
                                  "desc": "来自流的数据块。"
                                },
                                {
                                  "textRaw": "`options` {Object}",
                                  "name": "options",
                                  "type": "Object",
                                  "options": [
                                    {
                                      "textRaw": "`signal` {AbortSignal} 如果流被销毁则中止，允许提前中止 `fn` 调用。",
                                      "name": "signal",
                                      "type": "AbortSignal",
                                      "desc": "如果流被销毁则中止，允许提前中止 `fn` 调用。"
                                    }
                                  ]
                                }
                              ]
                            },
                            {
                              "textRaw": "`initial` {any} 用于缩减的初始值。",
                              "name": "initial",
                              "type": "any",
                              "desc": "用于缩减的初始值。"
                            },
                            {
                              "textRaw": "`options` {Object}",
                              "name": "options",
                              "type": "Object",
                              "options": [
                                {
                                  "textRaw": "`signal` {AbortSignal} 如果信号被中止，允许销毁流。",
                                  "name": "signal",
                                  "type": "AbortSignal",
                                  "desc": "如果信号被中止，允许销毁流。"
                                }
                              ]
                            },
                            {
                              "textRaw": "返回：{Promise} 一个 promise，用于缩减的最终值。",
                              "name": "返回：",
                              "type": "Promise",
                              "desc": "一个 promise，用于缩减的最终值。"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>此方法按顺序对流中的每个数据块调用 <code>fn</code>，将其传递给前一个元素计算的结果。它返回一个 promise，用于缩减的最终值。</p>\n<p>如果未提供 <code>initial</code> 值，则流的第一个数据块用作初始值。如果流为空，则 promise 将以 <code>TypeError</code> 拒绝，并带有 <code>ERR_INVALID_ARGS</code> 代码属性。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { readdir, stat } from 'node:fs/promises';\nimport { join } from 'node:path';\n\nconst directoryPath = './src';\nconst filesInDir = await readdir(directoryPath);\n\nconst folderSize = await Readable.from(filesInDir)\n  .reduce(async (totalSize, file) => {\n    const { size } = await stat(join(directoryPath, file));\n    return totalSize + size;\n  }, 0);\n\nconsole.log(folderSize);\n</code></pre>\n<p>缩减函数逐个元素地迭代流，这意味着没有 <code>concurrency</code> 参数或并行性。要并发执行 <code>reduce</code>，可以将异步函数提取到 <a href=\"#readablemapfn-options\"><code>readable.map</code></a> 方法中。</p>\n<pre><code class=\"language-mjs\">import { Readable } from 'node:stream';\nimport { readdir, stat } from 'node:fs/promises';\nimport { join } from 'node:path';\n\nconst directoryPath = './src';\nconst filesInDir = await readdir(directoryPath);\n\nconst folderSize = await Readable.from(filesInDir)\n  .map((file) => stat(join(directoryPath, file)), { concurrency: 2 })\n  .reduce((totalSize, { size }) => totalSize + size, 0);\n\nconsole.log(folderSize);\n</code></pre>"
                    }
                  ],
                  "properties": [
                    {
                      "textRaw": "`closed` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v18.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在发出 <code>'close'</code> 后为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`destroyed` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v8.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在调用 <a href=\"#readabledestroyerror\"><code>readable.destroy()</code></a> 后为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`readable` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v11.4.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果调用 <a href=\"#readablereadsize\"><code>readable.read()</code></a> 是安全的，则为 <code>true</code>，这意味着流尚未被销毁或发出 <code>'error'</code> 或 <code>'end'</code>。</p>"
                    },
                    {
                      "textRaw": "`readableAborted` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v16.8.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/57513",
                            "description": "Marking the API stable."
                          }
                        ]
                      },
                      "desc": "<p>返回流是否在发出 <code>'end'</code> 之前被销毁或出错。</p>"
                    },
                    {
                      "textRaw": "`readableDidRead` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v16.7.0",
                          "v14.18.0"
                        ],
                        "changes": [
                          {
                            "version": "v24.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/57513",
                            "description": "Marking the API stable."
                          }
                        ]
                      },
                      "desc": "<p>返回是否已发出 <code>'data'</code>。</p>"
                    },
                    {
                      "textRaw": "`readableEncoding` 类型：{null|string}",
                      "type": "null|string",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.7.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>获取给定 <code>Readable</code> 流的 <code>encoding</code> 属性。<code>encoding</code> 属性可以使用 <a href=\"#readablesetencodingencoding\"><code>readable.setEncoding()</code></a> 方法设置。</p>"
                    },
                    {
                      "textRaw": "`readableEnded` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.9.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>在发出 <a href=\"#event-end\"><code>'end'</code></a> 事件时变为 <code>true</code>。</p>"
                    },
                    {
                      "textRaw": "`errored` 类型：{Error}",
                      "type": "Error",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v18.0.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果流因错误而被销毁，则返回错误。</p>"
                    },
                    {
                      "textRaw": "`readableFlowing` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v9.4.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>此属性反映了 <code>Readable</code> 流的当前状态，如 [三种状态][] 部分所述。</p>"
                    },
                    {
                      "textRaw": "`readableHighWaterMark` 类型：{number}",
                      "type": "number",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v9.3.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>返回创建此 <code>Readable</code> 时传递的 <code>highWaterMark</code> 值。</p>"
                    },
                    {
                      "textRaw": "`readableLength` 类型：{number}",
                      "type": "number",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v9.4.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>此属性包含队列中准备读取的字节（或对象）数。该值提供了有关 <code>highWaterMark</code> 状态的内省数据。</p>"
                    },
                    {
                      "textRaw": "`readableObjectMode` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v12.3.0"
                        ],
                        "changes": []
                      },
                      "desc": "<p>获取给定 <code>Readable</code> 流的 <code>objectMode</code> 属性。</p>"
                    }
                  ]
                }
              ],
              "type": "misc",
              "displayName": "可读流"
            },
            {
              "textRaw": "双工和转换流",
              "name": "双工和转换流",
              "classes": [
                {
                  "textRaw": "类：`stream.Duplex`",
                  "name": "类：`stream.Duplex`",
                  "meta": {
                    "added": [
                      "v0.9.4"
                    ],
                    "changes": [
                      {
                        "version": "v6.8.0",
                        "pr-url": "https://github.com/nodejs/node/pull/8834",
                        "description": "Instances of `Duplex` now return `true` when checking `instanceof stream.Writable`."
                      }
                    ]
                  },
                  "type": "class",
                  "desc": "<p>双工流是同时实现 <a href=\"#class-streamreadable\"><code>Readable</code></a> 和 <a href=\"#class-streamwritable\"><code>Writable</code></a> 接口的流。</p>\n<p><code>Duplex</code> 流的示例包括：</p>\n<ul>\n<li>[TCP 套接字][]</li>\n<li><a href=\"zlib.html\">zlib 流</a></li>\n<li><a href=\"crypto.html\">crypto 流</a></li>\n</ul>",
                  "properties": [
                    {
                      "textRaw": "`allowHalfOpen` 类型：{boolean}",
                      "type": "boolean",
                      "name": "类型：",
                      "meta": {
                        "added": [
                          "v0.9.4"
                        ],
                        "changes": []
                      },
                      "desc": "<p>如果为 <code>false</code>，则当可读端结束时，流将自动结束可写端。最初由 <code>allowHalfOpen</code> 构造函数选项设置，默认为 <code>true</code>。</p>\n<p>这可以手动更改以更改现有 <code>Duplex</code> 流实例的半开行为，但必须在发出 <code>'end'</code> 事件之前更改。</p>"
                    }
                  ]
                },
                {
                  "textRaw": "类：`stream.Transform`",
                  "name": "类：`stream.Transform`",
                  "meta": {
                    "added": [
                      "v0.9.4"
                    ],
                    "changes": []
                  },
                  "type": "class",
                  "desc": "<p>转换流是 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流，其中输出以某种方式与输入相关。像所有 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流一样，<code>Transform</code> 流同时实现 <a href=\"#class-streamreadable\"><code>Readable</code></a> 和 <a href=\"#class-streamwritable\"><code>Writable</code></a> 接口。</p>\n<p><code>Transform</code> 流的示例包括：</p>\n<ul>\n<li><a href=\"zlib.html\">zlib 流</a></li>\n<li><a href=\"crypto.html\">crypto 流</a></li>\n</ul>",
                  "methods": [
                    {
                      "textRaw": "`transform.destroy([error])`",
                      "type": "method",
                      "name": "destroy",
                      "meta": {
                        "added": [
                          "v8.0.0"
                        ],
                        "changes": [
                          {
                            "version": "v14.0.0",
                            "pr-url": "https://github.com/nodejs/node/pull/29197",
                            "description": "Work as a no-op on a stream that has already been destroyed."
                          }
                        ]
                      },
                      "signatures": [
                        {
                          "params": [
                            {
                              "textRaw": "`error` {Error}",
                              "name": "error",
                              "type": "Error"
                            },
                            {
                              "textRaw": "返回：{this}",
                              "name": "返回：",
                              "type": "this"
                            }
                          ]
                        }
                      ],
                      "desc": "<p>销毁流，并可选择发出 <code>'error'</code> 事件。在此调用之后，转换流将释放任何内部资源。\n实现者不应重写此方法，而应实现 <a href=\"#readable_destroyerr-callback\"><code>readable._destroy()</code></a>。\n<code>Transform</code> 的 <code>_destroy()</code> 的默认实现也会发出 <code>'close'</code>，除非 <code>emitClose</code> 设置为 false。</p>\n<p>一旦调用 <code>destroy()</code>，任何进一步的调用都将是无操作的，并且除了来自 <code>_destroy()</code> 的错误之外，不会发出其他错误作为 <code>'error'</code>。</p>"
                    }
                  ]
                }
              ],
              "methods": [
                {
                  "textRaw": "`stream.duplexPair([options])`",
                  "type": "method",
                  "name": "duplexPair",
                  "meta": {
                    "added": [
                      "v22.6.0",
                      "v20.17.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`options` {Object} 传递给两个 [`Duplex`][] 构造函数的选项，用于设置缓冲等选项。",
                          "name": "options",
                          "type": "Object",
                          "desc": "传递给两个 [`Duplex`][] 构造函数的选项，用于设置缓冲等选项。"
                        },
                        {
                          "textRaw": "返回：{Array} 两个 [`Duplex`][] 实例的数组。",
                          "name": "返回：",
                          "type": "Array",
                          "desc": "两个 [`Duplex`][] 实例的数组。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>实用函数 <code>duplexPair</code> 返回一个包含两个项的数组，每个项都是连接到另一端的 <code>Duplex</code> 流：</p>\n<pre><code class=\"language-js\">const [ sideA, sideB ] = duplexPair();\n</code></pre>\n<p>写入一个流的任何内容在另一个流上都是可读的。它提供类似于网络连接的行为，其中客户端写入的数据可被服务器读取，反之亦然。</p>\n<p>双工流是对称的；可以使用其中一个或另一个，行为上没有差异。</p>"
                }
              ],
              "type": "misc",
              "displayName": "双工和转换流"
            }
          ],
          "methods": [
            {
              "textRaw": "`stream.finished(stream[, options], callback)`",
              "type": "method",
              "name": "finished",
              "meta": {
                "added": [
                  "v10.0.0"
                ],
                "changes": [
                  {
                    "version": "v19.5.0",
                    "pr-url": "https://github.com/nodejs/node/pull/46205",
                    "description": "Added support for `ReadableStream` and `WritableStream`."
                  },
                  {
                    "version": "v15.11.0",
                    "pr-url": "https://github.com/nodejs/node/pull/37354",
                    "description": "The `signal` option was added."
                  },
                  {
                    "version": "v14.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/32158",
                    "description": "The `finished(stream, cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
                  },
                  {
                    "version": "v14.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/31545",
                    "description": "Emitting `'close'` before `'end'` on a `Readable` stream will cause an `ERR_STREAM_PREMATURE_CLOSE` error."
                  },
                  {
                    "version": "v14.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/31509",
                    "description": "Callback will be invoked on streams which have already finished before the call to `finished(stream, cb)`."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {Stream|ReadableStream|WritableStream} 一个可读和/或可写的流/网络流。",
                      "name": "stream",
                      "type": "Stream|ReadableStream|WritableStream",
                      "desc": "一个可读和/或可写的流/网络流。"
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`error` {boolean} 如果设置为 `false`，则调用 `emit('error', err)` 不被视为已完成。**默认值：** `true`。",
                          "name": "error",
                          "type": "boolean",
                          "desc": "如果设置为 `false`，则调用 `emit('error', err)` 不被视为已完成。**默认值：** `true`。"
                        },
                        {
                          "textRaw": "`readable` {boolean} 当设置为 `false` 时，即使流可能仍然可读，也会在流结束时调用回调。**默认值：** `true`。",
                          "name": "readable",
                          "type": "boolean",
                          "desc": "当设置为 `false` 时，即使流可能仍然可读，也会在流结束时调用回调。**默认值：** `true`。"
                        },
                        {
                          "textRaw": "`writable` {boolean} 当设置为 `false` 时，即使流可能仍然可写，也会在流结束时调用回调。**默认值：** `true`。",
                          "name": "writable",
                          "type": "boolean",
                          "desc": "当设置为 `false` 时，即使流可能仍然可写，也会在流结束时调用回调。**默认值：** `true`。"
                        },
                        {
                          "textRaw": "`signal` {AbortSignal} 允许中止等待流完成。如果信号被中止，底层流将 _不会_ 被中止。回调将以 `AbortError` 调用。由此函数添加的所有已注册监听器也将被移除。",
                          "name": "signal",
                          "type": "AbortSignal",
                          "desc": "允许中止等待流完成。如果信号被中止，底层流将 _不会_ 被中止。回调将以 `AbortError` 调用。由此函数添加的所有已注册监听器也将被移除。"
                        }
                      ]
                    },
                    {
                      "textRaw": "`callback` {Function} 一个回调函数，接受一个可选的错误参数。",
                      "name": "callback",
                      "type": "Function",
                      "desc": "一个回调函数，接受一个可选的错误参数。"
                    },
                    {
                      "textRaw": "返回：{Function} 一个清理函数，移除所有已注册的监听器。",
                      "name": "返回：",
                      "type": "Function",
                      "desc": "一个清理函数，移除所有已注册的监听器。"
                    }
                  ]
                }
              ],
              "desc": "<p>一个函数，用于在流不再可读、可写或遇到错误或过早关闭事件时得到通知。</p>\n<pre><code class=\"language-js\">const { finished } = require('node:stream');\nconst fs = require('node:fs');\n\nconst rs = fs.createReadStream('archive.tar');\n\nfinished(rs, (err) => {\n  if (err) {\n    console.error('Stream failed.', err);\n  } else {\n    console.log('Stream is done reading.');\n  }\n});\n\nrs.resume(); // 排空流。\n</code></pre>\n<p>在错误处理场景中特别有用，其中流被过早销毁（如中止的 HTTP 请求），并且不会发出 <code>'end'</code> 或 <code>'finish'</code>。</p>\n<p><code>finished</code> API 提供了 <a href=\"#streamfinishedstream-options\">promise 版本</a>。</p>\n<p><code>stream.finished()</code> 在调用 <code>callback</code> 后留下悬空的事件监听器（特别是 <code>'error'</code>、<code>'end'</code>、<code>'finish'</code> 和 <code>'close'</code>）。这样做的原因是，以免（由于不正确的流实现导致的）意外的 <code>'error'</code> 事件引起意外的崩溃。\n如果不需要此行为，则需要在回调中调用返回的清理函数：</p>\n<pre><code class=\"language-js\">const cleanup = finished(rs, (err) => {\n  cleanup();\n  // ...\n});\n</code></pre>"
            },
            {
              "textRaw": "`stream.pipeline(source[, ...transforms], destination, callback)`",
              "type": "method",
              "name": "pipeline",
              "meta": {
                "added": [
                  "v10.0.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v19.7.0",
                      "v18.16.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46307",
                    "description": "Added support for webstreams."
                  },
                  {
                    "version": "v18.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/41678",
                    "description": "Passing an invalid callback to the `callback` argument now throws `ERR_INVALID_ARG_TYPE` instead of `ERR_INVALID_CALLBACK`."
                  },
                  {
                    "version": "v14.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/32158",
                    "description": "The `pipeline(..., cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
                  },
                  {
                    "version": "v13.10.0",
                    "pr-url": "https://github.com/nodejs/node/pull/31223",
                    "description": "Add support for async generators."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]}",
                      "name": "streams",
                      "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]"
                    },
                    {
                      "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function|ReadableStream}",
                      "name": "source",
                      "type": "Stream|Iterable|AsyncIterable|Function|ReadableStream",
                      "options": [
                        {
                          "textRaw": "返回：{Iterable|AsyncIterable}",
                          "name": "返回：",
                          "type": "Iterable|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`...transforms` {Stream|Function|TransformStream}",
                      "name": "...transforms",
                      "type": "Stream|Function|TransformStream",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{AsyncIterable}",
                          "name": "返回：",
                          "type": "AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`destination` {Stream|Function|WritableStream}",
                      "name": "destination",
                      "type": "Stream|Function|WritableStream",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{AsyncIterable|Promise}",
                          "name": "返回：",
                          "type": "AsyncIterable|Promise"
                        }
                      ]
                    },
                    {
                      "textRaw": "`callback` {Function} 当管道完全完成时调用。",
                      "name": "callback",
                      "type": "Function",
                      "desc": "当管道完全完成时调用。",
                      "options": [
                        {
                          "textRaw": "`err` {Error}",
                          "name": "err",
                          "type": "Error"
                        },
                        {
                          "textRaw": "`val` 由 `destination` 返回的 `Promise` 的解决值。",
                          "name": "val",
                          "desc": "由 `destination` 返回的 `Promise` 的解决值。"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{Stream}",
                      "name": "返回：",
                      "type": "Stream"
                    }
                  ]
                }
              ],
              "desc": "<p>一个模块方法，用于在流和生成器之间管道传输，转发错误并正确清理，并在管道完成时提供回调。</p>\n<pre><code class=\"language-js\">const { pipeline } = require('node:stream');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\n// 使用 pipeline API 轻松将一系列流管道传输在一起，并在管道完全完成时得到通知。\n\n// 一个高效地 gzip 可能巨大的 tar 文件的管道：\n\npipeline(\n  fs.createReadStream('archive.tar'),\n  zlib.createGzip(),\n  fs.createWriteStream('archive.tar.gz'),\n  (err) => {\n    if (err) {\n      console.error('Pipeline failed.', err);\n    } else {\n      console.log('Pipeline succeeded.');\n    }\n  },\n);\n</code></pre>\n<p><code>pipeline</code> API 提供了一个 <a href=\"#streampipelinesource-transforms-destination-options\">promise 版本</a>。</p>\n<p><code>stream.pipeline()</code> 将在所有流上调用 <code>stream.destroy(err)</code>，除了：</p>\n<ul>\n<li>已发出 <code>'end'</code> 或 <code>'close'</code> 的 <code>Readable</code> 流。</li>\n<li>已发出 <code>'finish'</code> 或 <code>'close'</code> 的 <code>Writable</code> 流。</li>\n</ul>\n<p><code>stream.pipeline()</code> 在调用 <code>callback</code> 后在流上留下悬空的事件监听器。在故障后重用流的情况下，这可能导致事件监听器泄漏和错误被吞掉。如果最后一个流是可读的，则将移除悬空的事件监听器，以便以后可以消费最后一个流。</p>\n<p><code>stream.pipeline()</code> 在引发错误时关闭所有流。\n将 <code>IncomingRequest</code> 与 <code>pipeline</code> 一起使用可能导致意外行为，因为它会在不发送预期响应的情况下销毁套接字。\n请参见下面的示例：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst http = require('node:http');\nconst { pipeline } = require('node:stream');\n\nconst server = http.createServer((req, res) => {\n  const fileStream = fs.createReadStream('./fileNotExist.txt');\n  pipeline(fileStream, res, (err) => {\n    if (err) {\n      console.log(err); // 没有这样的文件\n      // 一旦 `pipeline` 已经销毁了套接字，此消息将无法发送\n      return res.end('error!!!');\n    }\n  });\n});\n</code></pre>"
            },
            {
              "textRaw": "`stream.pipeline(streams, callback)`",
              "type": "method",
              "name": "pipeline",
              "meta": {
                "added": [
                  "v10.0.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v19.7.0",
                      "v18.16.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46307",
                    "description": "Added support for webstreams."
                  },
                  {
                    "version": "v18.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/41678",
                    "description": "Passing an invalid callback to the `callback` argument now throws `ERR_INVALID_ARG_TYPE` instead of `ERR_INVALID_CALLBACK`."
                  },
                  {
                    "version": "v14.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/32158",
                    "description": "The `pipeline(..., cb)` will wait for the `'close'` event before invoking the callback. The implementation tries to detect legacy streams and only apply this behavior to streams which are expected to emit `'close'`."
                  },
                  {
                    "version": "v13.10.0",
                    "pr-url": "https://github.com/nodejs/node/pull/31223",
                    "description": "Add support for async generators."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]}",
                      "name": "streams",
                      "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]"
                    },
                    {
                      "textRaw": "`source` {Stream|Iterable|AsyncIterable|Function|ReadableStream}",
                      "name": "source",
                      "type": "Stream|Iterable|AsyncIterable|Function|ReadableStream",
                      "options": [
                        {
                          "textRaw": "返回：{Iterable|AsyncIterable}",
                          "name": "返回：",
                          "type": "Iterable|AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`...transforms` {Stream|Function|TransformStream}",
                      "name": "...transforms",
                      "type": "Stream|Function|TransformStream",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{AsyncIterable}",
                          "name": "返回：",
                          "type": "AsyncIterable"
                        }
                      ]
                    },
                    {
                      "textRaw": "`destination` {Stream|Function|WritableStream}",
                      "name": "destination",
                      "type": "Stream|Function|WritableStream",
                      "options": [
                        {
                          "textRaw": "`source` {AsyncIterable}",
                          "name": "source",
                          "type": "AsyncIterable"
                        },
                        {
                          "textRaw": "返回：{AsyncIterable|Promise}",
                          "name": "返回：",
                          "type": "AsyncIterable|Promise"
                        }
                      ]
                    },
                    {
                      "textRaw": "`callback` {Function} 当管道完全完成时调用。",
                      "name": "callback",
                      "type": "Function",
                      "desc": "当管道完全完成时调用。",
                      "options": [
                        {
                          "textRaw": "`err` {Error}",
                          "name": "err",
                          "type": "Error"
                        },
                        {
                          "textRaw": "`val` 由 `destination` 返回的 `Promise` 的解决值。",
                          "name": "val",
                          "desc": "由 `destination` 返回的 `Promise` 的解决值。"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{Stream}",
                      "name": "返回：",
                      "type": "Stream"
                    }
                  ]
                }
              ],
              "desc": "<p>一个模块方法，用于在流和生成器之间管道传输，转发错误并正确清理，并在管道完成时提供回调。</p>\n<pre><code class=\"language-js\">const { pipeline } = require('node:stream');\nconst fs = require('node:fs');\nconst zlib = require('node:zlib');\n\n// 使用 pipeline API 轻松将一系列流管道传输在一起，并在管道完全完成时得到通知。\n\n// 一个高效地 gzip 可能巨大的 tar 文件的管道：\n\npipeline(\n  fs.createReadStream('archive.tar'),\n  zlib.createGzip(),\n  fs.createWriteStream('archive.tar.gz'),\n  (err) => {\n    if (err) {\n      console.error('Pipeline failed.', err);\n    } else {\n      console.log('Pipeline succeeded.');\n    }\n  },\n);\n</code></pre>\n<p><code>pipeline</code> API 提供了一个 <a href=\"#streampipelinesource-transforms-destination-options\">promise 版本</a>。</p>\n<p><code>stream.pipeline()</code> 将在所有流上调用 <code>stream.destroy(err)</code>，除了：</p>\n<ul>\n<li>已发出 <code>'end'</code> 或 <code>'close'</code> 的 <code>Readable</code> 流。</li>\n<li>已发出 <code>'finish'</code> 或 <code>'close'</code> 的 <code>Writable</code> 流。</li>\n</ul>\n<p><code>stream.pipeline()</code> 在调用 <code>callback</code> 后在流上留下悬空的事件监听器。在故障后重用流的情况下，这可能导致事件监听器泄漏和错误被吞掉。如果最后一个流是可读的，则将移除悬空的事件监听器，以便以后可以消费最后一个流。</p>\n<p><code>stream.pipeline()</code> 在引发错误时关闭所有流。\n将 <code>IncomingRequest</code> 与 <code>pipeline</code> 一起使用可能导致意外行为，因为它会在不发送预期响应的情况下销毁套接字。\n请参见下面的示例：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst http = require('node:http');\nconst { pipeline } = require('node:stream');\n\nconst server = http.createServer((req, res) => {\n  const fileStream = fs.createReadStream('./fileNotExist.txt');\n  pipeline(fileStream, res, (err) => {\n    if (err) {\n      console.log(err); // 没有这样的文件\n      // 一旦 `pipeline` 已经销毁了套接字，此消息将无法发送\n      return res.end('error!!!');\n    }\n  });\n});\n</code></pre>"
            },
            {
              "textRaw": "`stream.compose(...streams)`",
              "type": "method",
              "name": "compose",
              "meta": {
                "added": [
                  "v16.9.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v21.1.0",
                      "v20.10.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/50187",
                    "description": "Added support for stream class."
                  },
                  {
                    "version": [
                      "v19.8.0",
                      "v18.16.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46675",
                    "description": "Added support for webstreams."
                  }
                ]
              },
              "stability": 1,
              "stabilityText": "`stream.compose` 是实验性的。",
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streams` {Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]|Duplex\\[]|Function}",
                      "name": "streams",
                      "type": "Stream\\[]|Iterable\\[]|AsyncIterable\\[]|Function\\[]| ReadableStream\\[]|WritableStream\\[]|TransformStream\\[]|Duplex\\[]|Function"
                    },
                    {
                      "textRaw": "返回：{stream.Duplex}",
                      "name": "返回：",
                      "type": "stream.Duplex"
                    }
                  ]
                }
              ],
              "desc": "<p>将两个或多个流组合成一个 <code>Duplex</code> 流，该流写入第一个流并从最后一个流读取。每个提供的流都通过 <code>stream.pipeline</code> 管道传输到下一个流。如果任何流出错，则所有流都将被销毁，包括外部的 <code>Duplex</code> 流。</p>\n<p>因为 <code>stream.compose</code> 返回一个新的流，该流又可以（并且应该）被管道传输到其他流，它支持组合。相反，当将流传递给 <code>stream.pipeline</code> 时，通常第一个流是可读流，最后一个流是可写流，形成一个闭合电路。</p>\n<p>如果传递一个 <code>Function</code>，它必须是一个工厂方法，接受一个 <code>source</code> <code>Iterable</code>。</p>\n<pre><code class=\"language-mjs\">import { compose, Transform } from 'node:stream';\n\nconst removeSpaces = new Transform({\n  transform(chunk, encoding, callback) {\n    callback(null, String(chunk).replace(' ', ''));\n  },\n});\n\nasync function* toUpper(source) {\n  for await (const chunk of source) {\n    yield String(chunk).toUpperCase();\n  }\n}\n\nlet res = '';\nfor await (const buf of compose(removeSpaces, toUpper).end('hello world')) {\n  res += buf;\n}\n\nconsole.log(res); // 打印 'HELLOWORLD'\n</code></pre>\n<p><code>stream.compose</code> 可用于将异步可迭代对象、生成器和函数转换为流。</p>\n<ul>\n<li><code>AsyncIterable</code> 转换为可读的 <code>Duplex</code>。不能产生 <code>null</code>。</li>\n<li><code>AsyncGeneratorFunction</code> 转换为可读/可写的转换 <code>Duplex</code>。必须将源 <code>AsyncIterable</code> 作为第一个参数。不能产生 <code>null</code>。</li>\n<li><code>AsyncFunction</code> 转换为可写的 <code>Duplex</code>。必须返回 <code>null</code> 或 <code>undefined</code>。</li>\n</ul>\n<pre><code class=\"language-mjs\">import { compose } from 'node:stream';\nimport { finished } from 'node:stream/promises';\n\n// 将 AsyncIterable 转换为可读的 Duplex。\nconst s1 = compose(async function*() {\n  yield 'Hello';\n  yield 'World';\n}());\n\n// 将 AsyncGenerator 转换为转换 Duplex。\nconst s2 = compose(async function*(source) {\n  for await (const chunk of source) {\n    yield String(chunk).toUpperCase();\n  }\n});\n\nlet res = '';\n\n// 将 AsyncFunction 转换为可写的 Duplex。\nconst s3 = compose(async function(source) {\n  for await (const chunk of source) {\n    res += chunk;\n  }\n});\n\nawait finished(compose(s1, s2, s3));\n\nconsole.log(res); // 打印 'HELLOWORLD'\n</code></pre>\n<p>有关 <code>stream.compose</code> 作为操作符的信息，请参阅 <a href=\"#readablecomposestream-options\"><code>readable.compose(stream)</code></a>。</p>"
            },
            {
              "textRaw": "`stream.isErrored(stream)`",
              "type": "method",
              "name": "isErrored",
              "meta": {
                "added": [
                  "v17.3.0",
                  "v16.14.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v24.0.0",
                      "v22.17.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {Readable|Writable|Duplex|WritableStream|ReadableStream}",
                      "name": "stream",
                      "type": "Readable|Writable|Duplex|WritableStream|ReadableStream"
                    },
                    {
                      "textRaw": "返回：{boolean}",
                      "name": "返回：",
                      "type": "boolean"
                    }
                  ]
                }
              ],
              "desc": "<p>返回流是否遇到错误。</p>"
            },
            {
              "textRaw": "`stream.isReadable(stream)`",
              "type": "method",
              "name": "isReadable",
              "meta": {
                "added": [
                  "v17.4.0",
                  "v16.14.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v24.0.0",
                      "v22.17.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {Readable|Duplex|ReadableStream}",
                      "name": "stream",
                      "type": "Readable|Duplex|ReadableStream"
                    },
                    {
                      "textRaw": "返回：{boolean|null} - 仅当 `stream` 不是有效的 `Readable`、`Duplex` 或 `ReadableStream` 时返回 `null`。",
                      "name": "返回：",
                      "type": "boolean|null",
                      "desc": "仅当 `stream` 不是有效的 `Readable`、`Duplex` 或 `ReadableStream` 时返回 `null`。"
                    }
                  ]
                }
              ],
              "desc": "<p>返回流是否可读。</p>"
            },
            {
              "textRaw": "`stream.isWritable(stream)`",
              "type": "method",
              "name": "isWritable",
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {Writable|Duplex|WritableStream}",
                      "name": "stream",
                      "type": "Writable|Duplex|WritableStream"
                    },
                    {
                      "textRaw": "返回：{boolean|null} - 仅当 `stream` 不是有效的 `Writable`、`Duplex` 或 `WritableStream` 时返回 `null`。",
                      "name": "返回：",
                      "type": "boolean|null",
                      "desc": "仅当 `stream` 不是有效的 `Writable`、`Duplex` 或 `WritableStream` 时返回 `null`。"
                    }
                  ]
                }
              ],
              "desc": "<p>返回流是否可写。</p>"
            },
            {
              "textRaw": "`stream.Readable.from(iterable[, options])`",
              "type": "method",
              "name": "from",
              "meta": {
                "added": [
                  "v12.3.0",
                  "v10.17.0"
                ],
                "changes": []
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`iterable` {Iterable} 实现 `Symbol.asyncIterator` 或 `Symbol.iterator` 可迭代协议的对象。如果传递了空值，则发出 'error' 事件。",
                      "name": "iterable",
                      "type": "Iterable",
                      "desc": "实现 `Symbol.asyncIterator` 或 `Symbol.iterator` 可迭代协议的对象。如果传递了空值，则发出 'error' 事件。"
                    },
                    {
                      "textRaw": "`options` {Object} 提供给 `new stream.Readable([options])` 的选项。默认情况下，`Readable.from()` 会将 `options.objectMode` 设置为 `true`，除非通过将 `options.objectMode` 设置为 `false` 明确选择退出。",
                      "name": "options",
                      "type": "Object",
                      "desc": "提供给 `new stream.Readable([options])` 的选项。默认情况下，`Readable.from()` 会将 `options.objectMode` 设置为 `true`，除非通过将 `options.objectMode` 设置为 `false` 明确选择退出。"
                    },
                    {
                      "textRaw": "返回：{stream.Readable}",
                      "name": "返回：",
                      "type": "stream.Readable"
                    }
                  ]
                }
              ],
              "desc": "<p>一个实用方法，用于从迭代器创建可读流。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nasync function * generate() {\n  yield 'hello';\n  yield 'streams';\n}\n\nconst readable = Readable.from(generate());\n\nreadable.on('data', (chunk) => {\n  console.log(chunk);\n});\n</code></pre>\n<p>调用 <code>Readable.from(string)</code> 或 <code>Readable.from(buffer)</code> 不会迭代字符串或缓冲区以匹配其他流的语义，这是出于性能原因。</p>\n<p>如果传递包含 promise 的 <code>Iterable</code> 对象作为参数，可能会导致未处理的拒绝。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nReadable.from([\n  new Promise((resolve) => setTimeout(resolve('1'), 1500)),\n  new Promise((_, reject) => setTimeout(reject(new Error('2')), 1000)), // 未处理的拒绝\n]);\n</code></pre>"
            },
            {
              "textRaw": "`stream.Readable.fromWeb(readableStream[, options])`",
              "type": "method",
              "name": "fromWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`readableStream` {ReadableStream}",
                      "name": "readableStream",
                      "type": "ReadableStream"
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`encoding` {string}",
                          "name": "encoding",
                          "type": "string"
                        },
                        {
                          "textRaw": "`highWaterMark` {number}",
                          "name": "highWaterMark",
                          "type": "number"
                        },
                        {
                          "textRaw": "`objectMode` {boolean}",
                          "name": "objectMode",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`signal` {AbortSignal}",
                          "name": "signal",
                          "type": "AbortSignal"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{stream.Readable}",
                      "name": "返回：",
                      "type": "stream.Readable"
                    }
                  ]
                }
              ]
            },
            {
              "textRaw": "`stream.Readable.isDisturbed(stream)`",
              "type": "method",
              "name": "isDisturbed",
              "meta": {
                "added": [
                  "v16.8.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`stream` {stream.Readable|ReadableStream}",
                      "name": "stream",
                      "type": "stream.Readable|ReadableStream"
                    },
                    {
                      "textRaw": "返回：`boolean`",
                      "name": "返回：",
                      "desc": "boolean`"
                    }
                  ]
                }
              ],
              "desc": "<p>返回流是否已被读取或取消。</p>"
            },
            {
              "textRaw": "`stream.Readable.toWeb(streamReadable[, options])`",
              "type": "method",
              "name": "toWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  },
                  {
                    "version": [
                      "v18.7.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/43515",
                    "description": "include strategy options on Readable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streamReadable` {stream.Readable}",
                      "name": "streamReadable",
                      "type": "stream.Readable"
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`strategy` {Object}",
                          "name": "strategy",
                          "type": "Object",
                          "options": [
                            {
                              "textRaw": "`highWaterMark` {number} 在从给定的 `stream.Readable` 读取时应用背压之前，创建的 `ReadableStream` 的最大内部队列大小。如果未提供值，将从给定的 `stream.Readable` 中获取。",
                              "name": "highWaterMark",
                              "type": "number",
                              "desc": "在从给定的 `stream.Readable` 读取时应用背压之前，创建的 `ReadableStream` 的最大内部队列大小。如果未提供值，将从给定的 `stream.Readable` 中获取。"
                            },
                            {
                              "textRaw": "`size` {Function} 一个函数，用于计算给定数据块的大小。如果未提供值，所有数据块的大小将为 `1`。",
                              "name": "size",
                              "type": "Function",
                              "desc": "一个函数，用于计算给定数据块的大小。如果未提供值，所有数据块的大小将为 `1`。",
                              "options": [
                                {
                                  "textRaw": "`chunk` {any}",
                                  "name": "chunk",
                                  "type": "any"
                                },
                                {
                                  "textRaw": "返回：{number}",
                                  "name": "返回：",
                                  "type": "number"
                                }
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{ReadableStream}",
                      "name": "返回：",
                      "type": "ReadableStream"
                    }
                  ]
                }
              ]
            },
            {
              "textRaw": "`stream.Writable.fromWeb(writableStream[, options])`",
              "type": "method",
              "name": "fromWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`writableStream` {WritableStream}",
                      "name": "writableStream",
                      "type": "WritableStream"
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`decodeStrings` {boolean}",
                          "name": "decodeStrings",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`highWaterMark` {number}",
                          "name": "highWaterMark",
                          "type": "number"
                        },
                        {
                          "textRaw": "`objectMode` {boolean}",
                          "name": "objectMode",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`signal` {AbortSignal}",
                          "name": "signal",
                          "type": "AbortSignal"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{stream.Writable}",
                      "name": "返回：",
                      "type": "stream.Writable"
                    }
                  ]
                }
              ]
            },
            {
              "textRaw": "`stream.Writable.toWeb(streamWritable)`",
              "type": "method",
              "name": "toWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streamWritable` {stream.Writable}",
                      "name": "streamWritable",
                      "type": "stream.Writable"
                    },
                    {
                      "textRaw": "返回：{WritableStream}",
                      "name": "返回：",
                      "type": "WritableStream"
                    }
                  ]
                }
              ]
            },
            {
              "textRaw": "`stream.Duplex.from(src)`",
              "type": "method",
              "name": "from",
              "meta": {
                "added": [
                  "v16.8.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v19.5.0",
                      "v18.17.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46190",
                    "description": "The `src` argument can now be a `ReadableStream` or `WritableStream`."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`src` {Stream|Blob|ArrayBuffer|string|Iterable|AsyncIterable| AsyncGeneratorFunction|AsyncFunction|Promise|Object| ReadableStream|WritableStream}",
                      "name": "src",
                      "type": "Stream|Blob|ArrayBuffer|string|Iterable|AsyncIterable| AsyncGeneratorFunction|AsyncFunction|Promise|Object| ReadableStream|WritableStream"
                    }
                  ]
                }
              ],
              "desc": "<p>一个用于创建双工流的实用方法。</p>\n<ul>\n<li><code>Stream</code> 将可写流转换为可写 <code>Duplex</code>，将可读流转换为 <code>Duplex</code>。</li>\n<li><code>Blob</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>string</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>ArrayBuffer</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>AsyncIterable</code> 转换为可读 <code>Duplex</code>。不能产生 <code>null</code>。</li>\n<li><code>AsyncGeneratorFunction</code> 转换为可读/可写的转换 <code>Duplex</code>。必须将源 <code>AsyncIterable</code> 作为第一个参数。不能产生 <code>null</code>。</li>\n<li><code>AsyncFunction</code> 转换为可写 <code>Duplex</code>。必须返回 <code>null</code> 或 <code>undefined</code>。</li>\n<li><code>Object ({ writable, readable })</code> 将 <code>readable</code> 和 <code>writable</code> 转换为 <code>Stream</code>，然后将它们组合成 <code>Duplex</code>，其中 <code>Duplex</code> 将写入 <code>writable</code> 并从 <code>readable</code> 读取。</li>\n<li><code>Promise</code> 转换为可读 <code>Duplex</code>。值 <code>null</code> 被忽略。</li>\n<li><code>ReadableStream</code> 转换为可读 <code>Duplex</code>。</li>\n<li><code>WritableStream</code> 转换为可写 <code>Duplex</code>。</li>\n<li>返回：<a href=\"stream.html#class-streamduplex\" class=\"type\">&lt;stream.Duplex&gt;</a></li>\n</ul>\n<p>如果传递包含 promise 的 <code>Iterable</code> 对象作为参数，可能会导致未处理的拒绝。</p>\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\n\nDuplex.from([\n  new Promise((resolve) => setTimeout(resolve('1'), 1500)),\n  new Promise((_, reject) => setTimeout(reject(new Error('2')), 1000)), // 未处理的拒绝\n]);\n</code></pre>"
            },
            {
              "textRaw": "`stream.Duplex.fromWeb(pair[, options])`",
              "type": "method",
              "name": "fromWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`pair` {Object}",
                      "name": "pair",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`readable` {ReadableStream}",
                          "name": "readable",
                          "type": "ReadableStream"
                        },
                        {
                          "textRaw": "`writable` {WritableStream}",
                          "name": "writable",
                          "type": "WritableStream"
                        }
                      ]
                    },
                    {
                      "textRaw": "`options` {Object}",
                      "name": "options",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`allowHalfOpen` {boolean}",
                          "name": "allowHalfOpen",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`decodeStrings` {boolean}",
                          "name": "decodeStrings",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`encoding` {string}",
                          "name": "encoding",
                          "type": "string"
                        },
                        {
                          "textRaw": "`highWaterMark` {number}",
                          "name": "highWaterMark",
                          "type": "number"
                        },
                        {
                          "textRaw": "`objectMode` {boolean}",
                          "name": "objectMode",
                          "type": "boolean"
                        },
                        {
                          "textRaw": "`signal` {AbortSignal}",
                          "name": "signal",
                          "type": "AbortSignal"
                        }
                      ]
                    },
                    {
                      "textRaw": "返回：{stream.Duplex}",
                      "name": "返回：",
                      "type": "stream.Duplex"
                    }
                  ]
                }
              ],
              "desc": "<pre><code class=\"language-mjs\">import { Duplex } from 'node:stream';\nimport {\n  ReadableStream,\n  WritableStream,\n} from 'node:stream/web';\n\nconst readable = new ReadableStream({\n  start(controller) {\n    controller.enqueue('world');\n  },\n});\n\nconst writable = new WritableStream({\n  write(chunk) {\n    console.log('writable', chunk);\n  },\n});\n\nconst pair = {\n  readable,\n  writable,\n};\nconst duplex = Duplex.fromWeb(pair, { encoding: 'utf8', objectMode: true });\n\nduplex.write('hello');\n\nfor await (const chunk of duplex) {\n  console.log('readable', chunk);\n}\n</code></pre>\n<pre><code class=\"language-cjs\">const { Duplex } = require('node:stream');\nconst {\n  ReadableStream,\n  WritableStream,\n} = require('node:stream/web');\n\nconst readable = new ReadableStream({\n  start(controller) {\n    controller.enqueue('world');\n  },\n});\n\nconst writable = new WritableStream({\n  write(chunk) {\n    console.log('writable', chunk);\n  },\n});\n\nconst pair = {\n  readable,\n  writable,\n};\nconst duplex = Duplex.fromWeb(pair, { encoding: 'utf8', objectMode: true });\n\nduplex.write('hello');\nduplex.once('readable', () => console.log('readable', duplex.read()));\n</code></pre>"
            },
            {
              "textRaw": "`stream.Duplex.toWeb(streamDuplex)`",
              "type": "method",
              "name": "toWeb",
              "meta": {
                "added": [
                  "v17.0.0"
                ],
                "changes": [
                  {
                    "version": "v24.0.0",
                    "pr-url": "https://github.com/nodejs/node/pull/57513",
                    "description": "Marking the API stable."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`streamDuplex` {stream.Duplex}",
                      "name": "streamDuplex",
                      "type": "stream.Duplex"
                    },
                    {
                      "textRaw": "返回：{Object}",
                      "name": "返回：",
                      "type": "Object",
                      "options": [
                        {
                          "textRaw": "`readable` {ReadableStream}",
                          "name": "readable",
                          "type": "ReadableStream"
                        },
                        {
                          "textRaw": "`writable` {WritableStream}",
                          "name": "writable",
                          "type": "WritableStream"
                        }
                      ]
                    }
                  ]
                }
              ],
              "desc": "<pre><code class=\"language-mjs\">import { Duplex } from 'node:stream';\n\nconst duplex = Duplex({\n  objectMode: true,\n  read() {\n    this.push('world');\n    this.push(null);\n  },\n  write(chunk, encoding, callback) {\n    console.log('writable', chunk);\n    callback();\n  },\n});\n\nconst { readable, writable } = Duplex.toWeb(duplex);\nwritable.getWriter().write('hello');\n\nconst { value } = await readable.getReader().read();\nconsole.log('readable', value);\n</code></pre>\n<pre><code class=\"language-cjs\">const { Duplex } = require('node:stream');\n\nconst duplex = Duplex({\n  objectMode: true,\n  read() {\n    this.push('world');\n    this.push(null);\n  },\n  write(chunk, encoding, callback) {\n    console.log('writable', chunk);\n    callback();\n  },\n});\n\nconst { readable, writable } = Duplex.toWeb(duplex);\nwritable.getWriter().write('hello');\n\nreadable.getReader().read().then((result) => {\n  console.log('readable', result.value);\n});\n</code></pre>"
            },
            {
              "textRaw": "`stream.addAbortSignal(signal, stream)`",
              "type": "method",
              "name": "addAbortSignal",
              "meta": {
                "added": [
                  "v15.4.0"
                ],
                "changes": [
                  {
                    "version": [
                      "v19.7.0",
                      "v18.16.0"
                    ],
                    "pr-url": "https://github.com/nodejs/node/pull/46273",
                    "description": "Added support for `ReadableStream` and `WritableStream`."
                  }
                ]
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`signal` {AbortSignal} 表示可能取消的信号。",
                      "name": "signal",
                      "type": "AbortSignal",
                      "desc": "表示可能取消的信号。"
                    },
                    {
                      "textRaw": "`stream` {Stream|ReadableStream|WritableStream} 要附加信号的流。",
                      "name": "stream",
                      "type": "Stream|ReadableStream|WritableStream",
                      "desc": "要附加信号的流。"
                    }
                  ]
                }
              ],
              "desc": "<p>将 AbortSignal 附加到可读或可写流。这允许代码使用 <code>AbortController</code> 控制流销毁。</p>\n<p>在传递的 <code>AbortSignal</code> 对应的 <code>AbortController</code> 上调用 <code>abort</code> 的行为方式与在流上调用 <code>.destroy(new AbortError())</code> 相同，对于网络流，则与 <code>controller.error(new AbortError())</code> 相同。</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\n\nconst controller = new AbortController();\nconst read = addAbortSignal(\n  controller.signal,\n  fs.createReadStream(('object.json')),\n);\n// 稍后，中止操作，关闭流。\ncontroller.abort();\n</code></pre>\n<p>或者将 <code>AbortSignal</code> 与作为异步可迭代对象的可读流一起使用：</p>\n<pre><code class=\"language-js\">const controller = new AbortController();\nsetTimeout(() => controller.abort(), 10_000); // 设置超时。\nconst stream = addAbortSignal(\n  controller.signal,\n  fs.createReadStream(('object.json')),\n);\n(async () => {\n  try {\n    for await (const chunk of stream) {\n      await process(chunk);\n    }\n  } catch (e) {\n    if (e.name === 'AbortError') {\n      // 操作被取消。\n    } else {\n      throw e;\n    }\n  }\n})();\n</code></pre>\n<p>或者将 <code>AbortSignal</code> 与 ReadableStream 一起使用：</p>\n<pre><code class=\"language-js\">const controller = new AbortController();\nconst rs = new ReadableStream({\n  start(controller) {\n    controller.enqueue('hello');\n    controller.enqueue('world');\n    controller.close();\n  },\n});\n\naddAbortSignal(controller.signal, rs);\n\nfinished(rs, (err) => {\n  if (err) {\n    if (err.name === 'AbortError') {\n      // 操作被取消。\n    }\n  }\n});\n\nconst reader = rs.getReader();\n\nreader.read().then(({ value, done }) => {\n  console.log(value); // hello\n  console.log(done); // false\n  controller.abort();\n});\n</code></pre>"
            },
            {
              "textRaw": "`stream.getDefaultHighWaterMark(objectMode)`",
              "type": "method",
              "name": "getDefaultHighWaterMark",
              "meta": {
                "added": [
                  "v19.9.0",
                  "v18.17.0"
                ],
                "changes": []
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`objectMode` {boolean}",
                      "name": "objectMode",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "返回：{integer}",
                      "name": "返回：",
                      "type": "integer"
                    }
                  ]
                }
              ],
              "desc": "<p>返回流使用的默认 highWaterMark。默认为 <code>65536</code> (64 KiB)，对于 <code>objectMode</code> 为 <code>16</code>。</p>"
            },
            {
              "textRaw": "`stream.setDefaultHighWaterMark(objectMode, value)`",
              "type": "method",
              "name": "setDefaultHighWaterMark",
              "meta": {
                "added": [
                  "v19.9.0",
                  "v18.17.0"
                ],
                "changes": []
              },
              "signatures": [
                {
                  "params": [
                    {
                      "textRaw": "`objectMode` {boolean}",
                      "name": "objectMode",
                      "type": "boolean"
                    },
                    {
                      "textRaw": "`value` {integer} highWaterMark 值。",
                      "name": "value",
                      "type": "integer",
                      "desc": "highWaterMark 值。"
                    }
                  ]
                }
              ],
              "desc": "<p>设置流使用的默认 highWaterMark。</p>"
            }
          ]
        },
        {
          "textRaw": "流实现者的 API",
          "name": "流实现者的 API",
          "type": "misc",
          "desc": "<p><code>node:stream</code> 模块 API 的设计使得可以轻松使用 JavaScript 的原型继承模型来实现流。</p>\n<p>首先，流开发者会声明一个新的 JavaScript 类，该类扩展四个基本流类之一（<code>stream.Writable</code>、<code>stream.Readable</code>、<code>stream.Duplex</code> 或 <code>stream.Transform</code>），确保它们调用适当的父类构造函数：</p>\n<!-- eslint-disable no-useless-constructor -->\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nclass MyWritable extends Writable {\n  constructor({ highWaterMark, ...options }) {\n    super({ highWaterMark });\n    // ...\n  }\n}\n</code></pre>\n<p>扩展流时，请记住用户在转发这些选项到基构造函数之前可以并且应该提供哪些选项。例如，如果实现对 <code>autoDestroy</code> 和 <code>emitClose</code> 选项做出假设，则不允许用户覆盖这些选项。要明确转发哪些选项，而不是隐式转发所有选项。</p>\n<p>然后，新的流类必须实现一个或多个特定方法，具体取决于正在创建的流的类型，如下表所述：</p>\n<table>\n<thead>\n<tr>\n<th>用例</th>\n<th>类</th>\n<th>要实现的方法</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>仅读取</td>\n<td><a href=\"#class-streamreadable\"><code>Readable</code></a></td>\n<td><a href=\"#readable_readsize\"><code>_read()</code></a></td>\n</tr>\n<tr>\n<td>仅写入</td>\n<td><a href=\"#class-streamwritable\"><code>Writable</code></a></td>\n<td><a href=\"#writable_writechunk-encoding-callback\"><code>_write()</code></a>, <a href=\"#writable_writevchunks-callback\"><code>_writev()</code></a>, <a href=\"#writable_finalcallback\"><code>_final()</code></a></td>\n</tr>\n<tr>\n<td>读取和写入</td>\n<td><a href=\"#class-streamduplex\"><code>Duplex</code></a></td>\n<td><a href=\"#readable_readsize\"><code>_read()</code></a>, <a href=\"#writable_writechunk-encoding-callback\"><code>_write()</code></a>, <a href=\"#writable_writevchunks-callback\"><code>_writev()</code></a>, <a href=\"#writable_finalcallback\"><code>_final()</code></a></td>\n</tr>\n<tr>\n<td>操作写入的数据，然后读取结果</td>\n<td><a href=\"#class-streamtransform\"><code>Transform</code></a></td>\n<td><a href=\"#transform_transformchunk-encoding-callback\"><code>_transform()</code></a>, <a href=\"#transform_flushcallback\"><code>_flush()</code></a>, <a href=\"#writable_finalcallback\"><code>_final()</code></a></td>\n</tr>\n</tbody>\n</table>\n<p>流的实现代码 <em>绝不</em> 应调用供消费者使用的流的 \"公共\" 方法（如 [流消费者的 API][] 部分所述）。这样做可能导致消费流的应用程序代码产生不利的副作用。</p>\n<p>避免重写公共方法，如 <code>write()</code>、<code>end()</code>、<code>cork()</code>、<code>uncork()</code>、<code>read()</code> 和 <code>destroy()</code>，或通过 <code>.emit()</code> 发出内部事件，如 <code>'error'</code>、<code>'data'</code>、<code>'end'</code>、<code>'finish'</code> 和 <code>'close'</code>。这样做可能破坏当前和未来的流不变量，导致与其他流、流实用程序和用户期望的行为和/或兼容性问题。</p>",
          "miscs": [
            {
              "textRaw": "简化构造",
              "name": "简化构造",
              "meta": {
                "added": [
                  "v1.2.0"
                ],
                "changes": []
              },
              "desc": "<p>对于许多简单情况，可以在不依赖继承的情况下创建流。这可以通过直接创建 <code>stream.Writable</code>、<code>stream.Readable</code>、<code>stream.Duplex</code> 或 <code>stream.Transform</code> 的实例，并将适当的方法作为构造函数选项传递来实现。</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nconst myWritable = new Writable({\n  construct(callback) {\n    // 初始化状态并加载资源...\n  },\n  write(chunk, encoding, callback) {\n    // ...\n  },\n  destroy() {\n    // 释放资源...\n  },\n});\n</code></pre>",
              "type": "misc",
              "displayName": "简化构造"
            },
            {
              "textRaw": "实现可写流",
              "name": "实现可写流",
              "desc": "<p><code>stream.Writable</code> 类被扩展以实现 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流。</p>\n<p>自定义 <code>Writable</code> 流 <em>必须</em> 调用 <code>new stream.Writable([options])</code> 构造函数并实现 <code>writable._write()</code> 和/或 <code>writable._writev()</code> 方法。</p>",
              "ctors": [
                {
                  "textRaw": "`new stream.Writable([options])`",
                  "type": "ctor",
                  "name": "stream.Writable",
                  "meta": {
                    "changes": [
                      {
                        "version": "v22.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/52037",
                        "description": "bump default highWaterMark."
                      },
                      {
                        "version": "v15.5.0",
                        "pr-url": "https://github.com/nodejs/node/pull/36431",
                        "description": "support passing in an AbortSignal."
                      },
                      {
                        "version": "v14.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/30623",
                        "description": "Change `autoDestroy` option default to `true`."
                      },
                      {
                        "version": [
                          "v11.2.0",
                          "v10.16.0"
                        ],
                        "pr-url": "https://github.com/nodejs/node/pull/22795",
                        "description": "Add `autoDestroy` option to automatically `destroy()` the stream when it emits `'finish'` or errors."
                      },
                      {
                        "version": "v10.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/18438",
                        "description": "Add `emitClose` option to specify if `'close'` is emitted on destroy."
                      }
                    ]
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`options` {Object}",
                          "name": "options",
                          "type": "Object",
                          "options": [
                            {
                              "textRaw": "`highWaterMark` {number} [`stream.write()`][stream-write] 开始返回 `false` 时的缓冲区级别。**默认值：** `65536` (64 KiB)，对于 `objectMode` 流为 `16`。",
                              "name": "highWaterMark",
                              "type": "number",
                              "desc": "[`stream.write()`][stream-write] 开始返回 `false` 时的缓冲区级别。**默认值：** `65536` (64 KiB)，对于 `objectMode` 流为 `16`。"
                            },
                            {
                              "textRaw": "`decodeStrings` {boolean} 是否将传递给 [`stream.write()`][stream-write] 的 `string` 编码为 `Buffer`（使用 [`stream.write()`][stream-write] 调用中指定的编码），然后再将它们传递给 [`stream._write()`][stream-_write]。其他类型的数据不会转换（即 `Buffer` 不会解码为 `string`）。设置为 false 将阻止 `string` 被转换。**默认值：** `true`。",
                              "name": "decodeStrings",
                              "type": "boolean",
                              "desc": "是否将传递给 [`stream.write()`][stream-write] 的 `string` 编码为 `Buffer`（使用 [`stream.write()`][stream-write] 调用中指定的编码），然后再将它们传递给 [`stream._write()`][stream-_write]。其他类型的数据不会转换（即 `Buffer` 不会解码为 `string`）。设置为 false 将阻止 `string` 被转换。**默认值：** `true`。"
                            },
                            {
                              "textRaw": "`defaultEncoding` {string} 当未将编码指定为 [`stream.write()`][stream-write] 的参数时使用的默认编码。**默认值：** `'utf8'`。",
                              "name": "defaultEncoding",
                              "type": "string",
                              "desc": "当未将编码指定为 [`stream.write()`][stream-write] 的参数时使用的默认编码。**默认值：** `'utf8'`。"
                            },
                            {
                              "textRaw": "`objectMode` {boolean} [`stream.write(anyObj)`][stream-write] 是否是有效操作。当设置时，如果流实现支持，则可以写入除 string、{Buffer}、{TypedArray} 或 {DataView} 之外的 JavaScript 值。**默认值：** `false`。",
                              "name": "objectMode",
                              "type": "boolean",
                              "desc": "[`stream.write(anyObj)`][stream-write] 是否是有效操作。当设置时，如果流实现支持，则可以写入除 string、{Buffer}、{TypedArray} 或 {DataView} 之外的 JavaScript 值。**默认值：** `false`。"
                            },
                            {
                              "textRaw": "`emitClose` {boolean} 流在销毁后是否应发出 `'close'`。**默认值：** `true`。",
                              "name": "emitClose",
                              "type": "boolean",
                              "desc": "流在销毁后是否应发出 `'close'`。**默认值：** `true`。"
                            },
                            {
                              "textRaw": "`write` {Function} [`stream._write()`][stream-_write] 方法的实现。",
                              "name": "write",
                              "type": "Function",
                              "desc": "[`stream._write()`][stream-_write] 方法的实现。"
                            },
                            {
                              "textRaw": "`writev` {Function} [`stream._writev()`][stream-_writev] 方法的实现。",
                              "name": "writev",
                              "type": "Function",
                              "desc": "[`stream._writev()`][stream-_writev] 方法的实现。"
                            },
                            {
                              "textRaw": "`destroy` {Function} [`stream._destroy()`][writable-_destroy] 方法的实现。",
                              "name": "destroy",
                              "type": "Function",
                              "desc": "[`stream._destroy()`][writable-_destroy] 方法的实现。"
                            },
                            {
                              "textRaw": "`final` {Function} [`stream._final()`][stream-_final] 方法的实现。",
                              "name": "final",
                              "type": "Function",
                              "desc": "[`stream._final()`][stream-_final] 方法的实现。"
                            },
                            {
                              "textRaw": "`construct` {Function} [`stream._construct()`][writable-_construct] 方法的实现。",
                              "name": "construct",
                              "type": "Function",
                              "desc": "[`stream._construct()`][writable-_construct] 方法的实现。"
                            },
                            {
                              "textRaw": "`autoDestroy` {boolean} 此流是否应在结束后自动调用 `.destroy()`。**默认值：** `true`。",
                              "name": "autoDestroy",
                              "type": "boolean",
                              "desc": "此流是否应在结束后自动调用 `.destroy()`。**默认值：** `true`。"
                            },
                            {
                              "textRaw": "`signal` {AbortSignal} 表示可能取消的信号。",
                              "name": "signal",
                              "type": "AbortSignal",
                              "desc": "表示可能取消的信号。"
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "desc": "<!-- eslint-disable no-useless-constructor -->\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nclass MyWritable extends Writable {\n  constructor(options) {\n    // 调用 stream.Writable() 构造函数。\n    super(options);\n    // ...\n  }\n}\n</code></pre>\n<p>或者，在使用 ES6 之前风格的构造函数时：</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\nconst util = require('node:util');\n\nfunction MyWritable(options) {\n  if (!(this instanceof MyWritable))\n    return new MyWritable(options);\n  Writable.call(this, options);\n}\nutil.inherits(MyWritable, Writable);\n</code></pre>\n<p>或者，使用简化构造函数方法：</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nconst myWritable = new Writable({\n  write(chunk, encoding, callback) {\n    // ...\n  },\n  writev(chunks, callback) {\n    // ...\n  },\n});\n</code></pre>\n<p>在传递的 <code>AbortSignal</code> 对应的 <code>AbortController</code> 上调用 <code>abort</code> 的行为方式与在可写流上调用 <code>.destroy(new AbortError())</code> 相同。</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nconst controller = new AbortController();\nconst myWritable = new Writable({\n  write(chunk, encoding, callback) {\n    // ...\n  },\n  writev(chunks, callback) {\n    // ...\n  },\n  signal: controller.signal,\n});\n// 稍后，中止操作，关闭流。\ncontroller.abort();\n</code></pre>"
                }
              ],
              "methods": [
                {
                  "textRaw": "`writable._construct(callback)`",
                  "type": "method",
                  "name": "_construct",
                  "meta": {
                    "added": [
                      "v15.0.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`callback` {Function} 当流完成初始化时调用此函数（可选地带有错误参数）。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "当流完成初始化时调用此函数（可选地带有错误参数）。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p><code>_construct()</code> 方法不得直接调用。它可能由子类实现，如果是，则仅由内部 <code>Writable</code> 类方法调用。</p>\n<p>此可选函数将在流构造函数返回后的下一个时间点调用，延迟任何 <code>_write()</code>、<code>_final()</code> 和 <code>_destroy()</code> 调用，直到调用 <code>callback</code>。这对于在流可以使用之前初始化状态或异步初始化资源非常有用。</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\nconst fs = require('node:fs');\n\nclass WriteStream extends Writable {\n  constructor(filename) {\n    super();\n    this.filename = filename;\n    this.fd = null;\n  }\n  _construct(callback) {\n    fs.open(this.filename, 'w', (err, fd) => {\n      if (err) {\n        callback(err);\n      } else {\n        this.fd = fd;\n        callback();\n      }\n    });\n  }\n  _write(chunk, encoding, callback) {\n    fs.write(this.fd, chunk, callback);\n  }\n  _destroy(err, callback) {\n    if (this.fd) {\n      fs.close(this.fd, (er) => callback(er || err));\n    } else {\n      callback(err);\n    }\n  }\n}\n</code></pre>"
                },
                {
                  "textRaw": "`writable._write(chunk, encoding, callback)`",
                  "type": "method",
                  "name": "_write",
                  "meta": {
                    "changes": [
                      {
                        "version": "v12.11.0",
                        "pr-url": "https://github.com/nodejs/node/pull/29639",
                        "description": "_write() is optional when providing _writev()."
                      }
                    ]
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`chunk` {Buffer|string|any} 要写入的 `Buffer`，从传递给 [`stream.write()`][stream-write] 的 `string` 转换而来。如果流的 `decodeStrings` 选项为 `false` 或流处于对象模式，则块不会被转换，并且将是传递给 [`stream.write()`][stream-write] 的任何内容。",
                          "name": "chunk",
                          "type": "Buffer|string|any",
                          "desc": "要写入的 `Buffer`，从传递给 [`stream.write()`][stream-write] 的 `string` 转换而来。如果流的 `decodeStrings` 选项为 `false` 或流处于对象模式，则块不会被转换，并且将是传递给 [`stream.write()`][stream-write] 的任何内容。"
                        },
                        {
                          "textRaw": "`encoding` {string} 如果块是字符串，则 `encoding` 是该字符串的字符编码。如果块是 `Buffer`，或者流处于对象模式，则 `encoding` 可能被忽略。",
                          "name": "encoding",
                          "type": "string",
                          "desc": "如果块是字符串，则 `encoding` 是该字符串的字符编码。如果块是 `Buffer`，或者流处于对象模式，则 `encoding` 可能被忽略。"
                        },
                        {
                          "textRaw": "`callback` {Function} 当处理完提供的块时调用此函数（可选地带有错误参数）。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "当处理完提供的块时调用此函数（可选地带有错误参数）。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>所有 <code>Writable</code> 流实现必须提供 <a href=\"#writable_writechunk-encoding-callback\"><code>writable._write()</code></a> 和/或 <a href=\"#writable_writevchunks-callback\"><code>writable._writev()</code></a> 方法将数据发送到底层资源。</p>\n<p><a href=\"#class-streamtransform\"><code>Transform</code></a> 流提供了它们自己的 <a href=\"#writable_writechunk-encoding-callback\"><code>writable._write()</code></a> 实现。</p>\n<p>此函数不得由应用程序代码直接调用。它应由子类实现，并由内部 <code>Writable</code> 类方法调用。</p>\n<p><code>callback</code> 函数必须在 <code>writable._write()</code> 内部同步调用或异步调用（即不同的时间点）以发出写入成功完成或失败错误的信号。第一个传递给 <code>callback</code> 的参数必须是 <code>Error</code> 对象（如果调用失败）或 <code>null</code>（如果写入成功）。</p>\n<p>在调用 <code>writable._write()</code> 和调用 <code>callback</code> 之间发生的所有 <code>writable.write()</code> 调用将导致写入的数据被缓冲。当调用 <code>callback</code> 时，流可能会发出 <a href=\"#event-drain\"><code>'drain'</code></a> 事件。如果流实现能够同时处理多个数据块，则应实现 <code>writable._writev()</code> 方法。</p>\n<p>如果在构造函数选项中显式将 <code>decodeStrings</code> 属性设置为 <code>false</code>，则 <code>chunk</code> 将保持与传递给 <code>.write()</code> 相同的对象，并且可能是字符串而不是 <code>Buffer</code>。这是为了支持对某些字符串数据编码具有优化处理的实现。在这种情况下，<code>encoding</code> 参数将指示字符串的字符编码。否则，可以安全地忽略 <code>encoding</code> 参数。</p>\n<p><code>writable._write()</code> 方法带有下划线前缀，因为它是定义它的类的内部方法，不应由用户程序直接调用。</p>"
                },
                {
                  "textRaw": "`writable._writev(chunks, callback)`",
                  "type": "method",
                  "name": "_writev",
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`chunks` {Object\\[]} 要写入的数据。该值是一个 {Object} 数组，每个对象代表一个要写入的离散数据块。这些对象的属性是：",
                          "name": "chunks",
                          "type": "Object\\[]",
                          "desc": "要写入的数据。该值是一个 {Object} 数组，每个对象代表一个要写入的离散数据块。这些对象的属性是：",
                          "options": [
                            {
                              "textRaw": "`chunk` {Buffer|string} 包含要写入的数据的缓冲区实例或字符串。如果 `Writable` 是使用 `decodeStrings` 选项设置为 `false` 创建的，并且传递了字符串给 `write()`，则 `chunk` 将是字符串。",
                              "name": "chunk",
                              "type": "Buffer|string",
                              "desc": "包含要写入的数据的缓冲区实例或字符串。如果 `Writable` 是使用 `decodeStrings` 选项设置为 `false` 创建的，并且传递了字符串给 `write()`，则 `chunk` 将是字符串。"
                            },
                            {
                              "textRaw": "`encoding` {string} `chunk` 的字符编码。如果 `chunk` 是 `Buffer`，则 `encoding` 将为 `'buffer'`。",
                              "name": "encoding",
                              "type": "string",
                              "desc": "`chunk` 的字符编码。如果 `chunk` 是 `Buffer`，则 `encoding` 将为 `'buffer'`。"
                            }
                          ]
                        },
                        {
                          "textRaw": "`callback` {Function} 当处理完提供的块时调用的回调函数（可选地带有错误参数）。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "当处理完提供的块时调用的回调函数（可选地带有错误参数）。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>此函数不得由应用程序代码直接调用。它应由子类实现，并由内部 <code>Writable</code> 类方法调用。</p>\n<p><code>writable._writev()</code> 方法除了或替代 <code>writable._write()</code> 可以在流实现中实现，这些实现能够同时处理多个数据块。如果实现并且有来自先前写入的缓冲数据，则将调用 <code>_writev()</code> 而不是 <code>_write()</code>。</p>\n<p><code>writable._writev()</code> 方法带有下划线前缀，因为它是定义它的类的内部方法，不应由用户程序直接调用。</p>"
                },
                {
                  "textRaw": "`writable._destroy(err, callback)`",
                  "type": "method",
                  "name": "_destroy",
                  "meta": {
                    "added": [
                      "v8.0.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`err` {Error} 可能的错误。",
                          "name": "err",
                          "type": "Error",
                          "desc": "可能的错误。"
                        },
                        {
                          "textRaw": "`callback` {Function} 一个接受可选错误参数的回调函数。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "一个接受可选错误参数的回调函数。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p><code>_destroy()</code> 方法由 <a href=\"#writabledestroyerror\"><code>writable.destroy()</code></a> 调用。它可以被子类覆盖，但 <strong>不得</strong> 直接调用。</p>"
                },
                {
                  "textRaw": "`writable._final(callback)`",
                  "type": "method",
                  "name": "_final",
                  "meta": {
                    "added": [
                      "v8.0.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`callback` {Function} 在完成写入任何剩余数据时调用此函数（可选地带有错误参数）。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "在完成写入任何剩余数据时调用此函数（可选地带有错误参数）。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p><code>_final()</code> 方法 <strong>不得</strong> 直接调用。它可能由子类实现，如果是，则由内部 <code>Writable</code> 类方法调用。</p>\n<p>此可选函数将在流关闭之前调用，延迟 <code>'finish'</code> 事件直到调用 <code>callback</code>。这对于在流结束之前关闭资源或写入缓冲数据非常有用。</p>"
                }
              ],
              "modules": [
                {
                  "textRaw": "写入时出错",
                  "name": "写入时出错",
                  "desc": "<p>在 <a href=\"#writable_writechunk-encoding-callback\"><code>writable._write()</code></a>、<a href=\"#writable_writevchunks-callback\"><code>writable._writev()</code></a> 和 <a href=\"#writable_finalcallback\"><code>writable._final()</code></a> 方法的处理过程中发生的错误必须通过调用回调并传递错误作为第一个参数来传播。从这些方法中抛出 <code>Error</code> 或手动发出 <code>'error'</code> 事件会导致未定义的行为。</p>\n<p>如果 <code>Readable</code> 流管道传输到 <code>Writable</code> 流时 <code>Writable</code> 发出错误，则 <code>Readable</code> 流将被取消管道传输。</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nclass MyWritable extends Writable {\n  _write(chunk, encoding, callback) {\n    if (chunk.toString().indexOf('a') >= 0) {\n      callback(new Error('chunk is invalid'));\n    } else {\n      callback();\n    }\n  }\n}\n</code></pre>",
                  "type": "module",
                  "displayName": "写入时出错"
                },
                {
                  "textRaw": "可写流示例",
                  "name": "可写流示例",
                  "desc": "<p>以下说明了一个相当简单（也有点无意义）的自定义 <code>Writable</code> 流实现。虽然这个特定的 <code>Writable</code> 流实例没有任何真正的用处，但该示例说明了自定义 <a href=\"#class-streamwritable\"><code>Writable</code></a> 流实例所需的每个元素：</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\n\nclass MyWritable extends Writable {\n  _write(chunk, encoding, callback) {\n    if (chunk.toString().indexOf('a') >= 0) {\n      callback(new Error('chunk is invalid'));\n    } else {\n      callback();\n    }\n  }\n}\n</code></pre>",
                  "type": "module",
                  "displayName": "可写流示例"
                },
                {
                  "textRaw": "在可写流中解码缓冲区",
                  "name": "在可写流中解码缓冲区",
                  "desc": "<p>解码缓冲区是一项常见任务，例如，当使用输入为字符串的转换器时。当使用多字节字符编码（如 UTF-8）时，这不是一个简单的过程。以下示例展示了如何使用 <code>StringDecoder</code> 和 <a href=\"#class-streamwritable\"><code>Writable</code></a> 解码多字节字符串。</p>\n<pre><code class=\"language-js\">const { Writable } = require('node:stream');\nconst { StringDecoder } = require('node:string_decoder');\n\nclass StringWritable extends Writable {\n  constructor(options) {\n    super(options);\n    this._decoder = new StringDecoder(options?.defaultEncoding);\n    this.data = '';\n  }\n  _write(chunk, encoding, callback) {\n    if (encoding === 'buffer') {\n      chunk = this._decoder.write(chunk);\n    }\n    this.data += chunk;\n    callback();\n  }\n  _final(callback) {\n    this.data += this._decoder.end();\n    callback();\n  }\n}\n\nconst euro = [[0xE2, 0x82], [0xAC]].map(Buffer.from);\nconst w = new StringWritable();\n\nw.write('currency: ');\nw.write(euro[0]);\nw.end(euro[1]);\n\nconsole.log(w.data); // currency: €\n</code></pre>",
                  "type": "module",
                  "displayName": "在可写流中解码缓冲区"
                }
              ],
              "type": "misc",
              "displayName": "实现可写流"
            },
            {
              "textRaw": "实现可读流",
              "name": "实现可读流",
              "desc": "<p><code>stream.Readable</code> 类被扩展以实现 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流。</p>\n<p>自定义 <code>Readable</code> 流 <em>必须</em> 调用 <code>new stream.Readable([options])</code> 构造函数并实现 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法。</p>",
              "ctors": [
                {
                  "textRaw": "`new stream.Readable([options])`",
                  "type": "ctor",
                  "name": "stream.Readable",
                  "meta": {
                    "changes": [
                      {
                        "version": "v22.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/52037",
                        "description": "bump default highWaterMark."
                      },
                      {
                        "version": "v15.5.0",
                        "pr-url": "https://github.com/nodejs/node/pull/36431",
                        "description": "support passing in an AbortSignal."
                      },
                      {
                        "version": "v14.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/30623",
                        "description": "Change `autoDestroy` option default to `true`."
                      },
                      {
                        "version": [
                          "v11.2.0",
                          "v10.16.0"
                        ],
                        "pr-url": "https://github.com/nodejs/node/pull/22795",
                        "description": "Add `autoDestroy` option to automatically `destroy()` the stream when it emits `'end'` or errors."
                      }
                    ]
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`options` {Object}",
                          "name": "options",
                          "type": "Object",
                          "options": [
                            {
                              "textRaw": "`highWaterMark` {number} 在停止从底层资源读取之前存储在内部缓冲区中的 [字节数][hwm-gotcha] 的最大值。**默认值：** `65536` (64 KiB)，对于 `objectMode` 流为 `16`。",
                              "name": "highWaterMark",
                              "type": "number",
                              "desc": "在停止从底层资源读取之前存储在内部缓冲区中的 [字节数][hwm-gotcha] 的最大值。**默认值：** `65536` (64 KiB)，对于 `objectMode` 流为 `16`。"
                            },
                            {
                              "textRaw": "`encoding` {string} 如果指定，则缓冲区将使用指定编码解码为字符串。**默认值：** `null`。",
                              "name": "encoding",
                              "type": "string",
                              "desc": "如果指定，则缓冲区将使用指定编码解码为字符串。**默认值：** `null`。"
                            },
                            {
                              "textRaw": "`objectMode` {boolean} 此流是否应表现为对象流。这意味着 [`stream.read(n)`][stream-read] 返回单个值而不是大小为 `n` 的 `Buffer`。**默认值：** `false`。",
                              "name": "objectMode",
                              "type": "boolean",
                              "desc": "此流是否应表现为对象流。这意味着 [`stream.read(n)`][stream-read] 返回单个值而不是大小为 `n` 的 `Buffer`。**默认值：** `false`。"
                            },
                            {
                              "textRaw": "`emitClose` {boolean} 流在销毁后是否应发出 `'close'`。**默认值：** `true`。",
                              "name": "emitClose",
                              "type": "boolean",
                              "desc": "流在销毁后是否应发出 `'close'`。**默认值：** `true`。"
                            },
                            {
                              "textRaw": "`read` {Function} [`stream._read()`][stream-_read] 方法的实现。",
                              "name": "read",
                              "type": "Function",
                              "desc": "[`stream._read()`][stream-_read] 方法的实现。"
                            },
                            {
                              "textRaw": "`destroy` {Function} [`stream._destroy()`][readable-_destroy] 方法的实现。",
                              "name": "destroy",
                              "type": "Function",
                              "desc": "[`stream._destroy()`][readable-_destroy] 方法的实现。"
                            },
                            {
                              "textRaw": "`construct` {Function} [`stream._construct()`][readable-_construct] 方法的实现。",
                              "name": "construct",
                              "type": "Function",
                              "desc": "[`stream._construct()`][readable-_construct] 方法的实现。"
                            },
                            {
                              "textRaw": "`autoDestroy` {boolean} 此流是否应在结束后自动调用 `.destroy()`。**默认值：** `true`。",
                              "name": "autoDestroy",
                              "type": "boolean",
                              "desc": "此流是否应在结束后自动调用 `.destroy()`。**默认值：** `true`。"
                            },
                            {
                              "textRaw": "`signal` {AbortSignal} 表示可能取消的信号。",
                              "name": "signal",
                              "type": "AbortSignal",
                              "desc": "表示可能取消的信号。"
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "desc": "<!-- eslint-disable no-useless-constructor -->\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nclass MyReadable extends Readable {\n  constructor(options) {\n    // 调用 stream.Readable(options) 构造函数。\n    super(options);\n    // ...\n  }\n}\n</code></pre>\n<p>或者，在使用 ES6 之前风格的构造函数时：</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\nconst util = require('node:util');\n\nfunction MyReadable(options) {\n  if (!(this instanceof MyReadable))\n    return new MyReadable(options);\n  Readable.call(this, options);\n}\nutil.inherits(MyReadable, Readable);\n</code></pre>\n<p>或者，使用简化构造函数方法：</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nconst myReadable = new Readable({\n  read(size) {\n    // ...\n  },\n});\n</code></pre>\n<p>在传递的 <code>AbortSignal</code> 对应的 <code>AbortController</code> 上调用 <code>abort</code> 的行为方式与在创建的可读流上调用 <code>.destroy(new AbortError())</code> 相同。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\nconst controller = new AbortController();\nconst read = new Readable({\n  read(size) {\n    // ...\n  },\n  signal: controller.signal,\n});\n// 稍后，中止操作，关闭流。\ncontroller.abort();\n</code></pre>"
                }
              ],
              "methods": [
                {
                  "textRaw": "`readable._construct(callback)`",
                  "type": "method",
                  "name": "_construct",
                  "meta": {
                    "added": [
                      "v15.0.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`callback` {Function} 当流完成初始化时调用此函数（可选地带有错误参数）。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "当流完成初始化时调用此函数（可选地带有错误参数）。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p><code>_construct()</code> 方法不得直接调用。它可能由子类实现，如果是，则仅由内部 <code>Readable</code> 类方法调用。</p>\n<p>此可选函数将由流构造函数在下一个时间点调度，延迟任何 <code>_read()</code> 和 <code>_destroy()</code> 调用，直到调用 <code>callback</code>。这对于在流可以使用之前初始化状态或异步初始化资源非常有用。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\nconst fs = require('node:fs');\n\nclass ReadStream extends Readable {\n  constructor(filename) {\n    super();\n    this.filename = filename;\n    this.fd = null;\n  }\n  _construct(callback) {\n    fs.open(this.filename, (err, fd) => {\n      if (err) {\n        callback(err);\n      } else {\n        this.fd = fd;\n        callback();\n      }\n    });\n  }\n  _read(n) {\n    const buf = Buffer.alloc(n);\n    fs.read(this.fd, buf, 0, n, null, (err, bytesRead) => {\n      if (err) {\n        this.destroy(err);\n      } else {\n        this.push(bytesRead > 0 ? buf.slice(0, bytesRead) : null);\n      }\n    });\n  }\n  _destroy(err, callback) {\n    if (this.fd) {\n      fs.close(this.fd, (er) => callback(er || err));\n    } else {\n      callback(err);\n    }\n  }\n}\n</code></pre>"
                },
                {
                  "textRaw": "`readable._read(size)`",
                  "type": "method",
                  "name": "_read",
                  "meta": {
                    "added": [
                      "v0.9.4"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`size` {number} 要异步读取的字节数",
                          "name": "size",
                          "type": "number",
                          "desc": "要异步读取的字节数"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>此函数严禁由应用程序代码直接调用。它应由子类实现，并且仅由内部 <code>Readable</code> 类方法调用。</p>\n<p>所有 <code>Readable</code> 流实现都必须提供 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法的实现，以从底层资源获取数据。</p>\n<p>当 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 被调用时，如果资源中有数据可用，实现应使用 <a href=\"#readablepushchunk-encoding\"><code>this.push(dataChunk)</code></a> 方法开始将数据推入读取队列。在每次调用 <a href=\"#readablepushchunk-encoding\"><code>this.push(dataChunk)</code></a> 后，一旦流准备好接受更多数据，<code>_read()</code> 将再次被调用。<code>_read()</code> 可以继续从资源读取并推送数据，直到 <code>readable.push()</code> 返回 <code>false</code>。只有在 <code>_read()</code> 停止后再次被调用时，它才应恢复向队列推送额外数据。</p>\n<p>一旦 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法被调用，在通过 <a href=\"#readablepushchunk-encoding\"><code>readable.push()</code></a> 方法推送更多数据之前，它不会被再次调用。空数据（如空缓冲区和字符串）不会导致 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 被调用。</p>\n<p><code>size</code> 参数是建议性的。对于将\"读取\"作为返回数据的单个操作的实现，可以使用 <code>size</code> 参数来确定要获取多少数据。其他实现可能会忽略此参数，只在数据可用时提供数据。无需在调用 <a href=\"#readablepushchunk-encoding\"><code>stream.push(chunk)</code></a> 前等待直到有 <code>size</code> 字节可用。</p>\n<p><a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法带有下划线前缀，因为它是定义它的类的内部方法，绝不应由用户程序直接调用。</p>"
                },
                {
                  "textRaw": "`readable._destroy(err, callback)`",
                  "type": "method",
                  "name": "_destroy",
                  "meta": {
                    "added": [
                      "v8.0.0"
                    ],
                    "changes": []
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`err` {Error} 可能的错误。",
                          "name": "err",
                          "type": "Error",
                          "desc": "可能的错误。"
                        },
                        {
                          "textRaw": "`callback` {Function} 一个回调函数，接受一个可选的错误参数。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "一个回调函数，接受一个可选的错误参数。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p><code>_destroy()</code> 方法由 <a href=\"#readabledestroyerror\"><code>readable.destroy()</code></a> 调用。它可以被子类重写，但<strong>严禁</strong>直接调用。</p>"
                },
                {
                  "textRaw": "`readable.push(chunk[, encoding])`",
                  "type": "method",
                  "name": "push",
                  "meta": {
                    "changes": [
                      {
                        "version": [
                          "v22.0.0",
                          "v20.13.0"
                        ],
                        "pr-url": "https://github.com/nodejs/node/pull/51866",
                        "description": "The `chunk` argument can now be a `TypedArray` or `DataView` instance."
                      },
                      {
                        "version": "v8.0.0",
                        "pr-url": "https://github.com/nodejs/node/pull/11608",
                        "description": "The `chunk` argument can now be a `Uint8Array` instance."
                      }
                    ]
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`chunk` {Buffer|TypedArray|DataView|string|null|any} 要推入读取队列的数据块。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是任何 JavaScript 值。",
                          "name": "chunk",
                          "type": "Buffer|TypedArray|DataView|string|null|any",
                          "desc": "要推入读取队列的数据块。对于不在对象模式下操作的流，`chunk` 必须是 {string}、{Buffer}、{TypedArray} 或 {DataView}。对于对象模式流，`chunk` 可以是任何 JavaScript 值。"
                        },
                        {
                          "textRaw": "`encoding` {string} 字符串块的编码。必须是有效的 `Buffer` 编码，例如 `'utf8'` 或 `'ascii'`。",
                          "name": "encoding",
                          "type": "string",
                          "desc": "字符串块的编码。必须是有效的 `Buffer` 编码，例如 `'utf8'` 或 `'ascii'`。"
                        },
                        {
                          "textRaw": "返回: {boolean} 如果可以继续推送更多数据块，则为 `true`；否则为 `false`。",
                          "name": "返回",
                          "type": "boolean",
                          "desc": "如果可以继续推送更多数据块，则为 `true`；否则为 `false`。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>当 <code>chunk</code> 是 <a href=\"buffer.html#class-buffer\" class=\"type\">&lt;Buffer&gt;</a>、<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypedArray\" class=\"type\">&lt;TypedArray&gt;</a>、<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/DataView\" class=\"type\">&lt;DataView&gt;</a> 或 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#String_type\" class=\"type\">&lt;string&gt;</a> 时，该数据块将被添加到内部队列中供流的用户使用。将 <code>chunk</code> 传递为 <code>null</code> 表示流结束（EOF），之后不能再写入数据。</p>\n<p>当 <code>Readable</code> 处于暂停模式时，使用 <code>readable.push()</code> 添加的数据可以在触发 <a href=\"#event-readable\"><code>'readable'</code></a> 事件时通过调用 <a href=\"#readablereadsize\"><code>readable.read()</code></a> 方法读取。</p>\n<p>当 <code>Readable</code> 处于流动模式时，使用 <code>readable.push()</code> 添加的数据将通过触发 <code>'data'</code> 事件来传递。</p>\n<p><code>readable.push()</code> 方法设计得尽可能灵活。例如，当包装一个提供某种形式的暂停/恢复机制和数据回调的低级源时，可以使用自定义的 <code>Readable</code> 实例来包装该低级源：</p>\n<pre><code class=\"language-js\">// `_source` is an object with readStop() and readStart() methods,\n// and an `ondata` member that gets called when it has data, and\n// an `onend` member that gets called when the data is over.\n\nclass SourceWrapper extends Readable {\n  constructor(options) {\n    super(options);\n\n    this._source = getLowLevelSourceObject();\n\n    // Every time there's data, push it into the internal buffer.\n    this._source.ondata = (chunk) => {\n      // If push() returns false, then stop reading from source.\n      if (!this.push(chunk))\n        this._source.readStop();\n    };\n\n    // When the source ends, push the EOF-signaling `null` chunk.\n    this._source.onend = () => {\n      this.push(null);\n    };\n  }\n  // _read() will be called when the stream wants to pull more data in.\n  // The advisory size argument is ignored in this case.\n  _read(size) {\n    this._source.readStart();\n  }\n}\n</code></pre>\n<p><code>readable.push()</code> 方法用于将内容推入内部缓冲区。它可以由 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法驱动。</p>\n<p>对于不在对象模式下操作的流，如果 <code>readable.push()</code> 的 <code>chunk</code> 参数是 <code>undefined</code>，它将被视为空字符串或缓冲区。有关更多信息，请参阅 <a href=\"#readablepush\"><code>readable.push('')</code></a>。</p>"
                }
              ],
              "modules": [
                {
                  "textRaw": "读取过程中的错误",
                  "name": "读取过程中的错误",
                  "desc": "<p>在 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 处理过程中发生的错误必须通过 <a href=\"#readable_destroyerr-callback\"><code>readable.destroy(err)</code></a> 方法传播。从 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 内部抛出 <code>Error</code> 或手动触发 <code>'error'</code> 事件会导致未定义的行为。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nconst myReadable = new Readable({\n  read(size) {\n    const err = checkSomeErrorCondition();\n    if (err) {\n      this.destroy(err);\n    } else {\n      // Do some work.\n    }\n  },\n});\n</code></pre>",
                  "type": "module",
                  "displayName": "读取过程中的错误"
                }
              ],
              "examples": [
                {
                  "textRaw": "一个计数流的示例",
                  "name": "一个计数流的示例",
                  "type": "example",
                  "desc": "<p>以下是一个 <code>Readable</code> 流的基本示例，它按升序发射从 1 到 1,000,000 的数字，然后结束。</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nclass Counter extends Readable {\n  constructor(opt) {\n    super(opt);\n    this._max = 1000000;\n    this._index = 1;\n  }\n\n  _read() {\n    const i = this._index++;\n    if (i > this._max)\n      this.push(null);\n    else {\n      const str = String(i);\n      const buf = Buffer.from(str, 'ascii');\n      this.push(buf);\n    }\n  }\n}\n</code></pre>"
                }
              ],
              "type": "misc",
              "displayName": "实现可读流"
            },
            {
              "textRaw": "实现双工流",
              "name": "实现双工流",
              "desc": "<p><a href=\"#class-streamduplex\"><code>Duplex</code></a> 流是同时实现 <a href=\"#class-streamreadable\"><code>Readable</code></a> 和 <a href=\"#class-streamwritable\"><code>Writable</code></a> 的流，例如 TCP 套接字连接。</p>\n<p>由于 JavaScript 不支持多重继承，因此扩展 <code>stream.Duplex</code> 类来实现 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流（而不是扩展 <code>stream.Readable</code> <em>和</em> <code>stream.Writable</code> 类）。</p>\n<p><code>stream.Duplex</code> 类原型继承自 <code>stream.Readable</code>，并寄生继承自 <code>stream.Writable</code>，但由于在 <code>stream.Writable</code> 上重写了 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Symbol/hasInstance\"><code>Symbol.hasInstance</code></a>，<code>instanceof</code> 对于两个基类都能正常工作。</p>\n<p>自定义 <code>Duplex</code> 流 <em>必须</em> 调用 <code>new stream.Duplex([options])</code> 构造函数并实现 <em>both</em> <a href=\"#readable_readsize\"><code>readable._read()</code></a> 和 <code>writable._write()</code> 方法。</p>",
              "ctors": [
                {
                  "textRaw": "`new stream.Duplex(options)`",
                  "type": "ctor",
                  "name": "stream.Duplex",
                  "meta": {
                    "changes": [
                      {
                        "version": "v8.4.0",
                        "pr-url": "https://github.com/nodejs/node/pull/14636",
                        "description": "The `readableHighWaterMark` and `writableHighWaterMark` options are supported now."
                      }
                    ]
                  },
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`options` {Object} 传递给 `Writable` 和 `Readable` 构造函数。还有以下字段：",
                          "name": "options",
                          "type": "Object",
                          "desc": "传递给 `Writable` 和 `Readable` 构造函数。还有以下字段：",
                          "options": [
                            {
                              "textRaw": "`allowHalfOpen` {boolean} 如果设置为 `false`，则当可读端结束时，流将自动结束可写端。**默认值:** `true`。",
                              "name": "allowHalfOpen",
                              "type": "boolean",
                              "desc": "如果设置为 `false`，则当可读端结束时，流将自动结束可写端。**默认值:** `true`。"
                            },
                            {
                              "textRaw": "`readable` {boolean} 设置 `Duplex` 是否可读。**默认值:** `true`。",
                              "name": "readable",
                              "type": "boolean",
                              "desc": "设置 `Duplex` 是否可读。**默认值:** `true`。"
                            },
                            {
                              "textRaw": "`writable` {boolean} 设置 `Duplex` 是否可写。**默认值:** `true`。",
                              "name": "writable",
                              "type": "boolean",
                              "desc": "设置 `Duplex` 是否可写。**默认值:** `true`。"
                            },
                            {
                              "textRaw": "`readableObjectMode` {boolean} 设置流的可读端的 `objectMode`。如果 `objectMode` 为 `true`，则无效。**默认值:** `false`。",
                              "name": "readableObjectMode",
                              "type": "boolean",
                              "desc": "设置流的可读端的 `objectMode`。如果 `objectMode` 为 `true`，则无效。**默认值:** `false`。"
                            },
                            {
                              "textRaw": "`writableObjectMode` {boolean} 设置流的可写端的 `objectMode`。如果 `objectMode` 为 `true`，则无效。**默认值:** `false`。",
                              "name": "writableObjectMode",
                              "type": "boolean",
                              "desc": "设置流的可写端的 `objectMode`。如果 `objectMode` 为 `true`，则无效。**默认值:** `false`。"
                            },
                            {
                              "textRaw": "`readableHighWaterMark` {number} 设置流的可读端的 `highWaterMark`。如果提供了 `highWaterMark`，则无效。",
                              "name": "readableHighWaterMark",
                              "type": "number",
                              "desc": "设置流的可读端的 `highWaterMark`。如果提供了 `highWaterMark`，则无效。"
                            },
                            {
                              "textRaw": "`writableHighWaterMark` {number} 设置流的可写端的 `highWaterMark`。如果提供了 `highWaterMark`，则无效。",
                              "name": "writableHighWaterMark",
                              "type": "number",
                              "desc": "设置流的可写端的 `highWaterMark`。如果提供了 `highWaterMark`，则无效。"
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "desc": "<!-- eslint-disable no-useless-constructor -->\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\n\nclass MyDuplex extends Duplex {\n  constructor(options) {\n    super(options);\n    // ...\n  }\n}\n</code></pre>\n<p>或者，当使用 ES6 之前的样式构造函数时：</p>\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\nconst util = require('node:util');\n\nfunction MyDuplex(options) {\n  if (!(this instanceof MyDuplex))\n    return new MyDuplex(options);\n  Duplex.call(this, options);\n}\nutil.inherits(MyDuplex, Duplex);\n</code></pre>\n<p>或者，使用简化的构造函数方法：</p>\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\n\nconst myDuplex = new Duplex({\n  read(size) {\n    // ...\n  },\n  write(chunk, encoding, callback) {\n    // ...\n  },\n});\n</code></pre>\n<p>当使用 pipeline 时：</p>\n<pre><code class=\"language-js\">const { Transform, pipeline } = require('node:stream');\nconst fs = require('node:fs');\n\npipeline(\n  fs.createReadStream('object.json')\n    .setEncoding('utf8'),\n  new Transform({\n    decodeStrings: false, // Accept string input rather than Buffers\n    construct(callback) {\n      this.data = '';\n      callback();\n    },\n    transform(chunk, encoding, callback) {\n      this.data += chunk;\n      callback();\n    },\n    flush(callback) {\n      try {\n        // Make sure is valid json.\n        JSON.parse(this.data);\n        this.push(this.data);\n        callback();\n      } catch (err) {\n        callback(err);\n      }\n    },\n  }),\n  fs.createWriteStream('valid-object.json'),\n  (err) => {\n    if (err) {\n      console.error('failed', err);\n    } else {\n      console.log('completed');\n    }\n  },\n);\n</code></pre>"
                }
              ],
              "modules": [
                {
                  "textRaw": "一个双工流的示例",
                  "name": "一个双工流的示例",
                  "desc": "<p>以下是一个简单的 <code>Duplex</code> 流示例，它包装了一个假设的低级源对象，可以向其写入数据，并从中读取数据，尽管使用的 API 与 Node.js 流不兼容。以下是一个简单的 <code>Duplex</code> 流示例，它通过 <a href=\"#class-streamwritable\"><code>Writable</code></a> 接口缓冲传入的写入数据，并通过 <a href=\"#class-streamreadable\"><code>Readable</code></a> 接口读回。</p>\n<pre><code class=\"language-js\">const { Duplex } = require('node:stream');\nconst kSource = Symbol('source');\n\nclass MyDuplex extends Duplex {\n  constructor(source, options) {\n    super(options);\n    this[kSource] = source;\n  }\n\n  _write(chunk, encoding, callback) {\n    // The underlying source only deals with strings.\n    if (Buffer.isBuffer(chunk))\n      chunk = chunk.toString();\n    this[kSource].writeSomeData(chunk);\n    callback();\n  }\n\n  _read(size) {\n    this[kSource].fetchSomeData(size, (data, encoding) => {\n      this.push(Buffer.from(data, encoding));\n    });\n  }\n}\n</code></pre>\n<p><code>Duplex</code> 流最重要的方面是 <code>Readable</code> 和 <code>Writable</code> 端彼此独立运行，尽管共存于单个对象实例中。</p>",
                  "type": "module",
                  "displayName": "一个双工流的示例"
                },
                {
                  "textRaw": "对象模式双工流",
                  "name": "对象模式双工流",
                  "desc": "<p>对于 <code>Duplex</code> 流，可以分别使用 <code>readableObjectMode</code> 和 <code>writableObjectMode</code> 选项单独为 <code>Readable</code> 或 <code>Writable</code> 端设置 <code>objectMode</code>。</p>\n<p>例如，在以下示例中，创建了一个新的 <code>Transform</code> 流（一种 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流），它具有一个对象模式的可写端，接受 JavaScript 数字，并在可读端转换为十六进制字符串。</p>\n<pre><code class=\"language-js\">const { Transform } = require('node:stream');\n\n// All Transform streams are also Duplex Streams.\nconst myTransform = new Transform({\n  writableObjectMode: true,\n\n  transform(chunk, encoding, callback) {\n    // Coerce the chunk to a number if necessary.\n    chunk |= 0;\n\n    // Transform the chunk into something else.\n    const data = chunk.toString(16);\n\n    // Push the data onto the readable queue.\n    callback(null, '0'.repeat(data.length % 2) + data);\n  },\n});\n\nmyTransform.setEncoding('ascii');\nmyTransform.on('data', (chunk) => console.log(chunk));\n\nmyTransform.write(1);\n// Prints: 01\nmyTransform.write(10);\n// Prints: 0a\nmyTransform.write(100);\n// Prints: 64\n</code></pre>",
                  "type": "module",
                  "displayName": "对象模式双工流"
                }
              ],
              "type": "misc",
              "displayName": "实现双工流"
            },
            {
              "textRaw": "实现转换流",
              "name": "实现转换流",
              "desc": "<p><a href=\"#class-streamtransform\"><code>Transform</code></a> 流是一种 <a href=\"#class-streamduplex\"><code>Duplex</code></a> 流，其输出以某种方式从输入计算得出。例如包括压缩、加密或解密数据的 <a href=\"zlib.html\">zlib</a> 流或 <a href=\"crypto.html\">crypto</a> 流。</p>\n<p>不要求输出与输入大小相同、块数相同或同时到达。例如，<code>Hash</code> 流只会有一个输出块，在输入结束时提供。<code>zlib</code> 流产生的输出可能比其输入小得多或大得多。</p>\n<p><code>stream.Transform</code> 类被扩展以实现 <a href=\"#class-streamtransform\"><code>Transform</code></a> 流。</p>\n<p><code>stream.Transform</code> 类原型继承自 <code>stream.Duplex</code>，并实现了自己版本的 <code>writable._write()</code> 和 <a href=\"#readable_readsize\"><code>readable._read()</code></a> 方法。自定义 <code>Transform</code> 实现 <em>必须</em> 实现 <a href=\"#transform_transformchunk-encoding-callback\"><code>transform._transform()</code></a> 方法，并且 <em>可能</em> 也实现 <a href=\"#transform_flushcallback\"><code>transform._flush()</code></a> 方法。</p>\n<p>使用 <code>Transform</code> 流时必须小心，因为如果 <code>Readable</code> 端的输出未被消耗，写入流的数据可能导致流的 <code>Writable</code> 端暂停。</p>",
              "ctors": [
                {
                  "textRaw": "`new stream.Transform([options])`",
                  "type": "ctor",
                  "name": "stream.Transform",
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`options` {Object} 传递给 `Writable` 和 `Readable` 构造函数。还有以下字段：",
                          "name": "options",
                          "type": "Object",
                          "desc": "传递给 `Writable` 和 `Readable` 构造函数。还有以下字段：",
                          "options": [
                            {
                              "textRaw": "`transform` {Function} [`stream._transform()`][stream-_transform] 方法的实现。",
                              "name": "transform",
                              "type": "Function",
                              "desc": "[`stream._transform()`][stream-_transform] 方法的实现。"
                            },
                            {
                              "textRaw": "`flush` {Function} [`stream._flush()`][stream-_flush] 方法的实现。",
                              "name": "flush",
                              "type": "Function",
                              "desc": "[`stream._flush()`][stream-_flush] 方法的实现。"
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "desc": "<!-- eslint-disable no-useless-constructor -->\n<pre><code class=\"language-js\">const { Transform } = require('node:stream');\n\nclass MyTransform extends Transform {\n  constructor(options) {\n    super(options);\n    // ...\n  }\n}\n</code></pre>\n<p>或者，当使用 ES6 之前的样式构造函数时：</p>\n<pre><code class=\"language-js\">const { Transform } = require('node:stream');\nconst util = require('node:util');\n\nfunction MyTransform(options) {\n  if (!(this instanceof MyTransform))\n    return new MyTransform(options);\n  Transform.call(this, options);\n}\nutil.inherits(MyTransform, Transform);\n</code></pre>\n<p>或者，使用简化的构造函数方法：</p>\n<pre><code class=\"language-js\">const { Transform } = require('node:stream');\n\nconst myTransform = new Transform({\n  transform(chunk, encoding, callback) {\n    // ...\n  },\n});\n</code></pre>"
                }
              ],
              "modules": [
                {
                  "textRaw": "事件：`'end'`",
                  "name": "事件：`'end'`",
                  "desc": "<p><a href=\"#event-end\"><code>'end'</code></a> 事件来自 <code>stream.Readable</code> 类。在所有数据输出后（即在 <a href=\"#transform_flushcallback\"><code>transform._flush()</code></a> 中的回调被调用后）会触发 <code>'end'</code> 事件。如果发生错误，不应触发 <code>'end'</code>。</p>",
                  "type": "module",
                  "displayName": "事件：`'end'`"
                },
                {
                  "textRaw": "事件：`'finish'`",
                  "name": "事件：`'finish'`",
                  "desc": "<p><a href=\"#event-finish\"><code>'finish'</code></a> 事件来自 <code>stream.Writable</code> 类。在调用 <a href=\"#writableendchunk-encoding-callback\"><code>stream.end()</code></a> 并且所有块已被 <a href=\"#transform_transformchunk-encoding-callback\"><code>stream._transform()</code></a> 处理后，会触发 <code>'finish'</code> 事件。如果发生错误，不应触发 <code>'finish'</code>。</p>",
                  "type": "module",
                  "displayName": "事件：`'finish'`"
                },
                {
                  "textRaw": "类：`stream.PassThrough`",
                  "name": "类：`stream.passthrough`",
                  "desc": "<p><code>stream.PassThrough</code> 类是 <a href=\"#class-streamtransform\"><code>Transform</code></a> 流的一个简单实现，它只是将输入字节传递到输出。它主要用于示例和测试，但在某些情况下，<code>stream.PassThrough</code> 作为新型流的构建块是有用的。</p>",
                  "type": "module",
                  "displayName": "类：`stream.PassThrough`"
                }
              ],
              "methods": [
                {
                  "textRaw": "`transform._flush(callback)`",
                  "type": "method",
                  "name": "_flush",
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`callback` {Function} 一个回调函数（可选地带有错误参数和数据），在剩余数据被刷新时调用。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "一个回调函数（可选地带有错误参数和数据），在剩余数据被刷新时调用。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>此函数严禁由应用程序代码直接调用。它应由子类实现，并且仅由内部 <code>Readable</code> 类方法调用。</p>\n<p>在某些情况下，转换操作可能需要在流结束时发出额外的数据位。例如，<code>zlib</code> 压缩流将存储一定量的内部状态以最优地压缩输出。然而，当流结束时，这些额外数据需要被刷新，以便压缩数据是完整的。</p>\n<p>自定义 <a href=\"#class-streamtransform\"><code>Transform</code></a> 实现 <em>可能</em> 实现 <code>transform._flush()</code> 方法。这将在没有更多写入数据需要消耗时调用，但在触发 <a href=\"#event-end\"><code>'end'</code></a> 事件表示 <a href=\"#class-streamreadable\"><code>Readable</code></a> 流结束之前。</p>\n<p>在 <code>transform._flush()</code> 实现中，可以零次或多次调用 <code>transform.push()</code> 方法。刷新操作完成时必须调用 <code>callback</code> 函数。</p>\n<p><code>transform._flush()</code> 方法带有下划线前缀，因为它是定义它的类的内部方法，绝不应由用户程序直接调用。</p>"
                },
                {
                  "textRaw": "`transform._transform(chunk, encoding, callback)`",
                  "type": "method",
                  "name": "_transform",
                  "signatures": [
                    {
                      "params": [
                        {
                          "textRaw": "`chunk` {Buffer|string|any} 要转换的 `Buffer`，从传递给 [`stream.write()`][stream-write] 的 `string` 转换而来。如果流的 `decodeStrings` 选项为 `false` 或流处于对象模式，则块不会被转换，而是传递给 [`stream.write()`][stream-write] 的任何内容。",
                          "name": "chunk",
                          "type": "Buffer|string|any",
                          "desc": "要转换的 `Buffer`，从传递给 [`stream.write()`][stream-write] 的 `string` 转换而来。如果流的 `decodeStrings` 选项为 `false` 或流处于对象模式，则块不会被转换，而是传递给 [`stream.write()`][stream-write] 的任何内容。"
                        },
                        {
                          "textRaw": "`encoding` {string} 如果块是字符串，则这是编码类型。如果块是缓冲区，则这是特殊值 `'buffer'`。在这种情况下忽略它。",
                          "name": "encoding",
                          "type": "string",
                          "desc": "如果块是字符串，则这是编码类型。如果块是缓冲区，则这是特殊值 `'buffer'`。在这种情况下忽略它。"
                        },
                        {
                          "textRaw": "`callback` {Function} 一个回调函数（可选地带有错误参数和数据），在提供的 `chunk` 被处理后调用。",
                          "name": "callback",
                          "type": "Function",
                          "desc": "一个回调函数（可选地带有错误参数和数据），在提供的 `chunk` 被处理后调用。"
                        }
                      ]
                    }
                  ],
                  "desc": "<p>此函数严禁由应用程序代码直接调用。它应由子类实现，并且仅由内部 <code>Readable</code> 类方法调用。</p>\n<p>所有 <code>Transform</code> 流实现都必须提供 <code>_transform()</code> 方法来接受输入并产生输出。<code>transform._transform()</code> 实现处理写入的字节，计算输出，然后使用 <code>transform.push()</code> 方法将该输出传递给可读部分。</p>\n<p>可以根据块要输出多少结果，零次或多次调用 <code>transform.push()</code> 方法从单个输入块生成输出。</p>\n<p>可能从任何给定的输入数据块中都不会生成输出。</p>\n<p>只有在当前块完全消耗后才必须调用 <code>callback</code> 函数。如果处理输入时发生错误，传递给 <code>callback</code> 的第一个参数必须是 <code>Error</code> 对象，否则为 <code>null</code>。如果第二个参数传递给 <code>callback</code>，它将被转发到 <code>transform.push()</code> 方法，但仅当第一个参数为假值时。换句话说，以下内容是等价的：</p>\n<pre><code class=\"language-js\">transform.prototype._transform = function(data, encoding, callback) {\n  this.push(data);\n  callback();\n};\n\ntransform.prototype._transform = function(data, encoding, callback) {\n  callback(null, data);\n};\n</code></pre>\n<p><code>transform._transform()</code> 方法带有下划线前缀，因为它是定义它的类的内部方法，绝不应由用户程序直接调用。</p>\n<p><code>transform._transform()</code> 永远不会并行调用；流实现了队列机制，要接收下一个块，必须调用 <code>callback</code>，可以是同步的或异步的。</p>"
                }
              ],
              "type": "misc",
              "displayName": "实现转换流"
            }
          ]
        },
        {
          "textRaw": "附加说明",
          "name": "附加说明",
          "type": "misc",
          "miscs": [
            {
              "textRaw": "流与异步生成器和异步迭代器的兼容性",
              "name": "流与异步生成器和异步迭代器的兼容性",
              "desc": "<p>随着 JavaScript 中异步生成器和迭代器的支持，异步生成器实际上此时已成为一种一流语言级别的流构造。</p>\n<p>下面提供了一些使用 Node.js 流与异步生成器和异步迭代器的常见互操作案例。</p>",
              "modules": [
                {
                  "textRaw": "使用异步迭代器消费可读流",
                  "name": "使用异步迭代器消费可读流",
                  "desc": "<pre><code class=\"language-js\">(async function() {\n  for await (const chunk of readable) {\n    console.log(chunk);\n  }\n})();\n</code></pre>\n<p>异步迭代器在流上注册一个永久错误处理程序，以防止任何未处理的销毁后错误。</p>",
                  "type": "module",
                  "displayName": "使用异步迭代器消费可读流"
                },
                {
                  "textRaw": "使用异步生成器创建可读流",
                  "name": "使用异步生成器创建可读流",
                  "desc": "<p>可以使用 <code>Readable.from()</code> 工具方法从异步生成器创建 Node.js 可读流：</p>\n<pre><code class=\"language-js\">const { Readable } = require('node:stream');\n\nconst ac = new AbortController();\nconst signal = ac.signal;\n\nasync function * generate() {\n  yield 'a';\n  await someLongRunningFn({ signal });\n  yield 'b';\n  yield 'c';\n}\n\nconst readable = Readable.from(generate());\nreadable.on('close', () => {\n  ac.abort();\n});\n\nreadable.on('data', (chunk) => {\n  console.log(chunk);\n});\n</code></pre>",
                  "type": "module",
                  "displayName": "使用异步生成器创建可读流"
                }
              ],
              "miscs": [
                {
                  "textRaw": "从异步迭代器管道传输到可写流",
                  "name": "从异步迭代器管道传输到可写流",
                  "type": "misc",
                  "desc": "<p>当从异步迭代器写入可写流时，确保正确处理背压和错误。<a href=\"#streampipelinesource-transforms-destination-callback\"><code>stream.pipeline()</code></a> 抽象了背压和背压相关错误的处理：</p>\n<pre><code class=\"language-js\">const fs = require('node:fs');\nconst { pipeline } = require('node:stream');\nconst { pipeline: pipelinePromise } = require('node:stream/promises');\n\nconst writable = fs.createWriteStream('./file');\n\nconst ac = new AbortController();\nconst signal = ac.signal;\n\nconst iterator = createIterator({ signal });\n\n// Callback Pattern\npipeline(iterator, writable, (err, value) => {\n  if (err) {\n    console.error(err);\n  } else {\n    console.log(value, 'value returned');\n  }\n}).on('close', () => {\n  ac.abort();\n});\n\n// Promise Pattern\npipelinePromise(iterator, writable)\n  .then((value) => {\n    console.log(value, 'value returned');\n  })\n  .catch((err) => {\n    console.error(err);\n    ac.abort();\n  });\n</code></pre>"
                }
              ],
              "type": "misc",
              "displayName": "流与异步生成器和异步迭代器的兼容性"
            },
            {
              "textRaw": "与旧版 Node.js 的兼容性",
              "name": "与旧版 Node.js 的兼容性",
              "type": "misc",
              "desc": "<p>在 Node.js 0.10 之前，<code>Readable</code> 流接口更简单，但功能也更少、用处也更小。</p>\n<ul>\n<li>而不是等待调用 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 方法，<a href=\"#event-data\"><code>'data'</code></a> 事件会立即开始触发。需要执行一定工作量来决定如何处理数据的应用程序需要将读取的数据存储到缓冲区中，以免数据丢失。</li>\n<li><a href=\"#readablepause\"><code>stream.pause()</code></a> 方法是建议性的，而不是保证性的。这意味着即使流处于暂停状态，仍然需要准备接收 <a href=\"#event-data\"><code>'data'</code></a> 事件。</li>\n</ul>\n<p>在 Node.js 0.10 中，添加了 <a href=\"#class-streamreadable\"><code>Readable</code></a> 类。为了与旧版 Node.js 程序向后兼容，当添加 <a href=\"#event-data\"><code>'data'</code></a> 事件处理程序或调用 <a href=\"#readableresume\"><code>stream.resume()</code></a> 方法时，<code>Readable</code> 流会切换到\"流动模式\"。效果是，即使不使用新的 <a href=\"#readablereadsize\"><code>stream.read()</code></a> 方法和 <a href=\"#event-readable\"><code>'readable'</code></a> 事件，也不再需要担心丢失 <a href=\"#event-data\"><code>'data'</code></a> 块。</p>\n<p>虽然大多数应用程序会继续正常工作，但这在以下条件下引入了一个边缘情况：</p>\n<ul>\n<li>没有添加 <a href=\"#event-data\"><code>'data'</code></a> 事件监听器。</li>\n<li>从未调用 <a href=\"#readableresume\"><code>stream.resume()</code></a> 方法。</li>\n<li>流没有管道传输到任何可写目标。</li>\n</ul>\n<p>例如，考虑以下代码：</p>\n<pre><code class=\"language-js\">// WARNING!  BROKEN!\nnet.createServer((socket) => {\n\n  // We add an 'end' listener, but never consume the data.\n  socket.on('end', () => {\n    // It will never get here.\n    socket.end('The message was received but was not processed.\\n');\n  });\n\n}).listen(1337);\n</code></pre>\n<p>在 Node.js 0.10 之前，传入的消息数据会被简单丢弃。然而，在 Node.js 0.10 及更高版本中，套接字将永远保持暂停状态。</p>\n<p>这种情况下的解决方法是调用 <a href=\"#readableresume\"><code>stream.resume()</code></a> 方法开始数据流：</p>\n<pre><code class=\"language-js\">// Workaround.\nnet.createServer((socket) => {\n  socket.on('end', () => {\n    socket.end('The message was received but was not processed.\\n');\n  });\n\n  // Start the flow of data, discarding it.\n  socket.resume();\n}).listen(1337);\n</code></pre>\n<p>除了新的 <code>Readable</code> 流切换到流动模式之外，还可以使用 <a href=\"#readablewrapstream\"><code>readable.wrap()</code></a> 方法将 0.10 之前样式的流包装在 <code>Readable</code> 类中。</p>"
            },
            {
              "textRaw": "调用 `readable.setEncoding()` 后 `highWaterMark` 差异",
              "name": "调用_`readable.setencoding()`_后_`highwatermark`_差异",
              "desc": "<p>使用 <code>readable.setEncoding()</code> 将改变 <code>highWaterMark</code> 在非对象模式下的操作方式。</p>\n<p>通常，当前缓冲区的大小以 <em>字节</em> 为单位与 <code>highWaterMark</code> 进行比较。然而，在调用 <code>setEncoding()</code> 后，比较函数将开始以 <em>字符</em> 为单位测量缓冲区的大小。</p>\n<p>在 <code>latin1</code> 或 <code>ascii</code> 的常见情况下，这不是问题。但在处理可能包含多字节字符的字符串时，建议注意此行为。</p>",
              "type": "misc",
              "displayName": "调用 `readable.setEncoding()` 后 `highWaterMark` 差异"
            }
          ],
          "methods": [
            {
              "textRaw": "`readable.read(0)`",
              "type": "method",
              "name": "read",
              "signatures": [
                {
                  "params": []
                }
              ],
              "desc": "<p>在某些情况下，有必要触发底层可读流机制的刷新，而不实际消耗任何数据。在这种情况下，可以调用 <code>readable.read(0)</code>，它总是返回 <code>null</code>。</p>\n<p>如果内部读取缓冲区低于 <code>highWaterMark</code>，并且流当前没有在读取，那么调用 <code>stream.read(0)</code> 将触发低级的 <a href=\"#readable_readsize\"><code>stream._read()</code></a> 调用。</p>\n<p>虽然大多数应用程序几乎不需要这样做，但在 Node.js 内部有一些情况会这样做，特别是在 <code>Readable</code> 流类内部。</p>"
            },
            {
              "textRaw": "`readable.push('')`",
              "type": "method",
              "name": "push",
              "signatures": [
                {
                  "params": []
                }
              ],
              "desc": "<p>不建议使用 <code>readable.push('')</code>。</p>\n<p>将零字节的 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#String_type\" class=\"type\">&lt;string&gt;</a>、<a href=\"buffer.html#class-buffer\" class=\"type\">&lt;Buffer&gt;</a>、<a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypedArray\" class=\"type\">&lt;TypedArray&gt;</a> 或 <a href=\"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/DataView\" class=\"type\">&lt;DataView&gt;</a> 推送到不在对象模式下的流有一个有趣的副作用。因为它是调用 <a href=\"#readablepushchunk-encoding\"><code>readable.push()</code></a>，该调用将结束读取过程。但是，因为参数是空字符串，没有数据被添加到可读缓冲区，因此用户没有数据可消耗。</p>"
            }
          ]
        }
      ],
      "type": "module",
      "displayName": "Stream"
    }
  ]
}